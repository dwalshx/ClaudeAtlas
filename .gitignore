# Dependencies
node_modules/

# Build output
dist/

# Astro
.astro/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Planning artifacts (keep separate)
.objective/

# Claude
.claude/

# Large scraper artifacts (rebuilt each run)
data/skills-raw.json
data/skills.json.partial
data/etag-cache.json
data/scrape-log.txt
data/star-history.json.partial
scripts/backfill-star-history.log
data/skills.json.birth-partial
scripts/backfill-skill-birth-dates.log

# Generated badge SVGs (rebuilt each build via scripts/generate-badges.js)
public/badge/
# Generated registry (rebuilt each build via scripts/generate-registry.js)
public/skills-registry.json

# Embed script log (not the ndjson itself — that's committed as a cache)
scripts/embed-skills.log

# Plugin scraper intermediate + raw output files
# Raw output is ~34 MB and gets rebuilt on each scrape. Filtered/scored
# data/plugins.json will be committed once Phase 3.0 filter-plugins.js ships.
data/plugins-raw.json
data/plugins-raw.json.partial
scripts/scrape-plugins.log
scripts/scrape-plugins-resume.log

# Phase 3.0.1 smoke harness output (regenerated each run)
data/smoke-output.json
