# Phase 3.1 — Plan Check (Rev 1)

**Verifier:** gsd-plan-checker
**Date:** 2026-05-17
**Plan under review:** `.planning/phases/3.1-filter-overhaul/3.1-PLAN.md` (Rev 1, 2,202 lines)
**Goal:** Transform catalog from curated 1,885 to comprehensive ~5-15k with embedding-based dedup + novelty + slug collision fix; daily pipeline still <25 min; site rendering survives; free-tier constraints preserved.

---

## Verdict: NEEDS_REVISION

**2 BLOCKERS, 4 FLAGS.** Plan is mostly excellent and decision-faithful, but two silent-failure landmines slipped past the planner.

One-line: enrich.js's `vectorizeId()` re-implementation is wrong (silent zero-match join on most production IDs), and `PRESERVED_FIELDS` in filter.js is not extended to carry the three new fields across daily runs — any enrich failure wipes the dedup state.

---

## Per-concern verdict table

| # | Concern | Verdict | Evidence |
|---|---|---|---|
| 1 | Old-slug source for redirect map | PASS | `scrape-discover-repos.js:184` and `scrape.js:479` both emit `owner/skillName` — identical to current committed skills.json shape. |
| 2 | `is_duplicate` semantics on rendering side | FLAG | Zero consumers of `is_duplicate`/`canonical_slug`/`novelty_score` in `src/`. Per Decision F, dedup is LOAD-BEARING. Without site-side filtering, catalog visibly inflates by 30-50% duplicates. Plan's own Recovery section concedes this. Either ship rendering-side filter or explicitly defer to 3.4 with rationale. |
| 3 | Atomic file write race | PASS | enrich.js uses temp+rename. Filter exits 0 cleanly on Track-1-only days. Crash-safe. |
| 4 | **Vector ID join via `vectorizeId()`** | **BLOCKER** | Plan re-implements vectorizeId differently from `embed-skills.js:91-101`. Real impl handles `>64 chars` with SHA fallback. **Most production skill IDs (`owner/repo/path/to/SKILL.md`) trivially exceed 64 chars** — silent zero-match join, enrich.js fails closed (all fields null), CI passes (no error), site ships with zero `is_duplicate=true`. Same shape as 3.0.x silent-fail bugs. |
| 5 | Missing embeddings (new skills) | PASS | `absent[]` branch leaves fields as null. Documented. |
| 6 | O(n²) at scale | FLAG | 5s @ 1,885 → 800s (~13 min) @ 20k. Approaches 25-min cap at upper bound. Add `if (elapsed > 600) warn(...)`. |
| 7 | Pipeline order / ID stability | PASS | `id = repo_full_name + skill_path` (no slug). Slug change doesn't change id. computeContentSha excludes slug — verified. Upload-vectors stable. |
| 8 | Redirects for never-deployed old URLs | PASS | All 13 colliding slugs are live in current skills.json. Redirects warranted. |
| 9 | Featured count post-recalibration | PASS | Task 9 gates "Featured > 2× before"; Task 10 gates count ∈ [5k, 15k]. |
| 10 | CLAUDE.md cross-references | PASS | No deep links into spec paragraphs. Task 7 rewording is orphan-free. |
| 11 | `/skills-registry.json` + sitemap | PASS | `generate-registry.js:67` reads `s.slug`; runs prebuild after enrich. Astro sitemap auto-reflows. |
| 12 | Worker deploy + skills.json timing | PASS | Worker imports `data/slug-redirects.json` at bundle time; same workflow run as build. Atomic. |
| 13 | **PRESERVED_FIELDS not updated for new fields** | **BLOCKER** | `filter.js:30 PRESERVED_FIELDS = ['skill_first_commit_at']`. Plan's Step 5 sets new fields to null every filter run, expecting enrich.js to repopulate. **If enrich fails (timeout, OpenAI quota, NDJSON corrupt), bot commits all-null enrichment fields. Dedup state wiped.** Daily pipeline must survive one-day enrich failure. |
| 14 | New fields vs prebuild ordering | PASS | enrich runs before `npm run build`. compute-similar.js reads NDJSON not skills.json metadata. |
| 15 | similar-skills.js slug-dedup hack | PASS | Becomes no-op after Task 2. Defer cleanup acceptable. |

## 3.0.x lessons applied?

| Past bug | Caught by current lens? |
|---|---|
| 3.0.0: V8 string limit | N/A (read pattern unchanged) |
| 3.0.0: pushed:> unsupported on code search | N/A (no code search added) |
| 3.0.1: gitignored skills-raw on runner | PARTIAL (inherits 3.0.1 bootstrap + graceful fallback) |
| 3.0.1: low ETag hit rate | N/A (Track 1/2 unchanged) |
| **Concern 4 — vectorizeId regex mismatch** | **CAUGHT.** Would have been the 3.1 silent-fail bug. |
| **Concern 13 — PRESERVED_FIELDS regression** | **CAUGHT.** Single failed enrich would have wiped dedup state until next enrich succeeded. |

Lens is working — surfaced both blockers the planner missed.

---

## Required revisions (BLOCKERS)

### BLOCKER 1 — Fix vectorizeId in enrich.js

**Where:** Task 4, `scripts/enrich.js` lines 929-931.

**Change:**
1. Export `vectorizeId` from `scripts/embed-skills.js` (add `export` to existing function at line 91; no behavior change).
2. In `scripts/enrich.js`: `import { vectorizeId } from './embed-skills.js';` — delete the local re-implementation.
3. Add to `scripts/enrich.test.js` at least one test case with a real-shaped >64-char skill id (e.g., `'jeremylongshore/claude-code-plugins-plus-skills/skills/some-deep/path/SKILL.md'`) and assert the join resolves via `sk_<hash>` vector id in the fixture.

### BLOCKER 2 — Extend PRESERVED_FIELDS in filter.js

**Where:** Task 2 instructions on `scripts/filter.js:30`.

**Change:**
```js
const PRESERVED_FIELDS = [
  'skill_first_commit_at',
  'is_duplicate',
  'canonical_slug',
  'novelty_score',
];
```

Add comment in filter.js Step 5 noting it depends on Step 4c (prior-enrichment merge) having already copied values for unchanged records — so `s.is_duplicate === undefined` correctly distinguishes "never enriched" from "enriched yesterday."

---

## Suggested improvements (FLAGS)

- **FLAG 1 — Site-side dedup posture.** Either add small task updating `src/lib/skills.js` to filter `is_duplicate=true` from default browse, OR explicit Out-of-scope clause noting catalog visibly inflates until 3.4.
- **FLAG 2 — Timing hard-warn.** Add `if (elapsed > 600) warn('enrich exceeded 10 min — consider HNSW migration per R2')`.
- **FLAG 3 — Provenance comment in slug.js docstring** explaining "old slug" provenance.
- **FLAG 4 — Task 9 partial-data caveat** when OPENAI_API_KEY unavailable locally: "audit INCOMPLETE — vectors stale; flag explicitly."

---

## Recommendation

Return to planner for Rev 2 addressing BLOCKERS 1 + 2. Fold FLAGS 1-4 in same pass. Re-check focuses only on:
- enrich.js imports vectorizeId from embed-skills.js
- enrich.test.js has >64-char-id test exercising SHA branch
- filter.js PRESERVED_FIELDS extended
- Site-side dedup posture made explicit (implemented OR deferred-with-rationale)
