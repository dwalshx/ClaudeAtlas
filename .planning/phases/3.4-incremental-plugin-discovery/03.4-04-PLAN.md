---
phase: 3.4-incremental-plugin-discovery
plan: 04
type: execute
wave: 4
depends_on: ["03.4-03"]
files_modified:
  - .github/workflows/daily-scrape.yml
autonomous: false
requirements: [R-1, R-5, R-6, R-8]
user_setup: []
must_haves:
  truths:
    - "A feature-branch dispatch of daily-scrape.yml with PLUGINS_ENABLED='true' completes the warm plugin discovery AND the whole job under 360 min with margin"
    - "filter-mcps.js still derives >=10 MCP repos from the walk output on the branch run"
    - "PLUGINS_ENABLED='true' is flipped on main ONLY AFTER the green, under-budget branch measurement"
    - "The first warm SCHEDULED run after the flip is green AND data/history/<today>.json is committed (no lost snapshot)"
  artifacts:
    - path: ".github/workflows/daily-scrape.yml"
      provides: "PLUGINS_ENABLED='true' on main, re-enabling the gated plugin/MCP pipeline"
      contains: "PLUGINS_ENABLED: 'true'"
  key_links:
    - from: "branch measurement (warm plugin wall-clock + total job duration)"
      to: "the flag flip decision"
      via: "GO only if total < 360 min with margin AND >=10 MCP repos"
      pattern: "PLUGINS_ENABLED"
    - from: "flag flip on main"
      to: "first warm scheduled cron"
      via: "06:30 UTC run green + history committed"
      pattern: "data/history"
---

<objective>
Validate the incremental rework on a feature branch, then flip
`PLUGINS_ENABLED='true'` on main — the one remaining step from Phase 3.3. This
mirrors the Phase 3.3 Plan 07 measure-before-flip discipline EXACTLY (CONTEXT:
LOCKED, non-negotiable, Pitfall 5).

Why human-gated (Pitfall 5): if the plugin step still overruns, the job dies at
"Run plugin discovery" BEFORE the skills history-snapshot commit, and
`data/history/<today>.json` is permanently lost. So the flip happens ONLY after a
branch-measured warm run is green AND under budget with margin. A branch run is
safe because publish/deploy/KV/commit steps gate to `refs/heads/main`.

Purpose: Prove the budget fit, then re-enable the pipeline.
Output: The flag flip on main + an observed green first-warm scheduled run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.4-incremental-plugin-discovery/3.4-RESEARCH.md
@.planning/phases/3.4-incremental-plugin-discovery/03.4-VALIDATION.md
@.planning/phases/3.3-plugin-pages/3.3-07-MEASUREMENT.md
@CLAUDE.md

<notes>
- `gh workflow run` via the user's fine-grained PAT 403s (lacks `workflow` scope —
  STATE.md "PAT scope"). Branch dispatch + flag-flip merge are done via the GitHub
  UI by the user. These are checkpoint:human-action steps by necessity.
- The bootstrap release `plugins-raw-ndjson-bootstrap` + warm cache
  `plugins-raw-ndjson-bootstrap-1` are already in place (3.3 Task 1) — the branch
  run restores them warm; the resume-seed fix (Plan 01) is what finally makes that
  warm cache actually skip the corpus.
- Budget target (RESEARCH Budget Projection): plugin subtotal ~21-46 min warm →
  combined ~290-330 min vs 360 cap. The binding uncertainty is the GROWN skills
  baseline (~50k), so the COMBINED total must be read off the real branch run.
</notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Branch-dispatch daily-scrape.yml with PLUGINS_ENABLED='true' (measurement)</name>
  <what-built>
    The full incremental rework (Plans 01-03): seed-from-OUTPUT skip (A), GraphQL
    engagement refresh (B), pushed_at re-walk gate + recursive-tree walk + weekly
    safety net (C). The plugin/MCP page code + bootstrap release/cache are already
    live from Phase 3.3.
  </what-built>
  <how-to-verify>
    1. Create a throwaway feature branch from the merged phase work, e.g.
       `feat/measure-plugins-3.4`, with `PLUGINS_ENABLED: 'true'` set in
       daily-scrape.yml's `env:` block (line 77) ON THAT BRANCH ONLY.
       (Optionally, to measure the worst-case weekly full re-walk on any day, set
       the dispatch/env `PLUGINS_FULL_REWALK=1`; otherwise the run measures the
       cheap steady-state delta path.)
    2. In the GitHub UI: Actions → "Daily Scrape & Deploy" → Run workflow → select
       branch `feat/measure-plugins-3.4`. (The fine-grained PAT can't `gh workflow
       run`; use the UI.) Branch runs are publish/deploy/KV/commit-gated to main, so
       this touches nothing in prod.
    3. When the run finishes (or is cancelled), read the step durations:
       - "Run plugin discovery (scrape-plugins)" wall-clock — confirm the resume log
         shows "~7,300 known repos seeded from OUTPUT" (NOT a counter climbing from 0
         → Pitfall 1 warning sign) and the step finishes in single-digit-to-low-tens
         of minutes, NOT hours.
       - TOTAL job duration < 360 min WITH MARGIN (target ~290-330 per RESEARCH).
       - "Filter MCPs" step log: confirm filter-mcps reports
         "Repos with mcp_servers > 0: N" with N >= 10 (R-5 — the MIN_MCP_REPOS gate).
    4. Record the numbers in a short measurement note (mirror
       `3.3-07-MEASUREMENT.md`): plugin wall-clock, total job, MCP repo count,
       GO/NO-GO.
  </how-to-verify>
  <resume-signal>
    Paste the plugin-discovery step duration, the total job duration, and the
    filter-mcps "Repos with mcp_servers > 0" count. Reply GO if total < 360 min with
    margin AND MCP count >= 10; otherwise NO-GO with the numbers.
  </resume-signal>
  <action>PAUSE. Present the branch-create + UI-dispatch steps to the user (PAT cannot dispatch via CLI). Do not proceed until the user pastes the plugin-discovery step duration, total job duration, and the filter-mcps MCP-repo count, and replies GO. On NO-GO, surface the documented next levers (GraphQL component-walk D in RESEARCH §D, or split plugin discovery to a weekly workflow — 3.2-CRON-AUDIT Option B) and stop.</action>
  <verify>User reply confirms a green/measured branch run with plugin step warm (minutes, not hours), total job < 360 min with margin, and filter-mcps "Repos with mcp_servers > 0" >= 10.</verify>
  <done>Branch measurement recorded; GO returned (total < 360 min with margin, MCP repos >= 10), OR NO-GO with the next-lever decision documented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Flip PLUGINS_ENABLED='true' on main (GO only)</name>
  <what-built>
    A branch-measured, green, under-budget warm run from Task 1.
  </what-built>
  <how-to-verify>
    ONLY proceed if Task 1 returned GO.
    1. On a PR branch off main, change daily-scrape.yml line 77
       `PLUGINS_ENABLED: 'false'` → `PLUGINS_ENABLED: 'true'`. Update the
       surrounding comment (lines 61-75) to note the incremental rework (Phase 3.4)
       made the sweep fit, citing the Task 1 measurement numbers. Leave every gated
       step's `if:` condition unchanged — they already AND-combine the flag.
    2. Merge to main via the GitHub UI (the fine-grained PAT lacks
       `mergePullRequest`; UI merge per STATE.md "PAT scope").
    3. Note: a push-to-main event does NOT run scrape (push events skip the data
       pipeline and just fetch skills-latest + deploy). The plugin pipeline first
       exercises on the next SCHEDULED 06:30 UTC cron — that's Task 3's observation.
  </how-to-verify>
  <resume-signal>
    Confirm the merge SHA on main and that daily-scrape.yml line 77 reads
    `PLUGINS_ENABLED: 'true'`. Reply with the SHA.
  </resume-signal>
  <action>PAUSE. Only if Task 1 returned GO: present the one-line flag flip (daily-scrape.yml line 77 false→true) + comment update as a PR for the user to merge via the GitHub UI (PAT lacks mergePullRequest). Do not proceed until the user confirms the merge SHA on main.</action>
  <verify>daily-scrape.yml line 77 on main reads `PLUGINS_ENABLED: 'true'`; user confirms the merge SHA.</verify>
  <done>PLUGINS_ENABLED='true' merged to main; flip SHA recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Observe the first warm SCHEDULED run (R-8)</name>
  <what-built>
    PLUGINS_ENABLED='true' merged to main (Task 2).
  </what-built>
  <how-to-verify>
    After the next 06:30 UTC cron fires:
    1. `gh run list --workflow=daily-scrape.yml --limit 3 --repo dwalshx/ClaudeAtlas`
       — confirm the latest SCHEDULED run is green (success), under 360 min.
    2. `git log origin/main --oneline -3` — confirm a fresh bot data commit landed
       AND `git show --stat <bot-sha>` includes `data/history/<today>.json` (the
       unreplayable snapshot was NOT lost — R-6 satisfied).
    3. Spot-check the run log: plugin discovery completed warm, filter-mcps reported
       >=10 MCP repos, deploy succeeded. Optionally confirm plugin/MCP pages now
       render real data (they were live-but-empty before the flip):
       `curl -sI https://claudeatlas.com/plugins/ | head -1` → 200.
  </how-to-verify>
  <resume-signal>
    Confirm: latest scheduled run green + under 360 min, `data/history/<today>.json`
    committed, plugins rendering. Type "approved" to close Phase 3.4, or describe any
    issue.
  </resume-signal>
  <action>PAUSE until the next 06:30 UTC scheduled cron completes. Present the gh/git verification commands to the user. Do not close the phase until the user confirms the scheduled run is green under 360 min AND data/history/<today>.json committed. A timeout/snapshot-loss reopens to the NO-GO levers; a non-history bug is a follow-up fix.</action>
  <verify>Latest SCHEDULED daily-scrape run is green and < 360 min; `data/history/<today>.json` is in the latest bot commit; `curl -sI https://claudeatlas.com/plugins/` returns 200.</verify>
  <done>First warm scheduled run green + under budget + history snapshot committed + plugins rendering; user types "approved". Phase 3.4 complete.</done>
</task>

</tasks>

<verification>
- Task 1: branch run green (or measured), plugin step warm (not hours), total < 360 min, MCP repos >= 10.
- Task 2: daily-scrape.yml line 77 == `PLUGINS_ENABLED: 'true'` on main.
- Task 3: first scheduled cron green + `data/history/<today>.json` committed + plugins render.
</verification>

<success_criteria>
The whole daily run (skills + plugins + build + deploy) fits under 360 min with
margin; `PLUGINS_ENABLED='true'` on main; the first warm scheduled run is green
with the daily history snapshot intact. Phase 3.4 done = the one remaining step
from Phase 3.3 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/3.4-incremental-plugin-discovery/3.4-04-SUMMARY.md`
(record the measurement numbers, the flip SHA, and the first-warm-run observation —
mirror 3.3-07-MEASUREMENT.md's format).
</output>
