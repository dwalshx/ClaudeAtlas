# Plan 3.4-04 Task 1 — Branch Measurement (CONDITIONAL — fix before flip)

**Date:** 2026-06-14 (UTC Sunday → worst-case full re-walk path)
**Branch run:** `feat/measure-plugins-3.4` → daily-scrape.yml dispatch, run [27510524011](https://github.com/dwalshx/ClaudeAtlas/actions/runs/27510524011)
**Verdict:** Incremental rework WORKS; run succeeded under 360 min. But the **Sunday full re-walk margin is too thin** → **do NOT flip yet**; split the weekly full re-walk to its own workflow first (Plan 3.4-05), then flip.

## Headline: the core fix is proven

```
[discovery] Total unique repos with plugin/marketplace files: 7628
[gate] periodicFull=true (UTC day 0)                     ← worst-case Sunday full re-walk
resume: 7339 known repos seeded from OUTPUT+partial      ← Change A WORKS (vs 3.3's empty-seed NO-GO)
Repos with mcp_servers > 0: 47                           ← R-5 PASS (≥10)
```

Run **conclusion: success** (vs 3.3 which couldn't finish in 6h). All downstream
steps green (filter/embed/enrich/build). The `processedSet` seed-from-OUTPUT fix
(Change A) is confirmed working.

## Timings (worst-case Sunday full re-walk)

| Phase | Duration |
|-------|----------|
| Skills (Track1+2, embed, enrich) | ~45 min |
| **Plugin discovery total** | **~255 min** |
| ↳ discovery search (search/code, 13 code-search 403 waits) | ~9 min |
| ↳ GraphQL refresh, Change B (147 batches, **23 GraphQL 403 waits ≈ 23 min**) | ~35 min |
| ↳ **full component re-walk, Change C (~7,300 repos, recursive-tree)** | **~205 min** |
| Build Astro (HNSW win — was ~94) | ~22 min |
| **TOTAL (branch)** | **~322 min** / 360 cap |

Estimated TOTAL on a real **main** run (+ KV publish / Vectorize / deploy / commit,
which the branch skipped): **~332 min — ~28 min margin.**

## Verdict — GO 6 days, too tight on Sunday

- **6 days/week (steady-state):** the Change-C `pushed_at` gate re-walks only
  *changed* repos → plugin discovery is minutes; total **~2h. Comfortable.**
- **Sunday (full re-walk):** the ~205-min full component re-walk pushes the total
  to ~322 min branch / ~332 min main — under 360 but **only ~28 min margin**, and
  the skills build/enrich are O(n²) on a *growing* catalog. A future Sunday could
  breach 360 and die before the end-of-run `data/history/<today>.json` commit
  (Pitfall 5 — unreplayable snapshot lost).

## Decision (user, 2026-06-14): fix before flip

Move the weekly full re-walk OUT of the daily cron into its own weekly workflow
(3.2-CRON-AUDIT "Option B", pre-documented). Then the daily cron only ever does
the cheap steady-state (~2h every day, comfortable margin) and the full re-walk
gets its own 6h budget with zero competition. → **Plan 3.4-05** (gap closure).
After it lands: re-measure a steady-state branch run, then flip
`PLUGINS_ENABLED='true'` on main (Plan 04 Tasks 2-3).

## Secondary finding (non-blocking, fold into 3.4-05 if cheap)

The daily GraphQL engagement-refresh (Change B) burned **~23 min on GraphQL 403
secondary-rate-limit waits** refreshing all 7,339 repos — same throttling class as
Track 1 (quick-260603-e96). It fits the daily budget on non-Sunday days but is
wasteful; worth a batch-size / inter-batch-delay tune.
