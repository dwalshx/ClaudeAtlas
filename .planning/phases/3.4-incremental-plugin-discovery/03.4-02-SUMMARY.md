---
phase: 3.4-incremental-plugin-discovery
plan: 02
subsystem: infra
tags: [graphql, github-api, plugin-discovery, scraper, classic-pat, engagement-refresh]

# Dependency graph
requires:
  - phase: 3.4-incremental-plugin-discovery (Plan 01)
    provides: buildProcessedSeedFrom() seeding processedSet from cached OUTPUT — skips known repos' REST walk
  - phase: 3.1 (Track 1 / scrape-pulse.js)
    provides: fetchRepoBatchGraphql batched GraphQL client + classic-PAT auth + partial-data handling
provides:
  - "refreshKnownRepos(records): batched GraphQL pre-walk refresh of known plugin repos' engagement signals on GraphQL's separate 5,000-pts/hr budget"
  - "applyFreshFields(records, freshByRepo): pure repo_* -> bare plugins-raw field mapper (in-place, miss = no-op, default_branch ?? existing)"
  - "Fresh per-repo pushed_at available in-memory (returned Map) for Plan 03's pushed_at re-walk gate"
  - "SCRAPE_PAT_CLASSIC wired into the daily-scrape plugin-discovery step for GraphQL auth"
affects: [3.4-03 (pushed_at re-walk gate consumes the fresh Map), 3.4-04 (branch measurement + flag flip)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "GraphQL engagement-refresh mirrors Track 1 (scrape-pulse.js): 50 repos/alias-batch, 100ms inter-batch delay, ~1 point/batch"
    - "In-process PAT swap: classic PAT for GraphQL batches, fine-grained REST PAT restored in finally — single process, no second cache restore"
    - "Graceful staleness: a repo the refresh can't resolve keeps prior cached values (miss = no-op), refresh failure never throws the scrape"

key-files:
  created: []
  modified:
    - scripts/scrape-plugins.js
    - scripts/__tests__/scrape-plugins.test.js
    - scripts/check-banned-patterns.js
    - .github/workflows/daily-scrape.yml

key-decisions:
  - "Reused fetchRepoBatchGraphql (scrape-pulse.js's proven Track 1 client) verbatim — no hand-rolled GraphQL client, per CONTEXT Don't-Hand-Roll constraint"
  - "PAT swap lives INSIDE refreshKnownRepos (capture/swap/finally-restore) rather than splitting the process — keeps REST discovery on SCRAPE_PAT and GraphQL on the classic PAT in one process, minimal blast radius"
  - "applyFreshFields is a separate exported pure helper so the field-mapping contract (repo_* -> bare) is unit-testable network-free"

patterns-established:
  - "Pre-walk engagement-refresh pass: refresh known repos' signals on the GraphQL budget BEFORE the REST discovery walk, so skipping their REST walk doesn't freeze scorer inputs"

requirements-completed: [R-3, R-7]

# Metrics
duration: 4 min
completed: 2026-06-14
---

# Phase 3.4 Plan 02: GraphQL engagement-refresh of known plugin repos Summary

**Batched GraphQL pre-walk refresh (Change B) that keeps known plugin repos' stars/forks/issues/pushed_at fresh on GraphQL's separate 5,000-pts/hr budget despite skipping their REST walk — mirroring Track 1's `fetchRepoBatchGraphql`, with classic-PAT auth and graceful staleness.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-14T19:48:01Z
- **Completed:** 2026-06-14T19:51:43Z
- **Tasks:** 3 (TDD: RED + GREEN + CI wiring)
- **Files modified:** 4

## Accomplishments

- `applyFreshFields(records, freshByRepo)` — pure, exported mapper that writes the `repo_*`-prefixed GraphQL fields onto the bare plugins-raw keys (`stars`/`forks`/`open_issues`/`pushed_at`/`topics`/`archived`/`license`/`language`/`description`/`default_branch`) in-place; a record with no fresh entry is left byte-for-byte unchanged; `default_branch` uses `?? existing`.
- `refreshKnownRepos(records)` — batched GraphQL pre-walk pass (50/batch, 100ms delay) over the in-memory cached corpus; swaps `GITHUB_TOKEN` to `SCRAPE_PAT_CLASSIC` for the GraphQL loop and restores the REST PAT in a `finally`; returns the fresh Map for Plan 03's `pushed_at` gate.
- Wired into `main()` after `loadCheckpoint()` / resume log, before the discovery walk, with a cold-corpus guard.
- `SCRAPE_PAT_CLASSIC` added to the daily-scrape "Run plugin discovery" step env; `if:` gate left untouched (Plan 04 flips the flag).

## Task Commits

Each task was committed atomically:

1. **Task 1: R-3 field-mapping unit test (RED)** - `704d50e` (test)
2. **Task 2: refreshKnownRepos() + applyFreshFields() (GREEN, Change B)** - `8158dc8` (feat)
3. **Task 3: wire SCRAPE_PAT_CLASSIC in daily-scrape.yml** - `28e71ae` (ci)

_TDD: RED (704d50e) then GREEN (8158dc8); no refactor commit needed._

## Files Created/Modified

- `scripts/scrape-plugins.js` - Added `fetchRepoBatchGraphql` import, `REFRESH_BATCH_SIZE`/`REFRESH_BATCH_DELAY_MS` constants, exported `applyFreshFields`, `refreshKnownRepos`, and the main() pre-walk refresh pass.
- `scripts/__tests__/scrape-plugins.test.js` - Added 3 R-3 tests (field mapping in-place, miss = no-op, null `default_branch` keeps existing) + the `applyFreshFields` import.
- `scripts/check-banned-patterns.js` - Bumped the allowlist line for the `plugins-meta.json` sidecar write (520 → 634) to track the line shift from the new code.
- `.github/workflows/daily-scrape.yml` - Added `SCRAPE_PAT_CLASSIC` env to the plugin discovery step.

## Decisions Made

- Reused `fetchRepoBatchGraphql` (Track 1's proven client) verbatim — the CONTEXT "Don't Hand-Roll" constraint and the plan's `<interfaces>` block both mandate reuse.
- PAT swap lives inside `refreshKnownRepos` (capture `restPat` → set classic → `finally` restore) rather than splitting the process or adding a second workflow step — keeps REST discovery/walk on `SCRAPE_PAT` and GraphQL on the classic PAT in a single process operating on one in-memory corpus.
- `applyFreshFields` extracted as a separate exported pure helper so the `repo_* -> bare` mapping contract is unit-testable with no network/token.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated check-banned-patterns allowlist line number for the shifted plugins-meta.json write**
- **Found during:** Task 2 (GREEN — running `npm run check:patterns`)
- **Issue:** The new code (import + constants + two helpers + the main() refresh block, ~114 lines) shifted the pre-existing `writeFileSync(META_PATH, JSON.stringify(meta, null, 2), 'utf-8')` from its allowlisted line 520 to line 634. The allowlist uses a ±1 line tolerance, so the shift registered the line as a "new" Banned-B violation and failed `check:patterns` (R-7 gate). The line itself is an already-allowlisted bounded ~1 KB sidecar write — not a real footgun.
- **Fix:** Bumped the `scripts/scrape-plugins.js` allowlist entry from `line: 520` to `line: 634` and appended the 3.4-02 Change B shift cause to the reason string (the reason string already documents prior 3.3-01 and 3.4-01 shifts).
- **Files modified:** scripts/check-banned-patterns.js
- **Verification:** `npm run check:patterns` → "lint mode: clean (0 baselined, 0 new)"
- **Committed in:** 8158dc8 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix is a one-line allowlist line-number bump for a line my own change shifted — standard maintenance of the line-sensitive lint baseline. No scope creep; the underlying write was already allowlisted as a bounded sidecar.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. (`SCRAPE_PAT_CLASSIC` is an existing GitHub Actions secret already used by Track 1 / scrape-pulse.js — no new secret to provision.)

## Next Phase Readiness

- Plan 03 (Change C — `pushed_at` re-walk gate) can consume the fresh Map returned by `refreshKnownRepos` (left in scope as `fresh` in main()) to decide which known repos changed since their last walk.
- Plan 04's branch measurement will validate the real warm plugin-discovery wall-clock (refresh + delta walk) against the 360-min cap before the `PLUGINS_ENABLED` flag flip.
- No blockers.

## Self-Check: PASSED

- All 4 modified files exist on disk; SUMMARY.md created.
- All 3 task commits found in git history (704d50e, 8158dc8, 28e71ae).
- Full suite: 191 pass / 0 fail. `npm run check:patterns` clean.

---
*Phase: 3.4-incremental-plugin-discovery*
*Completed: 2026-06-14*
