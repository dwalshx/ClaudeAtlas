---
plan: 03.2.1-07
phase: 3.2.1-hnsw-optimization
status: complete
completed: 2026-06-11
tasks_completed: 2
tasks_total: 2
---

# Plan 03.2.1-07 Summary — Phase gate: branch-dispatch validation run

## What was built

Nothing new — this plan was the end-to-end phase gate: prove Plans 01–06 against the real
corpus in CI, then human-approve the merge.

## Task 1: Push branch + dispatch validation run

- Branch `phase-3.2.1-hnsw` pushed to origin at `766e4e2` (executor agent died on a
  transient API error after the push but before the dispatch; orchestrator completed the
  dispatch inline on 2026-06-11).
- Dispatched: `gh workflow run daily-scrape.yml --ref phase-3.2.1-hnsw -f validate_ann=true`
  (with GH_TOKEN/GITHUB_TOKEN env cleared to unshadow the keyring token — known gotcha).
- Run: https://github.com/dwalshx/ClaudeAtlas/actions/runs/27368360578

## Task 2: Checkpoint — human confirms gates + merges (RESOLVED: approved)

**Run results (2026-06-11, 56,317 real vectors, warm caches) — ALL GATES GREEN:**

| Gate | Result | Threshold |
|------|--------|-----------|
| `[ann] engine=hnsw` (enrich) | PASS | required |
| `[ann] engine=hnsw` (build/compute-similar) | PASS | required |
| enrich timing | **170.1s** (~2.8 min, was ~59 min) | <900s |
| compute-similar timing (56,314 sets) | **138.1s** (~2.3 min, was ~162 min) | <900s |
| pair_recall @ 0.92 | **1.0000** | ≥0.995 |
| dup_flap | **0.0000** | <0.005 |
| topk_overlap | 0.9985 | ≥0.95 |
| novelty_jaccard | 0.9922 | ≥0.95 |
| `[validate-ann] RESULT` | **PASS** | PASS |

- Run wall-clock: 2h 11m total, of which ~81 min was the one-time exact-baseline recall
  validation (dispatch-gated; steady-state crons skip it). Projected steady-state daily
  run: **~50 min** vs the 5h14m baseline observed the same morning (run 27342739891).
- User approved and merged via GitHub UI: **PR #14**, merge commit `5bff173` on main
  (2026-06-11). Merge push triggered the fast push-event rebuild/deploy (main-gated
  steps active; production serving same code, new pipeline live for the next cron).

## Deviations

- Executor agent for Task 1 was killed by a transient API 529 between push and dispatch;
  orchestrator recovered by verifying push state and dispatching inline. No work lost.
- PR creation (`gh pr create`) done by orchestrator; merge by user via UI (fine-grained
  PAT lacks mergePullRequest — documented limitation).

## Post-merge follow-up (carried to phase verification)

- **Watch the next scheduled 06:30 UTC daily-scrape run on main**: expect total < ~150 min
  (projected ~50 min), `[ann] engine=hnsw` in both gate steps, both timing gates green.
  This is the steady-state observation from 03.2.1-VALIDATION.md "Manual-Only Verifications".
