---
phase: 3.2.1-hnsw-optimization
verified: 2026-06-11T22:30:00Z
status: passed
score: 27/27 must-haves verified
---

# Phase 3.2.1: HNSW Optimization — Verification Report

**Phase Goal:** Kill the O(N²) build long-pole — replace the two O(N²) cosine scans (compute-similar.js ~162 min, enrich.js dedup ~59 min at ~51k records) with approximate-NN (hnswlib-node behind scripts/lib/ann.js with exact fallback + CI engine gates), reclaiming cron headroom under the 360-min GHA cap and unblocking Phase 3.3 plugin re-enable. Fold in security Audit B (flag-don't-block content scanner + human-verified fixture-repo denylist extension). $0 added cost.

**Verified:** 2026-06-11 (post-merge: PR #14 → main at `5bff173`)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (27 across 7 plans)

| #  | Truth (plan) | Status | Evidence |
|----|--------------|--------|----------|
| 1  | ann.js loads on win32-arm64 (exact) AND ubuntu CI (hnsw) without npm ci failure (01) | VERIFIED | Local: `[ann] engine=exact` fallback fires, suite green. hnswlib-node in `optionalDependencies` only (not deps/devDeps). CI run 27368360578: `engine=hnsw` in both consumers. |
| 2  | topKNeighbors returns identical neighbor sets + exactly equal sims on both engines (01) | VERIFIED | ann.js:185-186 recomputes every candidate sim via `dot()` and discards `1-distance`. Parity test (ann.test.js Test 4) runs on real hnsw in lint CI; CI recall run: topk_overlap 0.9985, sims exact-verified. |
| 3  | Byte-identical determinism (sorted insertion + randomSeed=100) (01) | VERIFIED | ann.js:174 `initIndex(n, 16, 200, 100)` — M/efC/seed fixed; caller-sorts-by-id contract documented + enforced in enrich.js:135 and compute-similar.js:84. Determinism tests in ann.test.js. |
| 4  | npm test runs full suite; lint CI fails (not skips) on native-build breakage (01) | VERIFIED | package.json `"test": "node --test \"scripts/**/*.test.js\""`. lint-pipeline.yml:64-66 runs `npm test` with `ANN_REQUIRE_HNSW: '1'`; ann.test.js:183-184 `assert.fail` when env set and hnsw absent. |
| 5  | content_flags computed against RAW 5000-char body BEFORE Step 4b truncation (02) | VERIFIED | filter.js:285-291 annotation pass after Step 1b, before Step 4b (line 319); offset-2500 payload test in content.rules.test.js. |
| 6  | Flag-don't-block: matching records flagged but stay in catalog (02) | VERIFIED | content.rules.js never registered in filter-rules/index.js (grep: zero references); not called from isSlop. Spot-check: curl-pipe-sh payload → `["curl_pipe_sh","ignore_previous"]`, record not dropped. Branch run: 247 flagged, 0 blocked. |
| 7  | pipeline-stats.json gains content_flags summary every filter run (02) | VERIFIED | filter.js:550-552 writes `content_flags: contentFlagStats` (flagged count + per-rule counts) into STATS_PATH. Committed data file predates the merge (cron commit 80a4c5d 16:26 UTC vs merge 21:11 UTC) — first main-cron emission is the documented post-merge follow-up; branch validation run already produced plausible counts. |
| 8  | content_flags recomputed every run, NOT in PRESERVED_FIELDS (02) | VERIFIED | PRESERVED_FIELDS (filter.js:62-71) = skill_first_commit_at, is_duplicate, canonical_slug, novelty_score, bundled_in_plugins. Explicit exclusion comment at filter.js:58. |
| 9  | enrich dedup via ann.js topKNeighbors (K_DUP=64, efSearch=150), O(N²) loop gone (03) | VERIFIED | enrich.js:154-157 `topKNeighbors(items, 64, { efSearch: 150 })`; zero `for (let j = i + 1` loops remain in enrich.js. CI: enrich 170.1s (was ~3,500s). |
| 10 | Misses-only error model: candidate sims exact-verified before 0.92 compare (03) | VERIFIED | ann.js:185-186 exact dot; enrich.js:162 `sim > DUP_THRESHOLD` (0.92 unchanged, line 50). CI: dup_flap 0.0000, pair_recall 1.0000. |
| 11 | All existing enrich.test.js tests pass unchanged (03) | VERIFIED | npm test: 161 pass / 0 fail / 1 CI-only skip (ann parity, dev-box only). |
| 12 | Deterministic enrich output (03) | VERIFIED | id-sort before topKNeighbors (enrich.js:135); determinism test in enrich.test.js; fixed HNSW seed. |
| 13 | PRESERVED_FIELDS contract untouched incl. bundled_in_plugins (03) | VERIFIED | filter.js:70 `bundled_in_plugins` present; enrich.js only writes is_duplicate/canonical_slug/novelty_score; F-3 regression test green in suite. |
| 14 | compute-similar byte-compatible output shape via ann.js (04) | VERIFIED | compute-similar.js:69 pure `computeSimilar()` export; shape regression test scripts/__tests__/compute-similar.test.js (7 tests, green). |
| 15 | Naive O(n²) loop + per-row full sort gone (04) | VERIFIED | Zero all-pairs loops in compute-similar.js; uses topKNeighbors (line 98). CI: 138.1s for 56,314 sets (was ~162 min). |
| 16 | Shape test green against ORIGINAL naive impl BEFORE swap (04) | VERIFIED | Wave-0 ordering documented in 04-SUMMARY + VALIDATION.md; test exists and passes post-swap. |
| 17 | Push-event behavior preserved (stale 1,078-vector seed OK) (04) | VERIFIED | No event-conditional logic added to compute-similar.js; gates scoped `github.event_name != 'push'`. |
| 18 | Cron FAILS LOUDLY on exact-fallback engine (05) | VERIFIED | daily-scrape.yml:364 (enrich) + :515 (build) grep `[ann] engine=hnsw` with exit 1; `set -o pipefail` before both tee pipes (lines 354, 503). |
| 19 | Cron fails if enrich > 15 min or compute-similar > 15 min (05) | VERIFIED | awk gates `< 900` at daily-scrape.yml:368 and :519, with parse-failure hard-fail. timeout-minutes still 360 (line 60). |
| 20 | Dispatch-gated CI validation on real ~51k vectors with four gates (05) | VERIFIED | `validate_ann` workflow input (line 12); step gated `workflow_dispatch && validate_ann == 'true'` (line 318). validate-ann-recall.js (326 lines): pair_recall ≥ 0.995, dup_flap < 0.005, topk_overlap ≥ 0.95, novelty_jaccard ≥ 0.95, RESULT=PASS/FAIL with exit codes; hard-requires hnsw (local run exits 1 with FATAL — spot-checked). |
| 21 | Push-event deploys unaffected (05) | VERIFIED | Both gate steps + validation step are non-push / dispatch-gated; merge push-event rebuild/deploy succeeded (production serving). |
| 22 | 4 candidate repos HUMAN-verified before denylisting (06) | VERIFIED | Plan 06 Task 1 blocking checkpoint resolved (06-SUMMARY); common.rules.js:33 comment "human-verified 2026-06-11" with per-repo rationale. |
| 23 | Denylisted records dropped on EVERY filter pass (06) | VERIFIED | isFixtureRepo is first gate in isSlop (filter-rules/index.js); regression test in filter.test.js; denylist has exactly 6 entries (2 original + 4 new). |
| 24 | Denylist remains exact repo_full_name match, NOT path filter (06) | VERIFIED | common.rules.js:46-48 `FIXTURE_REPO_DENYLIST.has(record?.repo_full_name)` — Set exact match. |
| 25 | Full branch-dispatch cron GREEN: engine=hnsw both logs, timing gates, 4 recall gates PASS (07) | VERIFIED | CI run https://github.com/dwalshx/ClaudeAtlas/actions/runs/27368360578 — 56,317 real vectors: engine=hnsw both consumers, pair_recall 1.0000, dup_flap 0.0000, topk_overlap 0.9985, novelty_jaccard 0.9922, RESULT=PASS. User approved; merged as PR #14 (`5bff173`). |
| 26 | Reclaimed headroom is real: enrich and build wall-clocks in post-HNSW range (07) | VERIFIED | enrich 170.1s (~2.8 min, was ~59 min); compute-similar 138.1s (~2.3 min, was ~162 min). Both far under the 900s gates. |
| 27 | Total cron projection leaves room for Phase 3.3 plugin re-enable under 360-min cap (07) | VERIFIED | Run wall-clock 2h11m incl. ~81 min one-shot validation; steady-state projection ~50 min vs 5h14m same-morning baseline (run 27342739891). ~50 + ~48 (plugins) ≈ 100 min — ample headroom. |

**Score:** 27/27 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/lib/ann.js` | 5 exports, ≥80 lines, guarded createRequire | VERIFIED | 191 lines; exports annEngine, normalizeFloat32, dot, topKNeighbors, exactTopK; hnswlib-node behind try/catch createRequire (lines 34-40), never top-level import |
| `scripts/lib/__tests__/ann.test.js` | parity/determinism/ip-sign/fallback tests, ≥80 lines | VERIFIED | 252 lines, 7 tests; ANN_REQUIRE_HNSW fail-don't-skip branch present |
| `package.json` | hnswlib-node in optionalDependencies + test script | VERIFIED | `optionalDependencies: {"hnswlib-node":"^3.0.0"}`; NOT in dependencies or devDependencies; quoted-glob test script |
| `scripts/lib/filter-rules/content.rules.js` | CONTENT_RULES (11 rules) + scanContentFlags, ≥60 lines | VERIFIED | 75 lines, exactly 11 rules (6 exfil + 5 jailbreak classes), FP-calibration counts in comments, prefilter optimization |
| `scripts/lib/filter-rules/__tests__/content.rules.test.js` | per-rule + posture + offset tests, ≥60 lines | VERIFIED | 163 lines, 18 tests |
| `scripts/filter.js` | annotation before Step 4b + stats wiring | VERIFIED | Annotation at line 291 (after Step 1b, before Step 4b at 319); stats at line 552 |
| `scripts/enrich.js` | ANN-backed dedup, contains topKNeighbors | VERIFIED | Lines 43, 157; downstream BFS + compareForCanonical (line 88, 191) unchanged |
| `scripts/enrich.test.js` | determinism + large-cluster + passthrough, contains K_DUP | VERIFIED | Test G: 80-member cluster > K_DUP=64 survives BFS |
| `scripts/compute-similar.js` | computeSimilar export + topKNeighbors | VERIFIED | Pure export at line 69; invokedAsScript guard (main at line 142) |
| `scripts/__tests__/compute-similar.test.js` | shape regression, ≥60 lines | VERIFIED | 162 lines, 7 tests |
| `scripts/validate-ann-recall.js` | four-gate harness, ≥120 lines | VERIFIED | 326 lines; readNdjsonRecords (no banned readFileSync); hnsw hard-require |
| `.github/workflows/daily-scrape.yml` | validate_ann input + gates | VERIFIED | Input line 12; gate steps at 360-368 and 506-519; timeout-minutes 360 unchanged |
| `scripts/lib/filter-rules/common.rules.js` | FIXTURE_REPO_DENYLIST extended | VERIFIED | 6 entries; 4 new with human-verified comment + per-repo rationale |
| `scripts/filter.test.js` | denylist regression | VERIFIED | FIXTURE_REPO_DENYLIST referenced; suite green |
| `.github/workflows/lint-pipeline.yml` | npm test on real hnsw | VERIFIED | `npm test` with `ANN_REQUIRE_HNSW: '1'` (lines 64-66) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ann.js | hnswlib-node | guarded createRequire | WIRED | ann.js:34-40 try/catch; loud fallback warn + `[ann] engine=` log |
| lint-pipeline.yml | ann.test.js | npm test + ANN_REQUIRE_HNSW=1 | WIRED | env set on test step; test asserts fail when hnsw missing |
| enrich.js | ann.js | import {topKNeighbors, normalizeFloat32, dot, annEngine} | WIRED | enrich.js:43 `from './lib/ann.js'` |
| enrich.js | BFS + compareForCanonical | unchanged downstream | WIRED | compareForCanonical at lines 88/191; cluster/canonical logic intact |
| compute-similar.js | ann.js | import {topKNeighbors, normalizeFloat32, annEngine} | WIRED | compute-similar.js:36 `from './lib/ann.js'` |
| compute-similar.test.js | compute-similar.js | imports pure computeSimilar | WIRED | export consumed by test; invokedAsScript guard present |
| filter.js | content.rules.js | annotation loop after Step 1b, before Step 4b | WIRED | filter.js:24 import; :291 `s.content_flags = scanContentFlags(s)` |
| filter.js | pipeline-stats.json | stats.content_flags in main() | WIRED | filter.js:552 |
| common.rules.js | filter-rules/index.js | isFixtureRepo first gate in isSlop | WIRED | no dispatcher change needed; gate verified by suite |
| daily-scrape.yml | /tmp/enrich.log + /tmp/build.log | pipefail + tee, grep engine=hnsw + awk timing | WIRED | lines 354-355, 364-368, 503-504, 515-519 |
| validate-ann-recall.js | ann.js | engine override `{engine: 'hnsw'}` | WIRED | line 237 |
| phase branch | daily-scrape.yml dispatch | `-f validate_ann=true` | WIRED | run 27368360578 executed and passed |

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| enrich.js | neighborSets → is_duplicate/canonical_slug/novelty_score | skill-vectors.ndjson via readNdjsonRecords | Yes — CI run: 56,317 real vectors, 170.1s | FLOWING |
| compute-similar.js | neighborSets → similar-skills.json | skill-vectors.ndjson | Yes — CI run: 56,314 similar sets, 138.1s | FLOWING |
| filter.js content_flags | contentFlagStats → pipeline-stats.json | raw scraper body (5000-char) | Yes — 247 flagged on live 33k+ corpus (branch run); committed sidecar updates on next main cron (post-merge follow-up) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm test` | 162 tests: 161 pass, 0 fail, 1 skip (ann parity — hnsw unavailable on win32-arm64 by design; runs in CI) | PASS |
| Banned-pattern lint | `npm run check:patterns` | `lint mode: clean (0 baselined, 0 new)` | PASS |
| Content scanner fires | scanContentFlags on curl\|bash + injection payload | `["curl_pipe_sh","ignore_previous"]`; clean record → `[]`; 11 rules total | PASS |
| Harness engine guard | `node scripts/validate-ann-recall.js` (exact-fallback box) | exit=1, `[validate-ann] FATAL: hnsw engine unavailable` | PASS (fails loudly as designed) |
| O(N²) loop removal | grep `for (let j = i + 1` in enrich.js + compute-similar.js | zero matches (only the bounded exactTopK fallback inside ann.js remains, by design) | PASS |
| End-to-end real-data run | CI run 27368360578 (branch dispatch, validate_ann=true) | engine=hnsw both consumers; enrich 170.1s; compute-similar 138.1s; pair_recall 1.0000; dup_flap 0.0000; topk_overlap 0.9985; novelty_jaccard 0.9922; RESULT=PASS | PASS |

### Requirements Coverage

No formal REQUIREMENTS.md entries (inserted phase). Phase-internal requirement labels, all satisfied:

| Requirement | Source plan | Status | Evidence |
|-------------|-------------|--------|----------|
| ANN-parity | 01 | SATISFIED | ann.test.js Test 4 on real hnsw in lint CI; exact-verification contract in ann.js |
| ANN-determinism | 01 | SATISFIED | Fixed seed/M/efC; sorted-insertion contract; determinism tests |
| Lint-clean | 01, 02 | SATISFIED | check:patterns clean (0 new) |
| AuditB-flags | 02 | SATISFIED | 11 rules, flag-don't-block, pre-truncation scan, stats summary |
| Dedup-stability | 03 | SATISFIED | Suite green incl. large-cluster stress; dup_flap 0.0000 on real data |
| Similar-shape | 04 | SATISFIED | Shape regression locked pre-swap, green post-swap |
| Engine-gate | 05, 07 | SATISFIED | grep gates in both steps; observed green on CI run |
| Timing-gate | 05, 07 | SATISFIED | <900s awk gates; observed 170.1s / 138.1s |
| Real-recall | 05, 07 | SATISFIED | All four gates PASS on 56,317 real vectors |
| AuditB-denylist | 06 | SATISFIED | 4 human-verified entries added; exact-match contract preserved |

### Anti-Patterns Found

None blocking. Scanned all phase-modified files: no TODO/FIXME/placeholder stubs, no empty implementations, no banned NDJSON/string patterns (check:patterns clean). The intentionally-present O(n²) loop in ann.js `exactTopK` is the documented fallback/test/validation baseline, not a regression.

### Human Verification Required

None blocking. The Plan 06 checkpoint (fixture-repo intent) and Plan 07 checkpoint (validation-run approval + merge) were both completed by the user during execution.

**Documented post-merge follow-up (not a gap):** observe the next scheduled 06:30 UTC daily-scrape run on main — expect total < ~150 min (projected ~50 min), `[ann] engine=hnsw` in both gate steps, both timing gates green, and `content_flags` appearing in the committed `data/pipeline-stats.json`. Carried from 03.2.1-07-SUMMARY.md; inherently future-dated.

### Gaps Summary

No gaps. Both O(N²) scans are gone from the daily path (enrich ~59 min → 2.8 min; compute-similar ~162 min → 2.3 min, both measured on the real 56k-vector corpus in CI), the silent-fallback regression path is closed by engine + timing gates, recall is empirically perfect-to-near-perfect on the real embedding distribution, Audit B ships as designed (flag-don't-block scanner + 4 human-verified denylist entries), hnswlib-node costs $0 and survives the win32-arm64 dev box as an optionalDependency, and Phase 3.3 plugin re-enable has ~260 min of reclaimed headroom under the 360-min cap.

---

_Verified: 2026-06-11T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
