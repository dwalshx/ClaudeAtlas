---
phase: 3.2.1
slug: hnsw-optimization
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-10
planned: 2026-06-10
---

# Phase 3.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `03.2.1-RESEARCH.md` § Validation Architecture.
> Task/Plan/Wave columns filled by plan-phase 2026-06-10 (plans 03.2.1-01 … 03.2.1-07).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (`node --test`), Node 22 (local) / 22 (lint CI) |
| **Config file** | none — script-per-file convention (`scripts/**/*.test.js`, `scripts/**/__tests__/*.test.js`) |
| **Quick run command** | `node --test scripts/lib/__tests__/ann.test.js` (created in Plan 01) |
| **Full suite command** | `npm test` (= `node --test "scripts/**/*.test.js"` — added in Plan 01 Task 1; the quoted glob avoids executing the `scripts/test/` regression harnesses) |
| **Estimated runtime** | ~30–90 seconds (full suite, fixtures only) |

---

## Sampling Rate

- **After every task commit:** Run targeted `node --test <changed-area tests>` + `npm run check:patterns`
- **After every plan wave:** Run `npm test` (full suite)
- **Before `/gsd:verify-work`:** Full suite green + the Plan 07 branch-dispatch cron observed with `engine=hnsw`, both timing gates passing, and `[validate-ann] RESULT=PASS`
- **Max feedback latency:** ~90 seconds (local); real-recall validation is CI-only (needs warm `skill-vectors-*` GHA cache — local vector file is the stale 1,078-record seed)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-T2 | 03.2.1-01 | 1 | ANN-parity — exact and hnsw engines return identical neighbor sets/sims on small fixtures (sims exact-verified in ann.js, so equality is exact) | unit | `node --test scripts/lib/__tests__/ann.test.js` | ❌ created in 01-T2 | ⬜ pending |
| 01-T2 | 03.2.1-01 | 1 | ANN-determinism — same input ⇒ byte-identical output (sorted insertion + randomSeed=100), incl. EMBED_DRY_RUN-style vectors | unit | `node --test scripts/lib/__tests__/ann.test.js` | ❌ created in 01-T2 | ⬜ pending |
| 01-T3 | 03.2.1-01 | 1 | Lint-CI hnsw coverage — parity tests run on the REAL native engine on every PR; ANN_REQUIRE_HNSW=1 fails (not skips) on build breakage | smoke | lint-pipeline.yml test job (`npm test`) | ✅ extend | ⬜ pending |
| 03-T1/T2 | 03.2.1-03 | 2 | Dedup-stability — enrich on fixture corpus: clusters/canonical via ann path == exact semantics; 80-member cluster > K_DUP survives BFS; PRESERVED_FIELDS inputs untouched | unit | `node --test scripts/enrich.test.js` | ✅ extend | ⬜ pending |
| 04-T1 | 03.2.1-04 | 2 | Similar-shape — compute-similar output JSON shape locked BEFORE rewrite (slug keys, 4-dp rounding, tier fallbacks); stays green after engine swap | unit | `node --test scripts/__tests__/compute-similar.test.js` | ❌ created in 04-T1 | ⬜ pending |
| 05-T1 + 07 | 03.2.1-05/07 | 3-4 | Real-recall — pair-recall ≥ 0.995 @ 0.92; dup-flap < 0.5%; top-K overlap ≥ 95%; novelty top-5% Jaccard ≥ 0.95 (HNSW vs exact, REAL cached vectors) | integration (CI-only) | `gh workflow run daily-scrape.yml --ref <branch> -f validate_ann=true` → `[validate-ann] RESULT=PASS` | ❌ created in 05-T1 | ⬜ pending |
| 05-T2 | 03.2.1-05 | 3 | Engine-gate — non-push cron asserts `[ann] engine=hnsw` in BOTH enrich and build logs (no silent exact-fallback) | smoke | gate steps in daily-scrape.yml | ❌ created in 05-T2 | ⬜ pending |
| 05-T2 | 03.2.1-05 | 3 | Timing-gate — enrich < 900 s AND compute-similar < 900 s on cron (enrich's internal 600 s warn retained as soft tripwire) | smoke | gate steps in daily-scrape.yml | ❌ created in 05-T2 | ⬜ pending |
| 02-T1/T2 | 03.2.1-02 | 1 | AuditB-flags — 11 rules fire on fixtures; flag-but-pass posture; flags computed pre-truncation (offset-2500 payload test); stats summary in pipeline-stats | unit | `node --test scripts/lib/filter-rules/__tests__/content.rules.test.js` + `node --test scripts/filter.test.js` | ❌ created in 02-T1 | ⬜ pending |
| 06-T1/T2 | 03.2.1-06 | 2 | AuditB-denylist — 4 candidate fixture repos HUMAN-verified, approved subset added; denylisted records dropped on every pass | checkpoint + unit | `node --test scripts/filter.test.js` | ✅ extend | ⬜ pending |
| every | all | every | Lint-clean — `npm run check:patterns` green (ann.js + content.rules.js + validate-ann-recall.js add no banned patterns) | lint | `npm run check:patterns` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (scheduling resolution)

The research-identified test gaps are scheduled as the FIRST task touching each area (test-before/with-implementation), rather than a separate Wave 0 plan:

- [x] `scripts/lib/__tests__/ann.test.js` — Plan 01 Task 2 (tests written before ann.js implementation; ann.js is NEW code so no pre-existing behavior to lock)
- [x] `scripts/__tests__/compute-similar.test.js` — Plan 04 Task 1 (CRITICAL ORDERING: shape test written and green against the CURRENT naive implementation BEFORE the engine swap in Task 2 — this is the true Wave-0 baseline lock)
- [x] `scripts/lib/filter-rules/__tests__/content.rules.test.js` — Plan 02 Task 1 (tests-first TDD; new module)
- [x] CI validation workflow/step for real-vector recall — Plan 05 (dispatch-gated step in daily-scrape.yml; executed in Plan 07)
- [x] `"test"` npm script — Plan 01 Task 1

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| First scheduled cron post-merge completes with reclaimed headroom | Timing-gate (end-to-end) | Real 51k-record run only happens on schedule/dispatch; wall-clock is the success metric | `gh run list --workflow=daily-scrape.yml --limit=1` after merge; confirm total < ~150 min and engine/timing gate steps green (Plan 07 resume-signal) |
| 4 candidate fixture repos are genuinely scanner fixtures (not real skills) | AuditB-denylist | Human judgment on repo intent before denylisting | Plan 06 Task 1 checkpoint — open each repo on GitHub, approve list |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are checkpoints (Plans 06-T1, 07-T2)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 gaps covered (resolved as test-first tasks; see table above)
- [x] No watch-mode flags
- [x] Feedback latency < 90s (local paths)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending execution
