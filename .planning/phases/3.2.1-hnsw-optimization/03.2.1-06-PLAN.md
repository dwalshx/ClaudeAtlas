---
phase: 3.2.1-hnsw-optimization
plan: 06
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - scripts/lib/filter-rules/common.rules.js
  - scripts/filter.test.js
autonomous: false
requirements: [AuditB-denylist]

must_haves:
  truths:
    - "Each of the 4 candidate repos was verified by a HUMAN as a scanner/fixture repo (not real installable skills) before being denylisted — same bar as the original two entries"
    - "Approved repos' records are dropped on EVERY filter pass (re-scrape can never re-introduce them)"
    - "The denylist remains an exact repo_full_name match — NOT a path filter (95 legit example-path skills must survive)"
  artifacts:
    - path: "scripts/lib/filter-rules/common.rules.js"
      provides: "FIXTURE_REPO_DENYLIST extended with the human-approved subset of the 4 candidates"
      contains: "FIXTURE_REPO_DENYLIST"
    - path: "scripts/filter.test.js"
      provides: "Regression test: denylisted repos rejected by isSlop/filterRaw"
      contains: "FIXTURE_REPO_DENYLIST"
  key_links:
    - from: "scripts/lib/filter-rules/common.rules.js"
      to: "scripts/lib/filter-rules/index.js"
      via: "isFixtureRepo() — already first gate in isSlop(); no dispatcher change needed"
      pattern: "isFixtureRepo"
---

<objective>
Extend `FIXTURE_REPO_DENYLIST` with the 4 scanner-fixture repos surfaced by the Audit B FP-calibration scan — gated on human verification of each repo's intent (locked decision: "gated on human verification of repo intent"). This is the ONLY blocking change in Audit B; everything else is flag-don't-block (Plan 02).

Candidates (each hosts deliberate 'prompt-injection-test' fixture skills per the 2026-06-10 research scan):
1. majiayu000/claude-skill-registry
2. majiayu000/claude-skill-registry-data
3. liminal-ai/skill-scanner-ts
4. RekitRex21/Dino_Scan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.2.1-hnsw-optimization/03.2.1-RESEARCH.md
@scripts/lib/filter-rules/common.rules.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the 4 candidate repos are genuinely scanner fixtures</name>
  <what-built>The Audit B calibration scan (Plan 02 research) flagged 4 repos whose SKILL.md files contain prompt-injection-test fixtures. Before they are hard-blocked from the catalog forever, a human must confirm each repo's contents are eval/test fixtures rather than real, installable skills — the same judgment bar applied to the two existing denylist entries (claude-world/claude-skill-antivirus, cisco-ai-defense/skill-scanner).</what-built>
  <how-to-verify>
    Open each repo on GitHub and skim its README + a couple of SKILL.md files. A scanner-fixture repo typically: describes itself as a scanner/antivirus/eval corpus, contains deliberately malicious sample payloads, and is not something a user would `claude plugin install`.
    1. https://github.com/majiayu000/claude-skill-registry
    2. https://github.com/majiayu000/claude-skill-registry-data
    3. https://github.com/liminal-ai/skill-scanner-ts
    4. https://github.com/RekitRex21/Dino_Scan
  </how-to-verify>
  <resume-signal>Reply with the approved list — e.g. "approve all 4", or "approve 1,3,4 — skip 2 because ...". Only approved repos proceed to Task 2.</resume-signal>
  <action>PAUSE and present the 4 repo URLs above to the user with the verification rubric. Do not modify any file until the user replies with the approved subset.</action>
  <verify>User reply received naming the approved subset (possibly all 4 or fewer).</verify>
  <done>Each of the 4 candidates has an explicit human disposition: approved-for-denylist or skipped-with-reason.</done>
</task>

<task type="auto">
  <name>Task 2: Add approved repos to FIXTURE_REPO_DENYLIST + regression test</name>
  <files>scripts/lib/filter-rules/common.rules.js, scripts/filter.test.js</files>
  <read_first>
    - scripts/lib/filter-rules/common.rules.js (lines 14-43 — the denylist block + its comment contract)
    - scripts/filter.test.js (existing test conventions; Plan 02 already extended this file — build on its current state)
    - scripts/lib/filter-rules/index.js (isSlop ordering — isFixtureRepo is already the FIRST gate; no change needed there)
  </read_first>
  <action>
    1. In common.rules.js, append the HUMAN-APPROVED subset to the FIXTURE_REPO_DENYLIST Set, each with a trailing comment, e.g.:
       ```javascript
       // Phase 3.2.1 (Audit B FP-calibration scan, 2026-06-10; human-verified <date>):
       'majiayu000/claude-skill-registry',        // prompt-injection-test fixture corpus
       'majiayu000/claude-skill-registry-data',   // data twin of the above
       'liminal-ai/skill-scanner-ts',             // TS skill-scanner with embedded eval fixtures
       'RekitRex21/Dino_Scan',                    // scanner fixtures
       ```
       (Trim to the approved subset from Task 1. If the user skipped any, note the skip + reason in a comment AND in the SUMMARY.)
    2. Extend scripts/filter.test.js with a regression test: build a minimal raw record for each newly approved repo_full_name (valid name/description/body so ONLY the denylist can reject it) plus one benign control record; run filterRaw; assert the denylisted records are ABSENT from `capped` and the control record survives. Also assert `isFixtureRepo({ repo_full_name: '<entry>' }) === true` for each new entry (import from common.rules.js).
    3. Do NOT touch hasTemplateName/BIZ_SLOP/lang-dedup or any other rule in the file. The denylist stays an exact repo_full_name match.
  </action>
  <verify>
    <automated>node --test scripts/filter.test.js && npm test && npm run check:patterns</automated>
  </verify>
  <acceptance_criteria>
    - Every Task-1-approved repo_full_name appears as a quoted string inside the FIXTURE_REPO_DENYLIST Set in common.rules.js
    - scripts/filter.test.js contains a test asserting filterRaw drops records from those repos while a benign control survives
    - `node --test scripts/filter.test.js` exits 0; `npm test` exits 0
    - `npm run check:patterns` exits 0
  </acceptance_criteria>
  <done>Approved fixture repos are permanently excluded from the catalog with regression coverage; unapproved candidates documented as skipped.</done>
</task>

</tasks>

<verification>
- `npm test` green
- The denylist comment block records WHO/WHEN verified (date + "human-verified" marker), matching the documentation bar of the original two entries
</verification>

<success_criteria>
- AuditB-denylist requirement closed: candidates human-verified, approved subset blocking on every filter pass, regression-locked
</success_criteria>

<output>
After completion, create `.planning/phases/3.2.1-hnsw-optimization/3.2.1-06-SUMMARY.md`
</output>
