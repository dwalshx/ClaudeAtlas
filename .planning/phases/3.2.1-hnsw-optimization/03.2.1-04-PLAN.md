---
phase: 3.2.1-hnsw-optimization
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/compute-similar.js
  - scripts/__tests__/compute-similar.test.js
autonomous: true
requirements: [Similar-shape]

must_haves:
  truths:
    - "compute-similar.js produces a byte-compatible similar-skills.json shape (slug keys; entries {slug, score, name, category, quality_tier}; score rounded to 4 decimals; ≤ TOP_K entries; no self) via ann.js"
    - "The naive O(n²) loop + per-row full sort is gone; runtime at 51k drops from ~162 min to ~3-5 min (Build step long-pole eliminated)"
    - "A shape-regression test exists and was green against the ORIGINAL naive implementation BEFORE the engine swap (compute-similar previously had NO tests)"
    - "Push-event behavior preserved deliberately: stale 1,078-vector seed still produces a small similar-skills.json — do not 'fix' (RESEARCH.md Pattern 3 note)"
  artifacts:
    - path: "scripts/__tests__/compute-similar.test.js"
      provides: "Output-shape regression locked in BEFORE the rewrite (Wave 0 gap from VALIDATION.md)"
      min_lines: 60
    - path: "scripts/compute-similar.js"
      provides: "Pure computeSimilar() export + ANN-backed top-K"
      exports: ["computeSimilar"]
      contains: "topKNeighbors"
  key_links:
    - from: "scripts/compute-similar.js"
      to: "scripts/lib/ann.js"
      via: "import { topKNeighbors, normalizeFloat32 }"
      pattern: "from '../scripts'|from './lib/ann.js'"
    - from: "scripts/__tests__/compute-similar.test.js"
      to: "scripts/compute-similar.js"
      via: "imports pure computeSimilar() — requires the invokedAsScript guard"
      pattern: "computeSimilar"
---

<objective>
Rewrite `scripts/compute-similar.js` — the build's dominant cost (~162 min at 51k, the naive worst case: full n², per-pair norm recompute, per-row 51k-object allocation + full sort) — to use ann.js topKNeighbors. Lock the output shape with a regression test FIRST (against the current naive code), then swap engines under the green test.

Purpose: kills the #1 cron long-pole. Build step projected 162+ → ~25-40 min.
Output: testable compute-similar.js + its first-ever test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.2.1-hnsw-optimization/03.2.1-CONTEXT.md
@.planning/phases/3.2.1-hnsw-optimization/03.2.1-RESEARCH.md

<interfaces>
From scripts/lib/ann.js (Plan 01):
```javascript
export function normalizeFloat32(values: number[]): Float32Array
// items pre-normalized + caller-sorted by id; returns per-item {idx, sim}[] sorted sim DESC, self excluded
export function topKNeighbors(items: {id, vec}[], k, { efSearch?, engine? }): {idx, sim}[][]
```

Vector record shape in data/skill-vectors.ndjson (produced by embed-skills.js):
```javascript
{ id: string, values: number[1536], metadata: { slug, name, category, quality_tier, skill_id, _content_sha, entity_type } }
```

Current output shape that MUST NOT change (consumed by src/lib/skills.js via similar-skills.json — allowlisted bounded sidecar):
```javascript
{
  generated_at: "ISO 8601",
  count: <number of slug keys>,
  similar: {
    "author/skill-slug": [
      { slug, score: 0.8712,   // Math.round(score * 10000) / 10000
        name, category, quality_tier },  // metadata fallbacks: '' '' 'listed'
      ...max TOP_K entries
    ]
  }
}
```

CRITICAL pre-existing footgun: compute-similar.js currently calls `main()` unconditionally at top level — importing it from a test would execute the full I/O path. Task 1 adds the invokedAsScript guard (copy the pattern from scripts/enrich.js lines 332-340).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure computeSimilar() + lock the output shape with tests (BEFORE any engine change)</name>
  <files>scripts/compute-similar.js, scripts/__tests__/compute-similar.test.js</files>
  <read_first>
    - scripts/compute-similar.js (FULL file — 141 lines; the naive loop, slug-dedup, output assembly, TOP_K=3 comment block)
    - scripts/enrich.js (lines 332-340 — the invokedAsScript guard pattern to copy)
    - scripts/__tests__/embed-skills.test.js (test conventions for scripts/__tests__/)
  </read_first>
  <behavior>
    - Shape test: fixture of 8 vector records (8-dim values, metadata {slug, name, category, quality_tier}); computeSimilar(records, 3) returns { similar, count } where: keys are exactly the unique slugs; each entry array has ≤ 3 items; each item has exactly the keys {slug, score, name, category, quality_tier}; no entry's slug equals its own key (self excluded); score === Math.round(raw * 10000) / 10000
    - Collision-twin test: two records with the SAME metadata.slug → only one key in output (first wins, matching current behavior)
    - Missing-slug test: a record without metadata.slug is skipped entirely (not a key, not a neighbor)
    - Metadata-fallback test: record with metadata lacking name/category/quality_tier produces entries with '' / '' / 'listed'
    - Determinism test: two calls on the same input → JSON.stringify-identical results
  </behavior>
  <action>
    REFACTOR ONLY in this task — the cosine math stays the existing naive implementation so the new tests are validated against CURRENT production behavior:
    1. Add the invokedAsScript guard (verbatim pattern from enrich.js) so `main()` only runs when executed as a script.
    2. Extract the slug-dedup + similarity + output-assembly logic (current lines 83-125) into:
       ```javascript
       /**
        * Pure core: vector records in, { similar, count } out. No I/O.
        * @param {Array<{id, values, metadata}>} records
        * @param {number} topK
        */
       export function computeSimilar(records, topK = TOP_K) { ... }
       ```
       main() becomes: existsSync guard (unchanged empty-output branch) → readNdjsonRecords → computeSimilar(records, TOP_K) → write output object with generated_at (unchanged writeFileSync — bounded sidecar, already lint-clean).
    3. Create scripts/__tests__/compute-similar.test.js with the behavior-block tests. Run them — they MUST pass against the still-naive implementation. Commit as `test(3.2.1-04): lock compute-similar output shape (pre-rewrite baseline)`.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/compute-similar.test.js && npm run check:patterns</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function computeSimilar" scripts/compute-similar.js` matches
    - `grep "invokedAsScript" scripts/compute-similar.js` matches; `main()` is no longer called unconditionally at top level
    - scripts/__tests__/compute-similar.test.js exists with ≥ 5 test() blocks
    - `node --test scripts/__tests__/compute-similar.test.js` exits 0 with the NAIVE implementation still in place (git log shows the test commit precedes the engine-swap commit)
    - `npm run check:patterns` exits 0
  </acceptance_criteria>
  <done>Output shape is regression-locked against current production behavior; module is import-safe for tests.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the naive scan for ann.js topKNeighbors</name>
  <files>scripts/compute-similar.js</files>
  <read_first>
    - scripts/compute-similar.js (post-Task-1 state)
    - scripts/lib/ann.js (sorted-by-id caller contract; normalizeFloat32)
    - .planning/phases/3.2.1-hnsw-optimization/03.2.1-RESEARCH.md (§Pattern 3, §Pitfall 7 memory shape)
  </read_first>
  <action>
    Inside computeSimilar() ONLY (I/O and output assembly untouched):
    1. `import { topKNeighbors, normalizeFloat32, annEngine } from './lib/ann.js';` — delete the local cosineSimilarity() function.
    2. After slug-dedup, sort `deduped` by slug (ann.js determinism contract): `deduped.sort((a, b) => (a.metadata.slug < b.metadata.slug ? -1 : 1));` (Key ORDER of the output object changes from quality-sorted to slug-sorted — this is fine: consumers look up by slug key; the shape test from Task 1 must not assert key order.)
    3. Normalize + free the float64 source (Pitfall 7 — at 51k, holding both float64 values AND Float32 copies risks heap pressure alongside the Astro build):
       ```javascript
       const items = deduped.map((rec) => ({ id: rec.metadata.slug, vec: normalizeFloat32(rec.values) }));
       for (const rec of deduped) rec.values = null;   // drop float64 refs before querying
       ```
    4. `log(`ann: engine=${annEngine()}`);` then `const neighborSets = topKNeighbors(items, topK);` and assemble the same output entries from neighborSets[i] (sim is the cosine — same math as before since vectors are unit-norm; keep `Math.round(sim * 10000) / 10000` and the metadata fallbacks '' / '' / 'listed').
    5. KEEP `TOP_K = 3`. Replace the Phase 3.2 Task 13 comment block (lines 40-45) with: "Phase 3.2.1: runtime is now ~O(N log N) via scripts/lib/ann.js — TOP_K affects OUTPUT SIZE only (~40% of similar-skills.json per 2 entries). Safe to raise to 5 if the related-skills UI wants more; no runtime cost consideration anymore." Do NOT change the value in this phase — the shape baseline and downstream UI stay stable.
    6. Preserve the progress log cadence (`% 5000` is fine now) and the elapsed-seconds log line `computed ${n} similar-skill sets in ${elapsed}s` EXACTLY — Plan 05's timing gate parses this line from the build log.
    7. Do NOT touch the push-event stale-seed behavior (the existsSync branch + whatever vectors file is present gets processed — RESEARCH.md Pattern 3 says preserve deliberately).
  </action>
  <verify>
    <automated>node --test scripts/__tests__/compute-similar.test.js && npm test && npm run check:patterns</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cosineSimilarity" scripts/compute-similar.js` returns 0 (naive kernel deleted)
    - `grep "topKNeighbors(items, topK)" scripts/compute-similar.js` matches
    - `grep "rec.values = null" scripts/compute-similar.js` matches (Pitfall 7 memory release)
    - `grep "const TOP_K = 3" scripts/compute-similar.js` still matches
    - `grep "similar-skill sets in" scripts/compute-similar.js` still matches (Plan 05 gate parse target)
    - ALL Task 1 shape tests pass UNCHANGED after the swap
    - `npm run check:patterns` exits 0
  </acceptance_criteria>
  <done>compute-similar is ANN-backed under a green pre-swap shape baseline; output contract + timing-gate log line preserved.</done>
</task>

</tasks>

<verification>
- `npm test` green (shape tests passed both BEFORE and AFTER the engine swap — that sequencing is the whole point)
- `npm run check:patterns` green
- Local smoke: `node scripts/compute-similar.js` against the local 1,078-vector seed completes in seconds, logs `[ann] engine=exact`, writes a well-formed similar-skills.json
</verification>

<success_criteria>
- similar-skills.json shape is provably unchanged (same test suite green across both implementations)
- The O(n²) loop, per-pair norm recomputation, and per-row full sorts are gone
- TOP_K semantics documented as output-size-only; value unchanged at 3
</success_criteria>

<output>
After completion, create `.planning/phases/3.2.1-hnsw-optimization/3.2.1-04-SUMMARY.md`
</output>
