---
phase: 3.2.1-hnsw-optimization
plan: 06
subsystem: pipeline
tags: [security, denylist, audit-b, filter, fixture-repos]

# Dependency graph
requires:
  - phase: 3.2.1-hnsw-optimization
    provides: Plan 02 — Audit B content scanner (content_flags annotations surfaced the 4 candidate repos via the FP-calibration scan)
provides:
  - FIXTURE_REPO_DENYLIST extended with 4 human-verified scanner/fixture repos (6 entries total)
  - Regression test locking the 4 new entries — filterRaw drop + isFixtureRepo exact-match contract
affects: [3.3 plugin re-enable (denylist applies to all entity types via isSlop first gate)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Denylist promotion is human-gated: candidates surface via flag-don't-block scan; only human-verified repos enter FIXTURE_REPO_DENYLIST"
    - "Denylist stays exact repo_full_name match — never path-based (95 legit examples/-path skills must survive)"

key-files:
  created: []
  modified:
    - scripts/lib/filter-rules/common.rules.js
    - scripts/filter.test.js

key-decisions:
  - "All 4 candidates approved by human review 2026-06-11 (none skipped): 2 aggregator mirrors with prompt-injection fixtures, 1 scanner port, 1 scanner (preventive, 0 current records)"
  - "Regression test asserts a near-miss repo name (claude-skill-registry-2) does NOT match — locks the exact-match contract against future fuzzy-matching drift"

patterns-established: []

requirements-completed: [AuditB-denylist]

# Metrics
duration: ~5min execution (+ human-verify checkpoint span)
completed: 2026-06-11
---

# Phase 3.2.1 Plan 06: Fixture-Repo Denylist Extension Summary

**FIXTURE_REPO_DENYLIST extended from 2 to 6 entries with the 4 Audit B FP-calibration candidates, each human-verified 2026-06-11 as scanner/fixture repos (not installable skills), with regression tests locking the drop behavior and the exact-match contract**

## Performance

- **Duration:** ~5 min execution (Task 2 continuation; Task 1 was a blocking human-verify checkpoint resolved by user approval)
- **Completed:** 2026-06-11
- **Tasks:** 2 (1 checkpoint + 1 auto)
- **Files modified:** 2

## Accomplishments

- 4 new denylist entries appended to `FIXTURE_REPO_DENYLIST` in `scripts/lib/filter-rules/common.rules.js` under a dated, human-verified comment block matching the documentation bar of the original 2 entries
- 2 new regression tests in `scripts/filter.test.js`: (a) filterRaw drops a fully admissible record from each of the 4 repos while a benign control survives — only the denylist can be the rejection cause; (b) `isFixtureRepo()` returns true for each entry and false for a near-miss name
- All verification green: `node --test scripts/filter.test.js` 22/22, `npm test` 161 pass / 1 pre-existing skip, `npm run check:patterns` clean

## Task Commits

1. **Task 1: Human verification of 4 candidate repos** — checkpoint, no commit (user approved all 4)
2. **Task 2: Denylist extension + regression test** — `91d5956` (feat)

## Files Created/Modified

- `scripts/lib/filter-rules/common.rules.js` — FIXTURE_REPO_DENYLIST: +4 entries with per-entry rationale comments (aggregator-mirror / data twin / scanner-port / scanner)
- `scripts/filter.test.js` — `PHASE_321_DENYLIST_ADDITIONS` fixture list + 2 security regression tests; `isFixtureRepo` import added

## Human Verification Outcome (Task 1)

All 4 candidates **approved for denylist** — none skipped:

1. **majiayu000/claude-skill-registry** — auto-generated aggregator mirror; its 502 filtered records are duplicates of upstream skills plus 2 prompt-injection fixtures
2. **majiayu000/claude-skill-registry-data** — data twin of #1; 340 filtered mirror records + 2 prompt-injection fixtures
3. **liminal-ai/skill-scanner-ts** — TS port of already-denylisted cisco-ai-defense/skill-scanner; its 1 record is a malicious test fixture
4. **RekitRex21/Dino_Scan** — security scanner repo; 0 current filtered records (preventive entry)

## Decisions Made

- Comment block format follows the plan's contract: `// Phase 3.2.1 (Audit B FP-calibration scan, 2026-06-10; human-verified 2026-06-11):` with trailing per-entry rationale
- Added an exact-match negative assertion (near-miss repo name) beyond the plan's minimum, locking the "exact repo_full_name match ONLY" invariant documented in the denylist header

## Deviations from Plan

None - plan executed exactly as written (all 4 candidates approved, so no skip annotations were needed).

## Known Stubs

None — the denylist is fully wired: `isFixtureRepo` is already the first gate in `isSlop()` (no dispatcher change needed), so the new entries are blocking on every filter pass effective immediately.

## User Setup Required

None.

## Next Phase Readiness

- AuditB-denylist requirement closed: the only blocking mechanism in Audit B is live with regression coverage; everything else remains flag-don't-block (Plan 02)
- Next daily cron's filter pass will drop the ~843 records currently in the catalog from repos 1–3 (502 + 340 + 1); repo 4 is preventive

## Self-Check: PASSED
