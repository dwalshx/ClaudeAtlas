---
phase: 3.2.1-hnsw-optimization
plan: 03
subsystem: pipeline
tags: [hnsw, ann, dedup, novelty, enrich, embeddings]

# Dependency graph
requires:
  - phase: 3.2.1-hnsw-optimization (plan 01)
    provides: scripts/lib/ann.js — topKNeighbors/normalizeFloat32/dot/annEngine with exact-verification + determinism contracts
provides:
  - enrich.js dedup/novelty migrated from O(n²) pairwise cosine to ann.js top-K candidate retrieval (K_DUP=64, efSearch=150)
  - 4 regression-lock tests: determinism, >K_DUP cluster connectivity, PRESERVED_FIELDS passthrough, novelty top-1 exactness
affects: [3.2.1-04 compute-similar migration, 3.2.1-05 validation harness, 3.2.1-07 cron wiring]

# Tech tracking
tech-stack:
  added: []
  patterns: ["ANN consumers sort items by id before topKNeighbors (ann.js determinism contract)", "top-K + threshold filter + edge symmetrization emulates range query; BFS downstream unchanged"]

key-files:
  created: []
  modified:
    - scripts/enrich.js
    - scripts/enrich.test.js

key-decisions:
  - "present[] sorted by skill.id before ANN retrieval — satisfies ann.js determinism contract without reordering the output skills array (all downstream structures are present-index-based)"
  - "600s hard-warn retargeted as the engine-fallback regression tripwire (RESEARCH.md Pitfall 2) instead of being removed"
  - "nnSim symmetrized from neighbor sets (sim > nnSim[j] update) so novelty input matches the old pairwise semantics even when only one endpoint retrieves the pair"

patterns-established:
  - "ANN migration pattern: replace only the candidate-edge construction; BFS clustering, compareForCanonical, novelty assignment, absent-record placeholders, and all I/O stay byte-for-byte unchanged"

requirements-completed: [Dedup-stability]

# Metrics
duration: 4min
completed: 2026-06-11
---

# Phase 3.2.1 Plan 03: enrich.js Dedup Migration to ann.js Summary

**enrich.js dedup/novelty now runs on ann.js top-K candidate retrieval (K_DUP=64, efSearch=150) with a misses-only error model — the O(n²) pairwise loop is deleted, projected cron cost drops ~59 min → ~6-10 min at 51k**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-11T05:14:00Z
- **Completed:** 2026-06-11T05:17:54Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Replaced the O(n²) pairwise cosine block in `enrichSkills()` with `topKNeighbors` from `scripts/lib/ann.js`; local `normalizeFloat32`/`dot` copies removed in favor of imports
- `present[]` sorted by `skill.id` before retrieval per the ann.js determinism contract; edges symmetrized so BFS connectivity survives one-sided candidate retrieval
- All 8 pre-existing enrich tests pass UNMODIFIED — clusters, canonical selection, novelty, absent-vector handling, plugin dispatch all behaviorally identical
- 4 new regression locks: double-run determinism (50 skills, 2 planted clusters), 80-member sim=1.0 cluster (> K_DUP=64) collapsing to exactly 1 cluster / 79 duplicates at the oldest slug, `skill_first_commit_at` + `bundled_in_plugins` passthrough, and novelty == 1 − exact top-1 sim verified with independent `dot()` math
- Live dry-run against the local 1,078-vector seed: 1.2 s enrichment, logs `[ann] engine=exact` + stats (38 duplicates / 30 clusters — matches pre-migration semantics)

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace the pairwise loop with ann.js candidate retrieval** - `7ebbde8` (feat)
2. **Task 2: Extend enrich.test.js — determinism, large-cluster stress, field passthrough** - `5d26dd5` (test)

## Files Created/Modified

- `scripts/enrich.js` - ANN-backed dedup + novelty via shared ann.js; header comment + 600s tripwire message updated; parseArgs/dispatch/loadVectors/write path untouched
- `scripts/enrich.test.js` - Tests F-I appended using existing vec()/expectedVid() conventions; engine-agnostic (exact on dev, hnsw in lint CI under ANN_REQUIRE_HNSW=1)

## Decisions Made

- Kept `dot` imported in enrich.js even though the main loop no longer calls it directly is NOT the case — `dot` import is exercised by the test file's independent novelty verification and keeps the math single-sourced (plan-specified)
- Test G uses 32-dim vectors (plan allowed dim ≥ 8; 21 orthogonal slots needed for the 20 unique records + the cluster slot)
- Test I uses a float32-exact fixture (sim = 0.5 from equal-weight slots) so novelty equality asserts strictly with zero tolerance

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Full suite (153 tests) green on first run after each task — including the parallel executors' areas (compute-similar, filter-rules) at commit time.

## Known Stubs

None — no placeholder values or unwired data paths introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Wave 2 enrich consumer migrated; Plan 04 (compute-similar.js) follows the same pattern against the same ann.js API
- Plan 05 validation harness can replay enrich determinism in CI on the hnsw engine (warm skill-vectors cache); the `[ann] engine=` log line + retargeted 600s warn give it two regression tripwires
- Locked constants confirmed in place: DUP_THRESHOLD=0.92, K_DUP=64, efSearch=150; PRESERVED_FIELDS contract untouched (incl. bundled_in_plugins)

## Self-Check: PASSED

- scripts/enrich.js: FOUND
- scripts/enrich.test.js: FOUND
- 03.2.1-03-SUMMARY.md: FOUND
- Commit 7ebbde8 (Task 1): FOUND
- Commit 5d26dd5 (Task 2): FOUND

---
*Phase: 3.2.1-hnsw-optimization*
*Completed: 2026-06-11*
