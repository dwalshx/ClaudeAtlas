---
phase: 3.2.1-hnsw-optimization
plan: 07
type: execute
wave: 4
depends_on: ["03", "04", "05", "06"]
files_modified: []
autonomous: false
requirements: [Real-recall, Engine-gate, Timing-gate]

must_haves:
  truths:
    - "One full branch-dispatch cron run observed GREEN with: [ann] engine=hnsw in both enrich and build logs, both timing gates passed, and all four recall-validation gates PASS on the real ~51k vectors"
    - "Observed enrich wall-clock ≈ 6-10 min (was ~59) and Build step ≈ 25-40 min (was >162) — the reclaimed headroom is real, not projected"
    - "Total cron projection lands ~75-120 min, leaving room for the Phase 3.3 plugin re-enable (+~48 min) under the 360-min cap"
  artifacts: []
  key_links:
    - from: "phase branch"
      to: "daily-scrape.yml branch dispatch"
      via: "gh workflow run daily-scrape.yml --ref <branch> -f validate_ann=true (workflow registered on main runs the BRANCH's version; upload/publish/deploy/KV/commit steps are main-only — branch run is production-safe)"
      pattern: "validate_ann=true"
---

<objective>
Phase gate: prove the whole thing end-to-end on real data before merge. The real ~51k vector corpus exists only in the warm GHA caches, so this is necessarily a CI observation: dispatch daily-scrape.yml on the phase branch with `validate_ann=true`, watch the engine/timing/recall gates, and confirm the reclaimed headroom against the pre-phase baseline (enrich ~59 min, build >162 min, total 4.5-5.5 h).

This run is production-safe by existing design: upload-vectors, skills-latest publish, KV publish, deploy, and the data commit are ALL `github.ref == 'refs/heads/main'`-gated — a branch dispatch scrapes, filters, embeds (incremental, warm cache), enriches, validates, and builds without touching prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.2.1-hnsw-optimization/03.2.1-VALIDATION.md
@.planning/SESSION-HANDOFF-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the phase branch and dispatch the validation cron run</name>
  <files>(no repo files — git push + gh dispatch only)</files>
  <read_first>
    - .planning/SESSION-HANDOFF-2026-06-10.md (§Operational gotchas — the `gh workflow run` 403 fix and the cron-commits-to-main divergence note)
    - .github/workflows/daily-scrape.yml (post-Plan-05 state — confirm validate_ann input exists and main-only gating is intact before dispatching)
  </read_first>
  <action>
    1. Ensure all phase work (Plans 01-06) is committed on a single phase branch (e.g. `phase-3.2.1-hnsw`) and pushed to origin. Before pushing, `git pull --no-edit origin main` (the cron commits data files to main daily; clean auto-merge expected).
    2. KNOWN GOTCHA (handoff doc): the env `GITHUB_TOKEN`/`GH_TOKEN` is a fine-grained scrape PAT lacking `workflow` scope and it SHADOWS the keyring token. Clear before dispatching:
       - PowerShell: `$env:GH_TOKEN=$null; $env:GITHUB_TOKEN=$null`
       - bash: `unset GH_TOKEN GITHUB_TOKEN`
    3. Dispatch: `gh workflow run daily-scrape.yml --ref phase-3.2.1-hnsw -f validate_ann=true`
    4. If the dispatch still 403s (PAT scope), STOP and hand the dispatch to the user at the Task 2 checkpoint: GitHub UI → Actions → "Daily Scrape & Deploy" → Run workflow → branch `phase-3.2.1-hnsw` → validate_ann = true.
    5. Record the run URL (`gh run list --workflow=daily-scrape.yml --limit=1`). Do NOT poll aggressively — the run takes ~2.5-4 h (pipeline ~75-120 min + validation ~60-100 min); `gh api rate_limit` is shared with the scraper PAT budget. Check at most every 30 min, or hand off to the user checkpoint.
  </action>
  <verify>
    <automated>gh run list --workflow=daily-scrape.yml --limit=1 --json status,headBranch,url</automated>
  </verify>
  <acceptance_criteria>
    - A daily-scrape.yml run exists with headBranch == the phase branch, triggered via workflow_dispatch with validate_ann=true (visible in run config/inputs)
    - The run URL is recorded in the execution notes for Task 2
  </acceptance_criteria>
  <done>Validation run dispatched on the phase branch (by Claude, or handed to the user with exact UI steps).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the validation run proves the phase goal, then merge</name>
  <what-built>Plans 01-06: ann.js (hnswlib-node + exact fallback), ANN-backed enrich + compute-similar, Audit B content scanner + denylist, engine/timing gates, and the recall-validation harness — all exercised end-to-end by the Task 1 branch run against the real ~51k catalog.</what-built>
  <how-to-verify>
    On the Task 1 run page (or `gh run view <id> --log`):
    1. Step "Validate ANN recall vs exact baseline" — log shows all four lines ending PASS and `[validate-ann] RESULT=PASS`:
       pair_recall ≥ 0.995 · dup_flap < 0.005 · topk_overlap ≥ 0.95 · novelty_jaccard ≥ 0.95
    2. Step "Gate: ANN engine + enrich timing" — green; log shows `enrich elapsed: <N>s` with N < 900 (expect ~360-600 s vs the old ~3,500 s)
    3. Step "Gate: compute-similar engine + timing" — green; `compute-similar elapsed: <N>s` with N < 900
    4. Both gate logs confirm `[ann] engine=hnsw` was found (no fallback)
    5. Overall run duration: expect ~2.5-4 h INCLUDING the ~60-100 min one-shot validation; the steady-state daily run (without validate_ann) projects to ~75-120 min vs today's 4.5-5.5 h
    6. pipeline-stats.json artifact/log shows the new `content_flags` summary with plausible counts (curl_pipe_sh in the low hundreds at ~51k records)
    If anything fails: the gates' error messages name the failure class; report back and the phase loops to gap-closure planning.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with merging the phase branch to main via the GitHub UI (your PAT lacks mergePullRequest — use the web UI), or describe what failed. After merge, the next SCHEDULED 06:30 UTC run is the final steady-state observation: confirm total < ~150 min with both gates green.</resume-signal>
  <action>PAUSE and present the run URL from Task 1 plus the 6-point verification list above. Do not merge or declare the phase complete until the user confirms.</action>
  <verify>User confirms: RESULT=PASS on all four recall gates, both engine/timing gates green, enrich and build wall-clocks in the expected post-HNSW ranges.</verify>
  <done>Phase branch approved for merge; merge performed via GitHub UI; steady-state scheduled-run observation noted as the post-merge follow-up.</done>
</task>

</tasks>

<verification>
- Branch-dispatch run green end-to-end with RESULT=PASS and both gates passed
- Post-merge scheduled run green with engine=hnsw and reclaimed headroom (manual-only verification per VALIDATION.md §Manual-Only Verifications)
</verification>

<success_criteria>
- Real-recall, Engine-gate, and Timing-gate requirements observed PASSING on real data
- Phase goal proven: O(N²) long-poles gone, ~200+ min of headroom reclaimed under the 360-min cap, Phase 3.3 plugin re-enable unblocked, $0 added cost
</success_criteria>

<output>
After completion, create `.planning/phases/3.2.1-hnsw-optimization/3.2.1-07-SUMMARY.md`
</output>
