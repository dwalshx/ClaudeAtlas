---
plan_check_for: 3.1.1-streaming-foundation
checker: gsd-plan-checker
date: 2026-05-18
verdict: PASS
round: 2
---

# F1 Plan Check (Round 2)

## Verdict

**PASS.** Rev 2 cleanly resolves all 5 Round-1 BLOCKERs and all 7 FLAGs with concrete, traceable plan-level changes; Rev 3's `_header` sentinel mechanism in `scripts/lib/ndjson.js` is well-specified at the API level (read-filter + optional `opts.onHeader`, write `opts.header`, defensive filter in `loadAllSkillsSync`) and is exactly the contract F2 Rev 2 assumes (verified by cross-reading `3.1.2-PLAN.md` lines 99, 110, 155, 172, 282-289, 416, 564-568, 826-848, 1394). Working tree is clean (`git status --short` empty; commit `c1b8bd6` landed). Two minor residual concerns remain (logged as FLAGs below, not BLOCKers) but neither warrants Rev 4 — they can be handled inline during execution.

## Resolved from Round 1

| Round 1 finding | Status | Evidence |
|---|---|---|
| **B1** DOD-9/DOD-10 had zero implementing tasks | RESOLVED | T5 §5f wires `customPages`; T5 §5g adds `check-sitemap-stability.js`; V11+V12 added; pre-F1 snapshot captured in T1 step 0; two new check scripts in `must_haves.artifacts`. |
| **B2** `@astrojs/sitemap` silently drops Listed slugs | RESOLVED | T5 §5f rewrites `astro.config.mjs` to enumerate every slug regardless of tier; `postbuild` runs `check-sitemap-completeness.js` asserting `<loc>` count ≈ `wc -l data/skills.ndjson` ± 25. |
| **B3** Lint misses `.map(JSON.stringify).join` | RESOLVED | T1 step 3 Banned C + Banned D heuristic; Tests 9+11 added; CLAUDE.md "Pipeline footguns" enumerates all banned forms. |
| **B4** `daily-scrape.yml:136` unmigrated | RESOLVED | T5 §5e item 1 rewrites guard to `wc -l < data/skills.ndjson`; lint extended to scan YAML (Banned E); Test 10. |
| **B5** Worker `await res.text()` reintroduces bug class | RESOLVED (Decision A) | T5 §5d entirely replaces `loadSkillsMap` with `env.SKILLS_KV.get(slug)`; explicit "No `await res.text()` anywhere"; `wrangler.toml` adds SKILLS_KV binding; new `publish-kv.js` handles bulk upserts; 60k sentinel + 80k TODO dropped; KV pricing analysis grounded in cited Cloudflare docs. |
| **F1** Rollback contradicts post-T5 layout | RESOLVED | `rollback_plan` rewritten; T5 §5e tags `skills-latest-prev`; T8 expanded to 3 scenarios A/B/C; CLAUDE.md Recovery updated. |
| **F2** T2 uses shim | RESOLVED | T2 step 5 writes real `src/lib/skills.js` behind `F1_STREAMING_LOADER` flag; T2b runs `F1_STREAMING_LOADER=1 npx astro build`. |
| **F3** Existing `readFileSync` will trip strict mode | RESOLVED | Explicit `LINT_ALLOWLIST` for similar-skills.json + api-graph.json with Research §A citations. |
| **F4** OpenAI rate limit on 50k fixture | RESOLVED (Decision B) | T3 step 1 adds `EMBED_DRY_RUN=1` path with SHA-256-seeded fake vectors; V13+V14; R10 to risk register; CLAUDE.md "Embedding cost controls". |
| **F5** T2 fixture body_markdown too small | RESOLVED | Body changed to ~5000 chars; fixture ~450 MB; memory math re-derived. |
| **F6** Effort estimate optimistic | RESOLVED | "3-5 days conservative" with SESSION-MEMO citation. |
| **F7** Working tree clean | RESOLVED | Pre-flight check; verified `git status --short` empty. |

All 12 Round-1 findings addressed with concrete plan changes — not just acknowledgement.

## New BLOCKERs

**None.**

## New FLAGs

### NF-1 — `wrangler.toml` placeholder namespace ID risks Bug-8 replay
`wrangler.toml`'s SKILLS_KV stanza carries `id = "REPLACE_WITH_NAMESPACE_ID"` with a comment reminder. CLAUDE.md known-issue Bug 8 is cited. Recommend hardening: T5's automated `<verify>` block should `grep -v REPLACE_WITH_NAMESPACE_ID wrangler.toml` and fail if placeholder still present at merge time. Not a BLOCKER because T5 is `checkpoint:human-verify`, but cheap defensive addition.

### NF-2 — `check-sitemap-stability.js` baseline drift over execution timeline
Pre-F1 sitemap snapshot captured at T1; if F1 takes 3-5 days, daily-scrape cron runs against pre-F1 main several times. The script correctly reports only URLs that vanished (additions tolerated), but if a slug naturally drops between T1 capture and T5 deploy (repo deleted, content_sha changed), the check fires false positive. Recommend re-fetching current pre-deploy snapshot for diff: `missing = pre.filter(u => !liveSet.has(u) && current_pre_deploy_set.has(u))`. As written, operator must triage manually.

## Detailed findings per criterion

1. **All 3 V8 failure patterns caught by lint?** YES (Tests 5/6/9/10/11).
2. **All 18 V8 sites from Research §A?** YES via T3-T6, including the YAML escape at `daily-scrape.yml:136`.
3. **T2 exercises production loader?** YES (`F1_STREAMING_LOADER=1` against real `src/lib/skills.js`).
4. **T5 KV-backed Worker concrete?** YES — pseudocode, ~10-30 ms cold latency, edge-cached, KV pricing cited.
5. **Release-asset migration preserves reproducibility?** YES (`skills-latest-prev` tagging).
6. **3-scenario rollback rehearsal?** YES (T8 A/B/C).
7. **Cherry-pick disposition matches Research §G?** YES.
8. **DOD-9 + DOD-10 verified by V11 + V12?** YES, both run automatically in `postbuild`.
9. **Header sentinel mechanism (X1) correct?** YES, contract symmetric with F2 Rev 2.

## Summary recommendation

**PASS.** Plan is ready for execution. NF-1 and NF-2 are documentable inline by the executor and do not warrant Rev 4. T5 remains highest-blast-radius; "3-5 days conservative" estimate well-calibrated; T5's split into 5a-5h gives clear suture lines.

Proceed with `/gsd:execute-phase 3.1.1-streaming-foundation` after F2 Rev 2 plan-check also passes (so the cross-phase contract is fully validated).
