---
phase: 3.2-plugin-and-mcp-scoring
plan: 01
type: execute
wave: 1
depends_on: ["3.1.2-polymorphic-envelope", "3.1.4-filter-v2-writer"]
autonomous: true
requirements:
  - 3.2-DOD-1
  - 3.2-DOD-2
  - 3.2-DOD-3
  - 3.2-DOD-4
  - 3.2-DOD-5
  - 3.2-DOD-6
  - 3.2-DOD-7
  - 3.2-DOD-8
  - 3.2-DOD-9
  - 3.2-DOD-10
  - 3.2-DOD-11
  - 3.2-DOD-12
files_modified:
  - src/lib/types.d.ts
  - src/lib/types.js
  - scripts/lib/entity-version.js
  - scripts/lib/legacy-skill-reader.js
  - scripts/lib/scorers/index.js
  - scripts/lib/scorers/plugin.scorer.js
  - scripts/lib/scorers/mcp.scorer.js
  - scripts/lib/filter-rules/index.js
  - scripts/lib/filter-rules/plugin.rules.js
  - scripts/lib/filter-rules/mcp.rules.js
  - scripts/lib/filter-rules/common.rules.js
  - scripts/lib/recipes/index.js
  - scripts/lib/recipes/plugin.recipe.js
  - scripts/lib/recipes/mcp.recipe.js
  - scripts/lib/embedding-input/index.js
  - scripts/lib/embedding-input/skill.js
  - scripts/lib/embedding-input/plugin.js
  - scripts/lib/embedding-input/mcp.js
  - scripts/lib/bundled-links.js
  - scripts/filter-plugins.js
  - scripts/filter-mcps.js
  - scripts/embed-skills.js
  - scripts/enrich.js
  - scripts/upload-vectors.js
  - scripts/generate-feeds.js
  - scripts/generate-llms-txt.js
  - scripts/check-banned-patterns.js
  - .github/workflows/daily-scrape.yml
  - CLAUDE.md
must_haves:
  truths:
    - "Plugin records exist as EntityRecord<PluginExtra> with entity_type='plugin' in data/plugins.ndjson, written via writeNdjsonStreaming + buildHeader('plugin')"
    - "MCP records exist as EntityRecord<McpExtra> with entity_type='mcp_server' in data/mcp-servers.ndjson, written via writeNdjsonStreaming + buildHeader('mcp_server')"
    - "Plugin scorer + MCP scorer + their rule packs + recipes are registered in the F2 registries (scorers/index.js, filter-rules/index.js, recipes/index.js)"
    - "Plugin filter applies MIN_DESCRIPTION_LENGTH=50, COMPONENT_FLOOR>=1, HAS_MANIFEST_OR_LISTING; MCP filter applies HAS_TRANSPORT_DECLARED + >=1 tool"
    - "Percentile tiers (10/30/60) applied independently per entity_type — ~450 Featured plugins at N=4500, ~4 Featured MCPs at N=38"
    - "Bidirectional bundle graph populated: skill.bundled_in_plugins[], plugin.extra.bundled_skills[]/bundled_agents[]/bundled_commands[]/bundled_hooks[]/bundled_mcp_servers[]"
    - "Plugin + MCP embedding inputs produce ~200-char strings; vectors uploaded to Vectorize with metadata.entity_type set correctly"
    - "Plugin enrich runs cosine dedup at N=~4500; MCP enrich skipped (N=38 below dedup threshold)"
    - "Feeds (whats-new, trending, notable) emit mixed-type entries with _claudeatlas.type_chip in {'skill','plugin','mcp_server'}"
    - "llms.txt count line includes plugin + mcp_server counts dynamically"
    - "daily-scrape.yml runs plugin discovery + filter + embed + enrich steps; branch-CI dispatch validates end-to-end at production scale"
    - "Cron runtime audit completed; mitigation applied so post-3.2 cron stays under 5h timeout"
  artifacts:
    - path: "scripts/lib/scorers/plugin.scorer.js"
      provides: "scorePlugin(record) returning 0-100 via 7-signal composite with Manifest-Completeness replacing Frontmatter"
      contains: "export function scorePlugin"
    - path: "scripts/lib/scorers/mcp.scorer.js"
      provides: "scoreMcp(record) returning 0-100 via 7-signal composite with MCP-Manifest-Completeness signal"
      contains: "export function scoreMcp"
    - path: "scripts/lib/filter-rules/plugin.rules.js"
      provides: "isPluginSlop(record) + HAS_MANIFEST_OR_LISTING + COMPONENT_FLOOR gates"
      contains: "export function isPluginSlop"
    - path: "scripts/lib/filter-rules/mcp.rules.js"
      provides: "isMcpSlop(record) + HAS_TRANSPORT_DECLARED gate"
      contains: "export function isMcpSlop"
    - path: "scripts/lib/recipes/plugin.recipe.js"
      provides: "pluginRecipe — DiscoveryRecipe shape matching skillRecipe pattern"
      contains: "export const pluginRecipe"
    - path: "scripts/lib/recipes/mcp.recipe.js"
      provides: "mcpRecipe — DiscoveryRecipe shape"
      contains: "export const mcpRecipe"
    - path: "scripts/lib/embedding-input/index.js"
      provides: "buildEmbeddingInput(record) registry dispatching by entity_type"
      contains: "export function buildEmbeddingInput"
    - path: "scripts/lib/embedding-input/plugin.js"
      provides: "name + description + manifest.keywords joined, ~200 chars"
      contains: "export function buildPluginEmbeddingInput"
    - path: "scripts/lib/embedding-input/mcp.js"
      provides: "name + description + transport + tool names"
      contains: "export function buildMcpEmbeddingInput"
    - path: "scripts/lib/bundled-links.js"
      provides: "linkBundles(plugins, skills) — bidirectional graph populator"
      contains: "export function linkBundles"
    - path: "scripts/filter-plugins.js"
      provides: "Reads plugins-raw.ndjson, runs slop+manifest+component gates, scores, assigns percentile tiers, writes plugins.ndjson with buildHeader('plugin')"
      contains: "writeNdjsonStreaming"
    - path: "scripts/filter-mcps.js"
      provides: "Reads MCP records out of plugins-raw.ndjson (entity_type='mcp_server'), filters, scores, writes mcp-servers.ndjson"
      contains: "writeNdjsonStreaming"
  key_links:
    - from: "scripts/filter-plugins.js"
      to: "scripts/lib/scorers/index.js → plugin.scorer.js"
      via: "scoreEntity(record) dispatch"
      pattern: "scoreEntity"
    - from: "scripts/filter-plugins.js"
      to: "scripts/lib/filter-rules/index.js → plugin.rules.js"
      via: "isSlop(record) dispatch on entity_type='plugin'"
      pattern: "isSlop"
    - from: "scripts/filter-plugins.js"
      to: "data/plugins.ndjson"
      via: "writeNdjsonStreaming(path, records, { header: buildHeader('plugin') })"
      pattern: "writeNdjsonStreaming.*buildHeader\\('plugin'\\)"
    - from: "scripts/filter-mcps.js"
      to: "data/mcp-servers.ndjson"
      via: "writeNdjsonStreaming with buildHeader('mcp_server')"
      pattern: "buildHeader\\('mcp_server'\\)"
    - from: "scripts/embed-skills.js"
      to: "scripts/lib/embedding-input/index.js"
      via: "buildEmbeddingInput(record) dispatch (replaces inline skill-only logic)"
      pattern: "buildEmbeddingInput"
    - from: "scripts/upload-vectors.js"
      to: "Cloudflare Vectorize"
      via: "metadata.entity_type set per record (plugin / mcp_server / skill)"
      pattern: "metadata.*entity_type"
    - from: "scripts/generate-feeds.js"
      to: "feeds/*.json"
      via: "_claudeatlas.type_chip set per entity_type"
      pattern: "type_chip"
    - from: ".github/workflows/daily-scrape.yml"
      to: "scripts/filter-plugins.js + filter-mcps.js"
      via: "cron steps after Track 2"
      pattern: "filter-plugins"
    - from: "scripts/filter-plugins.js + scripts/filter.js"
      to: "scripts/lib/bundled-links.js"
      via: "linkBundles(plugins, skills) populates extra.bundled_* + skill.bundled_in_plugins"
      pattern: "linkBundles"
---

<objective>
Phase 3.2 expands the ClaudeAtlas catalog pipeline to score, filter, and publish two new entity types — `plugin` and `mcp_server` — using the polymorphic envelope infrastructure shipped in Phase 3.1.2 and the v2 writer pattern shipped in Phase 3.1.4. After this phase ships:

1. `data/plugins.ndjson` and `data/mcp-servers.ndjson` exist as v2 EntityRecord files (schema_version=2 sentinel header + per-record `entity_type` + `extra.*` payload).
2. The daily-scrape cron produces fresh plugin + MCP data alongside skill data, all wired through the existing `scorers`, `filter-rules`, and `recipes` registries — true "add files, don't refactor" as promised by 3.1.2.
3. `/api/v1/search?type=plugin` and `?type=mcp_server` start returning real results because upload-vectors now writes `metadata.entity_type` for plugin and MCP records.
4. Feeds (whats-new / trending / notable) carry mixed-type entries with `_claudeatlas.type_chip` so Phase 3.3+ UI can render type badges.
5. The bidirectional skill↔plugin bundle graph is populated as data — `skill.bundled_in_plugins[]` and `plugin.extra.bundled_skills[]` etc. Display rendering is deferred to Phase 3.3.

**Purpose:** Phase 3.1.2 reshaped the data model specifically so this work could land as additive files. Phase 3.1.3 wired feeds with type-chip support in advance. This phase cashes in on both — turning ClaudeAtlas from a skill index into a true multi-entity Claude-ecosystem index.

**Output:** ~10 new module files, ~5 modified existing files, 1 modified workflow, 2 new NDJSON data files in production. ~450 Featured plugins + ~4 Featured MCPs admitted to the catalog. Cron stays under 5h timeout.

**Explicitly out of scope (deferred per D-07):**
- `/plugins/`, `/mcp/`, and `/plugins/[slug]/` pages → Phase 3.3
- Homepage redesign with separate Top Skills / Top Plugins sections → Phase 3.5
- Using `bundled_in_plugins.length` as a skill quality_score signal → Phase 3.4
- `Featured → Top` rename → Phase 3.6
- /trends page → Phase 3.9
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3.2-plugin-and-mcp-scoring/3.2-CONTEXT.md

# Architecture precedents this phase mirrors
@.planning/phases/3.1.2-polymorphic-envelope/3.1.2-PLAN.md
@.planning/phases/3.1.2-polymorphic-envelope/3.1.2-CUTOVER.md
@.planning/phases/3.1-filter-overhaul/3.1-PLAN.md

# F1 banned-patterns invariants — strict; CI lint blocks PRs that violate
@CLAUDE.md

# Existing v2 writer pattern (the template plugin/MCP filters follow)
@scripts/filter.js
@scripts/lib/entity-version.js
@scripts/lib/legacy-skill-reader.js

# Registry stubs to extend (DO NOT refactor — only add)
@scripts/lib/scorers/index.js
@scripts/lib/scorers/skill.scorer.js
@scripts/lib/filter-rules/index.js
@scripts/lib/filter-rules/skill.rules.js
@scripts/lib/filter-rules/common.rules.js
@scripts/lib/recipes/index.js
@scripts/lib/recipes/skill.recipe.js

# Typedef stubs to expand from sketch to full
@src/lib/types.d.ts
@src/lib/types.js

# Existing scraper output that becomes this phase's input
@scripts/scrape-plugins.js

# Downstream consumers that need entity_type awareness
@scripts/embed-skills.js
@scripts/enrich.js
@scripts/upload-vectors.js
@scripts/generate-feeds.js
@scripts/generate-llms-txt.js
@worker/index.js
@.github/workflows/daily-scrape.yml

<interfaces>
<!-- Contracts the executor needs to implement against. -->
<!-- Extracted from src/lib/types.d.ts + scripts/lib/entity-version.js + scripts/lib/legacy-skill-reader.js. -->

From src/lib/types.d.ts (CURRENT stubs to expand):

```typescript
export interface PluginExtra {
  type: 'plugin';
  plugin_path: string;
  manifest: Record<string, any>;
  readme_markdown: string;
  commands: string[];
  hooks: string[];
}

export interface McpExtra {
  type: 'mcp_server';
  server_path: string;
  manifest: Record<string, any>;
  readme_markdown: string;
  tools: string[];
  transport: 'stdio' | 'sse' | 'streamable-http' | null;
}
```

EXPANDED shapes this phase ships (Task 1):

```typescript
export interface PluginExtra {
  type: 'plugin';
  plugin_path: string;
  manifest: Record<string, any>;        // plugin.json content
  readme_markdown: string;              // trimmed to 1500 chars in filter output
  commands: string[];                   // command names found in commands/
  hooks: string[];                      // hook names found in hooks/
  // Phase 3.2 additions:
  marketplace_listings: string[];        // owner/repo of marketplace.json files listing this plugin
  bundled_skills: string[];             // entity-ids (skill:<repo>/<path>) bundled in this plugin
  bundled_agents: string[];             // agent names from agents/ dir
  bundled_commands: string[];           // mirrors commands[] semantically; explicit per D-02
  bundled_hooks: string[];              // mirrors hooks[] semantically
  bundled_mcp_servers: string[];        // entity-ids of MCP servers bundled
  manifest_completeness: number;        // 0-1; computed by scorer
}

export interface McpExtra {
  type: 'mcp_server';
  server_path: string;
  manifest: Record<string, any>;
  readme_markdown: string;
  tools: string[];
  transport: 'stdio' | 'sse' | 'streamable-http' | null;
  // Phase 3.2 additions:
  manifest_completeness: number;        // 0-1
}
```

EntityCommon additions (Task 1, applied to all entity_types via shared field):

```typescript
// Already exists on every record; populated for skills in Task 8:
bundled_in_plugins: string[];           // plugin-ids that bundle this entity (skill or otherwise)
```

From scripts/lib/entity-version.js:

```javascript
export function buildHeader(entity_type = null): {
  _header: true,
  schema_version: 2,
  entity_type: string | null,
  generated_at: string,
}
```

From scripts/lib/scorers/index.js:

```javascript
export const scorers: Record<EntityType, (record) => number>;
export function scoreEntity(record): number;  // dispatches by entity_type
```

From scripts/lib/filter-rules/index.js:

```javascript
export const rulePacks: Record<EntityType, { isSlop: (record) => boolean }>;
export function isSlop(record): boolean;  // runs common gates then per-type gates
```

From scripts/lib/recipes/index.js:

```javascript
export const recipes: Record<EntityType, DiscoveryRecipe>;
```
</interfaces>
</context>

<tasks>

<!-- ====================================================================== -->
<!-- WAVE 1: ADDITIVE — typedef + module skeletons (no production effect)   -->
<!-- ====================================================================== -->

<task type="auto" tdd="true">
  <name>Task 1: Expand PluginExtra + McpExtra typedefs and add bundled_in_plugins to EntityCommon</name>
  <files>src/lib/types.d.ts, src/lib/types.js</files>
  <behavior>
    - PluginExtra MUST include the Phase 3.2 fields per interfaces block above (marketplace_listings, bundled_skills/agents/commands/hooks/mcp_servers, manifest_completeness).
    - McpExtra MUST include manifest_completeness.
    - EntityCommon MUST include `bundled_in_plugins: string[]` (default []), applicable across all entity_types.
    - types.js (JSDoc mirror) MUST stay in lockstep — types.js is the runtime-visible typedef referenced by JSDoc in scripts/.
    - `import { /* nothing changed */ }` paths from existing consumers MUST still resolve (no breaking renames).
    - Test: a synthetic v2 plugin record with the new fields populated round-trips through `JSON.parse(JSON.stringify(rec))` and retains structure.
  </behavior>
  <action>
    Per D-01, D-02, D-10. Expand PluginExtra and McpExtra in src/lib/types.d.ts following the interfaces block in &lt;context&gt;. Add `bundled_in_plugins: string[]` to EntityCommon (this is the inverse-index field — every entity_type can be bundled, though in 3.2 only skills get populated; D-02). Mirror every change into src/lib/types.js JSDoc. Add unit test in src/lib/__tests__/types.test.js (or extend existing) that constructs a synthetic plugin EntityRecord with all new fields and asserts the shape via a runtime guard (e.g., `assert(rec.extra.bundled_skills instanceof Array)`).
  </action>
  <verify>
    <automated>node --test src/lib/__tests__/types.test.js</automated>
  </verify>
  <done>types.d.ts compiles via `npx tsc --noEmit` on the JSDoc check; new fields visible on EntityRecord union; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add plugin + mcp_server rule packs (HAS_MANIFEST_OR_LISTING, COMPONENT_FLOOR, HAS_TRANSPORT_DECLARED)</name>
  <files>scripts/lib/filter-rules/plugin.rules.js, scripts/lib/filter-rules/mcp.rules.js, scripts/lib/filter-rules/index.js, scripts/lib/filter-rules/common.rules.js, scripts/lib/filter-rules/__tests__/plugin.rules.test.js, scripts/lib/filter-rules/__tests__/mcp.rules.test.js</files>
  <behavior>
    PLUGIN gates per D-05:
    - `isPluginSlop(rec)` returns true if description < 50 chars (post-trim).
    - Returns true if neither plugin.json EXISTS (`extra.manifest` populated) NOR `extra.marketplace_listings.length >= 1` — HAS_MANIFEST_OR_LISTING gate.
    - Returns true if (`extra.commands.length + extra.hooks.length + extra.bundled_skills.length + extra.bundled_agents.length + extra.bundled_mcp_servers.length`) < 1 — COMPONENT_FLOOR gate.
    - Reuses common.rules.js for template names + biz-slop + placeholder description.
    - NO MIN_STARS gate (Phase 3.1 precedent).

    MCP gates per D-05:
    - `isMcpSlop(rec)` returns true if `extra.transport === null` (HAS_TRANSPORT_DECLARED).
    - Returns true if `extra.tools.length < 1`.
    - Returns true if description < 50 chars.

    Registry registration: rulePacks['plugin'] = { isSlop: isPluginSlop }; rulePacks['mcp_server'] = { isSlop: isMcpSlop }.

    Tests:
    - 4 plugin cases: valid plugin passes; manifestless+listed plugin passes; manifestless+unlisted plugin rejected; empty-component plugin rejected.
    - 3 MCP cases: valid MCP passes; transport=null rejected; zero-tools rejected.
  </behavior>
  <action>
    Implement plugin.rules.js + mcp.rules.js mirroring skill.rules.js shape. Register both in filter-rules/index.js (`rulePacks.plugin = {...}; rulePacks.mcp_server = {...}`). Common rules already exported. If common.rules.js needs a `hasShortDescription(rec, minLen)` helper (it doesn't currently), add one to share across plugin + mcp gates. Use upstream `rec.entity_type` to no-op the gate when called against the wrong type (defensive — `index.js`'s dispatch already routes correctly, but defense in depth never hurts).

    Per D-05 explicit values: MIN_DESCRIPTION_LENGTH=50, COMPONENT_FLOOR=1. Hard-code as module constants with JSDoc explaining provenance ("// Per Phase 3.2 D-05").
  </action>
  <verify>
    <automated>node --test scripts/lib/filter-rules/__tests__/plugin.rules.test.js scripts/lib/filter-rules/__tests__/mcp.rules.test.js</automated>
  </verify>
  <done>Both rule modules registered in index.js's `rulePacks` map; tests pass; the dispatcher's `isSlop({entity_type:'plugin', ...})` and `isSlop({entity_type:'mcp_server', ...})` route to the right pack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add plugin + mcp_server scorers (7-signal composite, Manifest-Completeness swap)</name>
  <files>scripts/lib/scorers/plugin.scorer.js, scripts/lib/scorers/mcp.scorer.js, scripts/lib/scorers/index.js, scripts/lib/scorers/__tests__/plugin.scorer.test.js, scripts/lib/scorers/__tests__/mcp.scorer.test.js</files>
  <behavior>
    Per D-03. Both scorers compute a 0-100 quality_score from 7 signals:

    Plugin signals (weight % in parens):
    - Stars (20%) — log-scaled, same formula as scoreSkill
    - Recency (20%) — days since push, 180-day cliff
    - **Manifest completeness (20%)** — has description>=50 + version + author + license + >=1 keyword in manifest. Score 0-1, mapped to weight.
    - Documentation (15%) — readme_markdown length + repo description length
    - Issue health (10%) — open-issues-to-stars ratio
    - License (10%) — permissive license present
    - Description (5%) — repo description meaningful

    MCP signals identical EXCEPT Manifest completeness:
    - **MCP manifest completeness (20%)** — description>=50 + transport declared + >=1 tool

    Both scorers MUST also set `record.extra.manifest_completeness` (0-1 float) as a side-effect for downstream display. Implement as pure functions that mutate `extra.manifest_completeness` then return the composite.

    Register in scorers/index.js: `scorers.plugin = scorePlugin; scorers.mcp_server = scoreMcp;`

    Tests:
    - Plugin: perfect manifest (desc+version+author+license+keyword) → manifest_completeness=1.0. Score with high stars+recent push reaches Featured range.
    - Plugin: minimal manifest (desc only) → manifest_completeness≈0.2.
    - MCP: stdio transport + 3 tools + desc → high manifest_completeness.
    - MCP: null transport → manifest_completeness contribution=0 regardless of other fields.
    - Both: snapshot test against a known-good fixture record (commit fixture under `__tests__/fixtures/`).
  </behavior>
  <action>
    Import the helpers scoreSkill uses (`scripts/score.js`) for Stars/Recency/IssueHealth/License/Description/Documentation sub-signals. Factor any inline helpers in score.js into shared helpers if they're not already exported — common case is `logStars()` and `daysSincePush()` — add `export` keywords so plugin.scorer.js can `import { logStars, daysSincePush, scoreLicense, scoreIssueHealth, scoreDescription, scoreDocumentation } from '../../score.js'`. If score.js's existing functions aren't structured for clean reuse, extract them into `scripts/lib/scorers/common-signals.js` and re-export from `score.js` for back-compat (do NOT change scoreSkill's behavior or output — assert identity against an existing skill fixture).

    Per D-03's "1-signal swap" promise, the Manifest-Completeness logic is the ONLY new code. Everything else reuses skill scoring primitives.
  </action>
  <verify>
    <automated>node --test scripts/lib/scorers/__tests__/plugin.scorer.test.js scripts/lib/scorers/__tests__/mcp.scorer.test.js && node --test scripts/lib/__tests__/score.test.js</automated>
  </verify>
  <done>Both scorers registered; tests pass; existing skill scorer output unchanged (regression test in score.test.js confirms via snapshot).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extract embedding-input registry; add skill + plugin + mcp builders</name>
  <files>scripts/lib/embedding-input/index.js, scripts/lib/embedding-input/skill.js, scripts/lib/embedding-input/plugin.js, scripts/lib/embedding-input/mcp.js, scripts/lib/embedding-input/__tests__/index.test.js</files>
  <behavior>
    Per D-09. Establish the embedding-input registry (CONTEXT notes this directory doesn't yet exist; this is the formalization moment).

    - `buildSkillEmbeddingInput(rec)` — extracts current inline logic from embed-skills.js (name + description + frontmatter keywords if present). Output identical to today's input string for a given fixture (regression test).
    - `buildPluginEmbeddingInput(rec)` — `${name} | ${description} | ${(manifest.keywords||[]).join(', ')}`. Trim to ~200 chars.
    - `buildMcpEmbeddingInput(rec)` — `${name} | ${description} | transport=${transport} | tools: ${tools.join(', ')}`. Trim to ~200 chars.
    - `buildEmbeddingInput(rec)` dispatches by entity_type; throws on unknown type (fail loud — silent fallthrough produces wrong vectors).

    Tests:
    - Skill: against committed fixture, output string MUST match snapshot of today's inline logic.
    - Plugin: keywords joined with comma+space; total length <= 200.
    - MCP: transport=null → string still well-formed (`transport=null | tools: ...`).
    - Dispatch: unknown entity_type throws.
  </behavior>
  <action>
    Extract the current inline embedding-input construction from embed-skills.js into skill.js — DO NOT modify embed-skills.js's behavior in this task; the consumer wiring happens in Task 9. The skill builder MUST produce byte-identical output for a fixture record (locked via snapshot test) to guarantee zero churn on the existing 35k Vectorize index.

    Plugin keywords come from `rec.extra.manifest?.keywords` (array per plugin.json convention) — if absent, omit the keywords portion gracefully. MCP tools from `rec.extra.tools`.
  </action>
  <verify>
    <automated>node --test scripts/lib/embedding-input/__tests__/index.test.js</automated>
  </verify>
  <done>Registry exports buildEmbeddingInput; per-type builders pass tests; skill builder produces snapshot-identical output to existing inline code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add plugin + mcp recipes (stub shape, parallel to skillRecipe)</name>
  <files>scripts/lib/recipes/plugin.recipe.js, scripts/lib/recipes/mcp.recipe.js, scripts/lib/recipes/index.js, scripts/lib/recipes/__tests__/index.test.js</files>
  <behavior>
    Per D-10. Both recipes carry the DiscoveryRecipe shape from skill.recipe.js:
    - entity_type
    - output_path (`data/plugins-raw.ndjson` for plugin; `data/plugins-raw.ndjson` ALSO for MCP since scrape-plugins.js writes mixed records — discriminated by entity_type at read time)
    - state_path
    - `async *discover()` — empty body for F2-style (real discovery lives in scrape-plugins.js, same pattern as skillRecipe)
    - `async parse(hit)` — calls into scrape-plugins.js's existing parse helpers OR is a no-op pass-through
    - `computeId(rec)` — `plugin:${rec.repo_full_name}/${rec.extra.plugin_path}` or `mcp_server:${rec.repo_full_name}/${rec.extra.server_path}`

    Registry: `recipes.plugin = pluginRecipe; recipes.mcp_server = mcpRecipe;`

    Tests:
    - Both recipes import cleanly via the registry.
    - `computeId` produces expected string for a fixture record.
    - Recipe shape conforms (has entity_type, output_path, state_path, discover, parse, computeId).
  </behavior>
  <action>
    Same stub pattern as skill.recipe.js — empty discover() generator, computeId returns the canonical id format. The recipe-engine-driven path is still future work; the production path remains scrape-plugins.js. Registering the recipes unblocks `recipes['plugin']` lookups elsewhere (e.g., for future cross-cutting tooling).
  </action>
  <verify>
    <automated>node --test scripts/lib/recipes/__tests__/index.test.js</automated>
  </verify>
  <done>Both recipes registered; tests pass; recipes/index.js exports `recipes.plugin` and `recipes.mcp_server`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend legacy upcaster with plugin + MCP v1→v2 paths</name>
  <files>scripts/lib/legacy-skill-reader.js, scripts/lib/__tests__/legacy-skill-reader.test.js</files>
  <behavior>
    The current `upcastRecord` is skill-only — it always returns entity_type='skill'. For Phase 3.2 it must dispatch on the incoming record's `entity_type` (if present) and route to a plugin or MCP upcaster. v1 plugin/MCP records (no schema_version) coming out of scrape-plugins.js's pre-3.2 output get nested into `extra` correctly.

    - `upcastRecord(rec)` dispatches: if `rec.schema_version === 2` → pass through; if `rec.entity_type === 'plugin'` → upcastPluginRecord; if `rec.entity_type === 'mcp_server'` → upcastMcpRecord; else default to current skill path.
    - upcastPluginRecord(): id = `plugin:${repo}/${plugin_path}`; nests plugin_path/manifest/readme_markdown/commands/hooks under `extra`; sets default `extra.bundled_*=[]` and `extra.marketplace_listings=[]`; preserves legacy flat fields for D+0–D+7 dual-shape if v1 records carry them (probably won't — but defensive).
    - upcastMcpRecord(): id = `mcp_server:${repo}/${server_path}`; nests server_path/manifest/readme_markdown/tools/transport.
    - Both set `bundled_in_plugins=[]` default.

    **D+7 coordination note (in code comment):** the D+7 cutover commit per 3.1.2-CUTOVER.md will DELETE this file. The plugin/MCP upcast paths must be transient — they exist only to bridge any v1-shape plugin records still on disk after 3.2 ships. After D+7 (~2026-06-04), the scrape-plugins.js + filter-plugins.js pipeline writes native v2 always, so the upcaster paths become unreachable and the cleanup PR removes the whole file.

    Tests:
    - v1 plugin record (no schema_version) → v2 EntityRecord<PluginExtra> with extra.plugin_path etc.
    - v1 mcp record → v2 EntityRecord<McpExtra> with extra.transport.
    - v2 plugin record → unchanged.
    - Existing skill tests still pass (regression).
  </behavior>
  <action>
    Refactor upcastRecord to a dispatcher that calls type-specific helpers (extract the existing skill body into `upcastSkillRecord`). Add the two new helpers. Keep the dual-shape back-compat block for skills (still needed for the D+0→D+7 window); plugins/MCPs don't need dual-shape because no current consumer reads `plugin.commands` as a flat top-level field.

    Add comment block at top of file noting Phase 3.2 expansion and the D+7 cleanup coupling.
  </action>
  <verify>
    <automated>node --test scripts/lib/__tests__/legacy-skill-reader.test.js</automated>
  </verify>
  <done>Tests pass; the existing skill upcast behavior is byte-identical (regression snapshot); plugin and MCP records upcast correctly.</done>
</task>

<!-- ====================================================================== -->
<!-- WAVE 2: FILTER INTEGRATION — wire registries into a working pipeline   -->
<!-- ====================================================================== -->

<task type="auto" tdd="true">
  <name>Task 7: filter-plugins.js + filter-mcps.js (parallel to filter.js, using F2 registries)</name>
  <files>scripts/filter-plugins.js, scripts/filter-mcps.js, scripts/__tests__/filter-plugins.test.js, scripts/__tests__/filter-mcps.test.js</files>
  <behavior>
    Per D-04, D-05. Both filters follow scripts/filter.js's structure exactly, just dispatched per entity_type.

    filter-plugins.js:
    - Reads `data/plugins-raw.ndjson` via `readNdjsonRecords` (F1 streaming).
    - Splits the file by `rec.entity_type` — handles only `entity_type === 'plugin'` (MCPs go to filter-mcps.js; skills inside plugin repos get dropped — those are surfaced via scrape.js / filter.js).
    - Pre-step: upcast each v1 record via `upcastRecord` from legacy-skill-reader.js.
    - Apply common+plugin slop gates via `isSlop` dispatch from filter-rules/index.js.
    - Score via `scoreEntity` dispatch from scorers/index.js (which calls scorePlugin).
    - Apply percentile tiers (10/30/60 top-down, same logic block as filter.js Step 3) — copy the tier-assignment code from filter.js OR (preferred) extract it into `scripts/lib/tier-assignment.js` shared helper.
    - Preserve enrichments across runs via PRESERVED_FIELDS (is_duplicate, canonical_id, novelty_score, bundled_in_plugins). Use a `loadPriorEnrichments(OUTPUT_PATH)` helper shared with filter.js.
    - Apply slug via `assignSlugs` — works across entity_types because the slug helper is already type-agnostic (D-08).
    - Trim `extra.readme_markdown` to 1500 chars; preserve body_length invariant per the F2 JSDoc (mirror filter.js's invariant assert).
    - Write to `data/plugins.ndjson` via `writeNdjsonStreaming(OUTPUT_PATH, records, { header: buildHeader('plugin') })`.
    - Append plugin stats to pipeline-stats.json (don't overwrite skill stats — read existing, merge `plugin: {...}` section, write).

    filter-mcps.js: same shape, but:
    - Reads same `data/plugins-raw.ndjson`, dispatches on `entity_type === 'mcp_server'`.
    - At N=~38, skips percentile tier assignment guard if N<10 (would produce 0 Featured + 0 Solid). Instead: assign top 10% Featured (rounded up so N=38 → 4 Featured), next 30% Solid (~11), rest Listed. Per D-04.
    - Writes to `data/mcp-servers.ndjson` with `buildHeader('mcp_server')`.

    Tests:
    - Synthetic input: 5 plugins (3 valid, 2 slop) + 3 MCPs (2 valid, 1 missing transport).
    - filterPlugins output: 3 records, all entity_type='plugin', all carry tags, percentile tiers assigned (since N=3 → 1 Featured / 1 Solid / 1 Listed).
    - filterMcps output: 2 records, slop filtered.
    - Both writers produce a `_header: true` line first in the output file (assertion on raw file content).
    - PRESERVED_FIELDS round-trip: a prior plugin record's `is_duplicate=true` survives a re-filter when raw input lacks the field.

    F1 invariants:
    - NO `readFileSync` on `data/plugins-raw.ndjson`; use `readNdjsonRecords`.
    - NO `JSON.stringify(arr, null, 2)` on the records list; use `writeNdjsonStreaming`.
    - Pipeline-stats.json (bounded sidecar) `JSON.stringify(stats, null, 2)` is fine — allowlist it in check-banned-patterns.js if needed.
  </behavior>
  <action>
    Use scripts/filter.js as the template. Recommended structure:

    1. Extract tier-assignment + preserved-enrichments + slug-assign + body trim into `scripts/lib/filter-pipeline.js` shared helpers so filter.js, filter-plugins.js, filter-mcps.js call into the same logic with entity-type-specific config (PRESERVED_FIELDS list, body-trim field name `extra.readme_markdown` vs `extra.body_markdown`, etc.). This keeps drift impossible.
    2. Each script's `main()` becomes ~60 lines: read → upcast → slop → score → tier → trim → preserve → write.
    3. Both write to plugins.ndjson / mcp-servers.ndjson — NOT a unified skills.ndjson. CONTEXT notes the "skills-latest becomes unified" framing but it's cleaner to ship separate files in 3.2 and let publish/consumption logic decide whether to merge. **Decision per planner discretion (D-allowed area):** ship separate files, defer unification to Phase 3.3 or 3.5 when /plugins/ pages need it.

    Per D-allowed planner discretion: filter logic lives in separate `filter-plugins.js` + `filter-mcps.js` scripts rather than dispatched inside `filter.js`. Reasoning: keeps filter.js's skill-specific logic (Track 1 freshness merge, applyTrack1Freshness, etc.) from getting polluted, and the parallel scripts can run in parallel in the cron.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/filter-plugins.test.js scripts/__tests__/filter-mcps.test.js && node scripts/check-banned-patterns.js --mode=lint</automated>
  </verify>
  <done>Both filters produce v2 NDJSON with correct header sentinel; tests pass; banned-patterns lint clean; pipeline-stats.json carries plugin + mcp sections without clobbering skill stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Bidirectional bundle-graph populator (linkBundles)</name>
  <files>scripts/lib/bundled-links.js, scripts/lib/__tests__/bundled-links.test.js, scripts/filter.js (call site), scripts/filter-plugins.js (call site)</files>
  <behavior>
    Per D-02. Compute the cross-entity bundle graph after both skill and plugin filters have produced their output but before the records are written to disk (since both files are written separately).

    `linkBundles(plugins, skills, { mcpServers })`:
    - For each plugin, populate `plugin.extra.bundled_skills = [skill.id for skill in skills where skill.repo_full_name === plugin.repo_full_name AND skill.extra.skill_path starts with plugin.extra.plugin_path's directory]`. This captures the "plugin bundles its colocated skills" relationship.
    - Populate `plugin.extra.bundled_agents` from `plugin.extra.commands` namespace heuristic OR from directory walk metadata if present in `plugin.extra.manifest.agents`.
    - Populate `plugin.extra.bundled_commands` from plugin.extra.commands (mechanical).
    - Populate `plugin.extra.bundled_hooks` from plugin.extra.hooks (mechanical).
    - Populate `plugin.extra.bundled_mcp_servers = [mcp.id for mcp in mcpServers where mcp.repo_full_name === plugin.repo_full_name]`.
    - For each skill in `bundled_skills`, INVERSE-populate `skill.bundled_in_plugins.push(plugin.id)`. Order is stable (sort by plugin.id ascending) so the field is deterministic across runs.

    Idempotency: re-running linkBundles MUST produce identical output. Tests assert byte-identical output across two passes.

    Tests:
    - Plugin in `owner/repo/.claude-plugin/plugin.json` bundles skills at `owner/repo/skills/foo/SKILL.md`. After linkBundles: plugin.extra.bundled_skills = ['skill:owner/repo/skills/foo/SKILL.md']; skill.bundled_in_plugins = ['plugin:owner/repo/.claude-plugin/plugin.json'].
    - Plugin in repo A and skill in repo B: NO linkage.
    - Plugin manifest with `agents: ['agent-x']`: bundled_agents includes 'agent-x'.
    - Re-running on same input is idempotent.

    NOTE: This is a data-only feature in Phase 3.2 (display rendering deferred to 3.3 per D-02). The test suite only validates the populated fields; no rendering tests.
  </behavior>
  <action>
    The trickiest part is the cross-call orchestration. filter.js and filter-plugins.js run as separate scripts in the cron, but linkBundles needs BOTH the plugin list AND the skill list in memory at once. Options:

    1. **Run linkBundles as a separate cron step AFTER both filters complete.** Reads plugins.ndjson + skills.ndjson, performs the linkage, REWRITES both files. Pro: clean separation. Con: extra I/O.
    2. **Run linkBundles inside filter-plugins.js** by lazy-loading skills.ndjson when it exists. Pro: no extra step. Con: ordering dependency (filter.js must run first).
    3. **Skip the inverse skill linkage in this phase**, populate only the forward (plugin → skill) side, and defer the inverse to Phase 3.4 when the signal-use lands. Pro: simplest. Con: D-02 explicitly requires both sides.

    **Decision (planner discretion):** Option 1 — create a new script `scripts/link-bundles.js` that runs after both filters. Cron-step ordering becomes: filter.js → filter-plugins.js → filter-mcps.js → link-bundles.js → embed-skills.js → ... The script reads both NDJSON files via `readNdjsonRecords`, calls `linkBundles()`, writes both files back via `writeNdjsonStreaming` with the correct buildHeader. This way each filter stays focused and the cross-entity work is isolated.

    Add link-bundles.js to npm scripts as `npm run link-bundles`. Wire into daily-scrape.yml in Task 12.
  </action>
  <verify>
    <automated>node --test scripts/lib/__tests__/bundled-links.test.js</automated>
  </verify>
  <done>linkBundles is pure + idempotent + tested; the new link-bundles.js script reads both files and writes them back with the cross-references populated; banned-patterns lint clean.</done>
</task>

<!-- ====================================================================== -->
<!-- WAVE 3: PIPELINE INTEGRATION — embed, enrich, upload                   -->
<!-- ====================================================================== -->

<task type="auto" tdd="false">
  <name>Task 9: Extend embed-skills.js to handle plugin + MCP records via embedding-input dispatch</name>
  <files>scripts/embed-skills.js, scripts/__tests__/embed-skills.test.js</files>
  <behavior>
    Per D-09. embed-skills.js becomes the unified embedder for all three entity_types.

    - CLI: accepts `--input <path> --output <path> --entity-type <skill|plugin|mcp_server>`. Default input=`data/skills.ndjson` output=`data/skill-vectors.ndjson` entity-type=skill (back-compat with current invocation).
    - Replaces inline embedding-input construction with `buildEmbeddingInput(record)` from Task 4's registry.
    - For plugin/MCP runs: input=`data/plugins.ndjson` or `data/mcp-servers.ndjson`, output=`data/plugin-vectors.ndjson` or `data/mcp-vectors.ndjson`.
    - Honors `EMBED_DRY_RUN=1` for all three types (deterministic SHA-256-seeded fake vectors per the established pattern). MUST work identically across types.
    - Adds `metadata.entity_type = record.entity_type` to each vector record (preparation for Task 10's upload-vectors).
    - content_sha delta-detection unchanged: per-record sha based on `buildEmbeddingInput(record)` output, skips records whose sha matches the prior vector file.

    Smoke test: dry-run a 3-plugin + 2-mcp fixture; assert output NDJSON has correct vectors (deterministic) and metadata.entity_type values.
  </behavior>
  <action>
    Refactor embed-skills.js to be entity-type-agnostic at its core. The CLI flag default chain preserves current daily-scrape.yml invocation (`node scripts/embed-skills.js` → skills). New invocations: `node scripts/embed-skills.js --entity-type plugin` and `--entity-type mcp_server`.

    Renaming embed-skills.js to embed.js is tempting but adds a daily-scrape.yml diff for no functional gain — skip the rename.

    Per D-09 cost projection: ~$0.01 one-time for ~4,540 new embeds. Negligible. Daily delta after the initial run will be tiny (only new/changed records).
  </action>
  <verify>
    <automated>EMBED_DRY_RUN=1 node scripts/embed-skills.js --input data/__fixtures__/plugins-mini.ndjson --output /tmp/plugin-vectors.ndjson --entity-type plugin && node --test scripts/__tests__/embed-skills.test.js</automated>
  </verify>
  <done>embed-skills.js handles all three types; dry-run produces deterministic vectors; metadata.entity_type set; test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Extend upload-vectors.js + enrich.js for entity_type dispatch</name>
  <files>scripts/upload-vectors.js, scripts/enrich.js, scripts/__tests__/upload-vectors.test.js</files>
  <behavior>
    upload-vectors.js:
    - Accepts `--input <path>` (default: skill-vectors.ndjson, for back-compat).
    - For each vector record, sets Vectorize metadata: `{ entity_type, slug, quality_tier, ...existing }`. The `?type=` filter on the worker triad already reads `metadata.entity_type` per Phase 3.1.2 T8 — these uploads enable it for plugins + MCPs.
    - In CI/branch contexts, the script remains dry-run friendly (no Vectorize API calls without `--commit`).

    enrich.js (cosine dedup + novelty):
    - Today is skills-only. Per D-10 + CONTEXT operational reality, MCP enrich is SKIPPED at N=38 (dedup overhead exceeds benefit).
    - Plugin enrich runs the same O(n²) cosine logic at N≈4,500. Confirm this completes in ≤2 minutes locally on a 4,500-record sample. (Skills already run at N≈35k in ~13 min — plugin run is comfortably faster.)
    - Add CLI: `--input data/plugin-vectors.ndjson --records data/plugins.ndjson` for plugin enrich; existing default invocation stays skills-only.
    - Writes `is_duplicate`, `canonical_id`, `novelty_score` back to the records file via the same streaming-rewrite pattern enrich.js uses today.

    Tests:
    - upload-vectors: dry-run a plugin vectors fixture, assert the would-be Vectorize payload carries `metadata.entity_type='plugin'`.
    - enrich (skills): regression test against current behavior unchanged.
    - enrich (plugin): on a 10-plugin fixture with 2 near-duplicates (cosine ≥ 0.92), the duplicates get flagged.
  </behavior>
  <action>
    Both scripts already have the right shape (per CONTEXT). Just need:
    1. upload-vectors.js: parameterize input path; ensure metadata.entity_type is included. The metadata field MAY already be passed through from embed-skills.js's output — verify it's not dropped en route.
    2. enrich.js: add CLI flags + dispatch; MCP path is a guarded early-return with a log line "[enrich] skipping mcp_server (N=${n} below dedup threshold)".

    Per CONTEXT, MCP dedup is planner-discretion to skip — I'm choosing to skip per the operational reality of N=38. Document in code comment.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/upload-vectors.test.js && node scripts/enrich.js --input data/__fixtures__/plugin-vectors-mini.ndjson --records data/__fixtures__/plugins-mini.ndjson --dry-run</automated>
  </verify>
  <done>upload-vectors carries entity_type metadata; enrich handles plugins; MCP enrich skipped per planner-discretion documented in code; tests pass.</done>
</task>

<!-- ====================================================================== -->
<!-- WAVE 4: DOWNSTREAM CONSUMERS                                            -->
<!-- ====================================================================== -->

<task type="auto" tdd="false">
  <name>Task 11: generate-feeds.js + generate-llms-txt.js — mixed-type emission</name>
  <files>scripts/generate-feeds.js, scripts/generate-llms-txt.js, scripts/__tests__/generate-feeds.test.js</files>
  <behavior>
    Per D-06.

    generate-feeds.js:
    - Currently loads skills.ndjson via the entities loader. Extend to ALSO load plugins.ndjson and mcp-servers.ndjson. Concatenate into a single `entities` array.
    - Each entry gets `_claudeatlas.type_chip = entity.entity_type` (literal: 'skill' | 'plugin' | 'mcp_server'). Phase 3.1.3 added the field; just populate it now.
    - All 3 feeds (whats-new, trending, notable) emit mixed-type entries. Sort/rank logic stays unchanged — it's score+date-based and entity-type-agnostic.
    - Test: feed contains at least 1 entry per type when input has all three types.

    generate-llms-txt.js:
    - Currently counts skills from skills.ndjson. Extend to count plugins.ndjson + mcp-servers.ndjson. Counter line in llms.txt becomes (example): `Catalog: 35,341 skills · 4,500 plugins · 38 MCP servers (live).`
    - Pull counts from pipeline-stats.json (now contains all three sections after Task 7).

    NOTE: D-07 explicitly excludes /skills-registry.json from plugin/MCP inclusion in this phase. Don't touch generate-registry.js.
  </behavior>
  <action>
    feeds: add a helper `loadAllEntities()` that reads all 3 NDJSON files (graceful no-op if a file is missing — early in 3.2 ship, plugins.ndjson may not exist on disk in some CI branches). Honor banned-patterns: use readNdjsonRecords throughout.

    llms-txt: read pipeline-stats.json (bounded sidecar — fine to JSON.parse) and template the counts. If a section is missing, omit it gracefully.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/generate-feeds.test.js && node scripts/generate-llms-txt.js --output /tmp/llms.txt && grep -E "plugin|mcp" /tmp/llms.txt</automated>
  </verify>
  <done>Feeds carry type_chip; llms.txt counts include plugin + mcp; tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 12: Wire daily-scrape.yml + check-banned-patterns allowlist updates</name>
  <files>.github/workflows/daily-scrape.yml, scripts/check-banned-patterns.js, CLAUDE.md</files>
  <behavior>
    Per D-11. Insert plugin + MCP pipeline steps into the daily cron between the existing Track 2 and Filter steps. Cron flow becomes:

    1. Track 1 (Star Pulse)
    2. Track 2 (Discovery)
    3. **NEW: Scrape plugins** (`node scripts/scrape-plugins.js` — already exists; ~30 min)
    4. Filter skills (existing)
    5. **NEW: Filter plugins** (`node scripts/filter-plugins.js`)
    6. **NEW: Filter MCPs** (`node scripts/filter-mcps.js`)
    7. **NEW: Link bundles** (`node scripts/link-bundles.js`)
    8. Embed skills (existing)
    9. **NEW: Embed plugins** (`node scripts/embed-skills.js --entity-type plugin`)
    10. **NEW: Embed MCPs** (`node scripts/embed-skills.js --entity-type mcp_server`)
    11. Enrich skills (existing)
    12. **NEW: Enrich plugins** (`node scripts/enrich.js --input data/plugin-vectors.ndjson --records data/plugins.ndjson`)
    13. Upload vectors (extend to handle 3 inputs; loop)
    14. Build site
    15. Deploy
    16. Commit data back

    Branch-safe CI: publish/deploy steps remain gated to `main` ref check (existing pattern). NEW steps that publish (e.g., upload-vectors when committing) must respect the same gate.

    `check-banned-patterns.js`:
    - Add allowlist entries for any new sidecar JSON writes introduced (e.g., `data/plugin-stats.json` if filter-plugins.js writes one; data/bundle-graph.json if link-bundles produces one).
    - DO NOT relax the F1 banned patterns for any new growing data file.

    CLAUDE.md:
    - Add a new section under "Project structure" describing data/plugins.ndjson, data/mcp-servers.ndjson, data/plugin-vectors.ndjson, data/mcp-vectors.ndjson as new committed/cached files.
    - Add to "Known issues" section: "Plugin filter shares enrichment-preservation logic with skill filter — if either enrich step fails, the affected records carry stale is_duplicate flags."
    - Update the daily-scrape flow narrative to reflect new steps.
  </behavior>
  <action>
    Use the existing daily-scrape.yml as the template. Each new step gets:
    - `name:`
    - `if:` gate matching the existing pattern (cron + push events)
    - `run:` invoking the script
    - timeout per step
    - cache key for any state file (plugin checkpoints reuse the GHA cache pattern from scrape.js)

    Banned-patterns allowlist: scripts/check-banned-patterns.js has an `ALLOWLIST` array; append entries with file path + line range + reason.

    CLAUDE.md: append, don't rewrite. Append a "Phase 3.2 additions" subsection that future agents will read for context.
  </action>
  <verify>
    <automated>node scripts/check-banned-patterns.js --mode=lint && npx --yes js-yaml .github/workflows/daily-scrape.yml > /dev/null && node -e "import('./scripts/lib/scorers/index.js').then(m =&gt; { if (!m.scorers.plugin || !m.scorers.mcp_server) throw new Error('registries missing types'); })"</automated>
  </verify>
  <done>Workflow YAML valid; all new scripts referenced exist; banned-patterns lint clean; CLAUDE.md updated; ready for branch-CI dispatch.</done>
</task>

<!-- ====================================================================== -->
<!-- WAVE 5: CRON MITIGATION + END-TO-END SMOKE + VERIFICATION              -->
<!-- ====================================================================== -->

<task type="auto" tdd="false">
  <name>Task 13: 5h cron timeout audit + mitigation (3.2-DOD-12)</name>
  <files>.github/workflows/daily-scrape.yml, scripts/enrich.js OR .github/workflows/weekly-discover.yml OR new files per mitigation chosen, .planning/phases/3.2-plugin-and-mcp-scoring/3.2-CRON-AUDIT.md</files>
  <behavior>
    Per 3.2-DOD-12. Today's scheduled cron at 09:00 UTC hit the 5h timeout at 5h0m18s. Adding plugin discovery (~30 min) + plugin filter/embed/enrich (~10-20 min) without mitigation pushes runtime well past 5h.

    Step 1 — Audit (write to 3.2-CRON-AUDIT.md):
    - Pull last 5 successful run timings via `gh run list --workflow=daily-scrape.yml --limit 20 --json`.
    - Break down per step using `gh run view <id> --log` (or just from the workflow's `time` outputs).
    - Identify the top 3 longest steps. Compute the post-3.2 projected runtime.

    Step 2 — Pick a mitigation:
    - **Option A: HNSW migration for enrich.js compute-similar.** Long-term answer per the backlog. Frees ~70 min of cron time. Implementation cost: high (~half a phase). Probably overkill for 3.2.
    - **Option B: Split plugin scrape to its own workflow** (weekly-plugin-discover.yml fires Sundays). Plugin records change less frequently than skills (~1-5% daily delta). Daily-scrape just embeds + enriches existing plugins.ndjson. Implementation cost: medium.
    - **Option C: Reduce K in enrich's compute-similar from 5 to 3.** Smaller per-record neighbor list, ~30% less compute. Implementation cost: trivial (config change).
    - **Option D: Bump the GHA workflow timeout from 5h to 6h.** Per Phase 3.1.1 T8's timeout bump precedent. GitHub-hosted runners have a 6h cap (max). Buys an hour. Implementation cost: trivial.

    **Decision (planner discretion):** Combine D + C for this phase:
    - Bump timeout to 5.5h (within GHA 6h cap, leaving margin).
    - Reduce enrich.js K from 5 to 3 (compute-similar caller — verify K is used for the similar-skills output, not for the dedup threshold; dedup uses cosine threshold not K).
    - This is the MINIMUM mitigation that unblocks 3.2 ship. Option B (split workflow) becomes the answer for Phase 3.3+ if the projected runtime still exceeds 5.5h.

    Write the audit + decision rationale to 3.2-CRON-AUDIT.md.

    Step 3 — Implement chosen mitigation (D+C):
    - Edit daily-scrape.yml: `timeout-minutes: 330` (5.5h).
    - Edit enrich.js: K constant from 5 to 3 (assuming the constant exists; otherwise add CLI flag `--k 3` and pass from cron).
    - Document in CLAUDE.md "Known issues" section that K was reduced and may need to bump back up if similar-skills feature degrades visibly.
  </behavior>
  <action>
    The audit step is information-gathering — run `gh run list/view` commands and write findings to 3.2-CRON-AUDIT.md. The audit doc becomes a permanent artifact reviewable in PR.

    Mitigation D + C is conservative — they're the lowest-risk changes that buy the budget. If post-implementation projection still exceeds 5.5h, escalate to Option B (split workflow) WITHIN this task — don't ship 3.2 with a cron that will timeout immediately.

    Open question for future phases: HNSW migration (Option A) is the long-term right answer. Add a Phase 3.x backlog item in STATE.md "HNSW migration for enrich.js" if not already there (CONTEXT confirms it's in the "Deferred to backlog" section).
  </action>
  <verify>
    <automated>node -e "import('fs').then(({readFileSync}) =&gt; { const yml = readFileSync('.github/workflows/daily-scrape.yml','utf-8'); if (!/timeout-minutes:\s*330/.test(yml)) throw new Error('timeout not bumped'); }); " && test -f .planning/phases/3.2-plugin-and-mcp-scoring/3.2-CRON-AUDIT.md</automated>
  </verify>
  <done>Audit doc committed at 3.2-CRON-AUDIT.md; timeout bumped to 330 min; enrich K reduced; CLAUDE.md updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 14: Production-scale branch-CI smoke + v2 round-trip verification</name>
  <files>scripts/verify-3.2-roundtrip.js, .planning/phases/3.2-plugin-and-mcp-scoring/3.2-SMOKE-RESULTS.md</files>
  <behavior>
    Final go/no-go check before merging to main. Pattern follows Phase 3.1.3's branch-CI smoke (per CONTEXT note about Phase 3.1.3's executor pattern).

    Step 1 — Local v2 round-trip script (`scripts/verify-3.2-roundtrip.js`):
    - Read data/plugins.ndjson via readNdjsonRecords; assert _header line carries `schema_version=2` and `entity_type='plugin'`.
    - Assert every record has `entity_type='plugin'`, `schema_version=2`, populated `extra.plugin_path`, `extra.manifest`, `extra.bundled_skills` (array, possibly empty), `bundled_in_plugins` field present on EntityCommon.
    - Same checks for data/mcp-servers.ndjson with `entity_type='mcp_server'`.
    - Assert skills.ndjson now carries `bundled_in_plugins` field on every record (may be []).
    - Assert pipeline-stats.json contains both `plugin` and `mcp_server` sections.
    - Exit 0 on success; exit 1 on any failure with descriptive error.

    Step 2 — Branch-CI dispatch:
    - Push the feature branch.
    - `gh workflow run daily-scrape.yml --ref gsd/phase-3.2-plugin-and-mcp-scoring`
    - Watch the run to completion (`gh run watch`).
    - Confirm: completes under 5.5h; all new steps green; no banned-patterns lint failures; deploy/publish steps were SKIPPED (branch not main).

    Step 3 — Write smoke results (`3.2-SMOKE-RESULTS.md`):
    - Capture run ID + final timing breakdown per step.
    - Capture record counts in each output file.
    - Capture tier distribution per entity_type.
    - Capture any anomalies (e.g., unexpected slop rejection rate; tier composition surprises at small-N MCP).
    - Sign-off line: "Phase 3.2 verified at branch-CI production scale on YYYY-MM-DD; ready to merge to main."

    No new tests file — this task is the integration test.
  </behavior>
  <action>
    Write the round-trip verifier first. Run it locally against fixtures to confirm correctness. Then dispatch the branch CI. The smoke doc is the artifact reviewers read in the merge PR.

    If branch CI fails: file an issue, do NOT merge. Use the dispatch logs to root-cause; common failures will be (a) timeout still exceeds 5.5h → escalate to Option B in Task 13, (b) banned-patterns lint catches a missed allowlist entry → add it and re-dispatch, (c) a missing file because filter-plugins.js had no input (plugins-raw.ndjson not in GHA cache) → trigger bootstrap-skills-raw.yml equivalent for plugins, or seed via a one-time release asset.
  </action>
  <verify>
    <automated>node scripts/verify-3.2-roundtrip.js</automated>
  </verify>
  <done>Round-trip verifier passes against real pipeline output; branch-CI dispatch completes green under 5.5h; smoke results doc committed; phase ready to merge.</done>
</task>

</tasks>

<verification>

## Phase-Level Verification (post-merge)

1. **Pipeline produces fresh data daily.** Confirm via `gh run list --workflow=daily-scrape.yml --limit 3` shows all three runs complete in under 5.5h with green status. Confirm `data/plugins.ndjson` and `data/mcp-servers.ndjson` carry today's date in their `_header.generated_at`.

2. **Feeds carry mixed types.** `curl -s https://claudeatlas.com/feed/whats-new.json | jq '[.[] | ._claudeatlas.type_chip] | unique'` returns `["mcp_server", "plugin", "skill"]` (in some order).

3. **Search ?type filter returns real plugin results.** `curl -s 'https://claudeatlas.com/api/v1/search?q=auth&type=plugin' | jq '.results | length'` returns > 0. Same for `?type=mcp_server`.

4. **Bidirectional bundle graph populated.** `cat data/skills.ndjson | head -100 | jq 'select(.bundled_in_plugins | length > 0) | .id'` returns at least one skill (assuming at least one Featured plugin bundles a skill in the catalog).

5. **Tier composition sane.** From pipeline-stats.json: plugin tier distribution ≈ 10% featured / 30% solid / 60% listed (within ±2pp at N≈4500). MCP tier distribution: ~4 featured / ~11 solid / ~23 listed (at N≈38).

6. **Cost ceiling preserved.** No new paid services added; Vectorize overage stays within $30/yr budget per CONTEXT projection ($0.35/mo for 7M new dims).

7. **F2 D+7 cutover (~2026-06-04) not blocked.** Confirm legacy-skill-reader.js's plugin/MCP upcast paths are reachable only by v1-shape records — assert no v1-shape plugin/MCP records exist on disk post-3.2 ship. If true, the D+7 cleanup PR can safely delete legacy-skill-reader.js (including the plugin/MCP additions from Task 6).

</verification>

<success_criteria>

Phase 3.2 ships successfully when:

- [ ] All 14 tasks committed (1 commit per task minimum) with green branch-CI dispatch.
- [ ] `data/plugins.ndjson` carries schema_version=2 header sentinel + entity_type='plugin' on every record + populated extra.* fields per the expanded PluginExtra.
- [ ] `data/mcp-servers.ndjson` carries schema_version=2 + entity_type='mcp_server' on every record.
- [ ] `data/skills.ndjson` records gain `bundled_in_plugins` field (may be empty array).
- [ ] All 6 new modules registered in their respective registries (scorers/index.js, filter-rules/index.js, recipes/index.js, embedding-input/index.js).
- [ ] daily-scrape.yml runs all new steps under the 5.5h bumped timeout.
- [ ] /api/v1/search returns plugin and MCP results when filtered by `?type=`.
- [ ] All 3 feed files (whats-new, trending, notable) carry mixed-type entries with `_claudeatlas.type_chip` populated.
- [ ] llms.txt dynamic counts include plugin + mcp_server.
- [ ] Banned-patterns lint clean (CI `lint` job green).
- [ ] Asset-size guard + file-count guard pass (3.2 adds at most ~1,800 plugin SVG badges, well under the 95k file cap — note: badge generation is NOT in this phase; deferred to Phase 3.3, so the file count delta from 3.2 ship is effectively zero static assets).
- [ ] Cron-audit doc + smoke-results doc committed.
- [ ] No site outage during deploy (zero-downtime per PROJECT.md constraint).

</success_criteria>

<output>

After completion, create `.planning/phases/3.2-plugin-and-mcp-scoring/3.2-SUMMARY.md` per the standard summary template, capturing:

- Final record counts per entity_type
- Tier distribution per entity_type
- Cron runtime (pre- vs post-3.2)
- Deviations from plan (e.g., if Option B in Task 13 was needed instead of D+C)
- Cost delta (should be ~$0.35/mo Vectorize overage as projected)
- Open items for Phase 3.3 (plugin pages will consume this data)
- Confirmation that D+7 cutover (~2026-06-04) is NOT blocked by anything shipped here

Then commit the SUMMARY and merge to main. Phase 3.3 is the natural next phase (plugin detail pages consume the data this phase produces).

</output>
