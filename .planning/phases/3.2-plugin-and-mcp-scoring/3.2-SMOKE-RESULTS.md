# Phase 3.2 — Branch-CI smoke results (Task 14)

**Date:** 2026-05-31
**Branch:** `docs/3.2-plan-and-check`
**Verifier:** `scripts/verify-3.2-roundtrip.js`

This is the final go/no-go artifact before merging Phase 3.2 to `main`. It has
two parts: (A) the locally-verified envelope round-trip, and (B) the
production-scale branch-CI dispatch (the live gate for B-1 / B-2 / B-3).

---

## Part A — Local v2 round-trip (PASSED)

`node scripts/verify-3.2-roundtrip.js` → **exit 0** (fixture mode; real
`data/*.ndjson` are gitignored and rebuilt only in CI, so the verifier
validates the committed mini fixtures' envelope shape locally and defers the
F-4 build/asset-size guard to the branch-CI run on real data).

Verified locally:

- `data/__fixtures__/plugins-mini.ndjson` — `_header` sentinel with
  `schema_version=2` + `entity_type=plugin`; every record `entity_type=plugin`,
  `schema_version=2`, populated `extra.plugin_path` / `extra.manifest` /
  `extra.bundled_skills[]`.
- `data/__fixtures__/mcp-mini.ndjson` — `_header` with `schema_version=2` +
  `entity_type=mcp_server`; every record `entity_type=mcp_server`,
  `schema_version=2`, populated `extra.server_path` / `extra.manifest` /
  `extra.tools[]`.

Supporting local gates (all green):

- `node scripts/check-banned-patterns.js --mode=lint` → clean (0 new). F-6
  allowlist delta for Task 12 = **zero** new entries.
- `npx js-yaml .github/workflows/daily-scrape.yml` → valid.
- Scorer registry exposes `skill`, `plugin`, `mcp_server`.
- Task-suite tests (filter / feeds / embed / enrich / bundled-links /
  upload-vectors) green.

In PRODUCTION mode (after a pipeline run leaves real `data/plugins.ndjson` +
`data/mcp-servers.ndjson` on disk), the same verifier additionally asserts:
`skills.ndjson` carries `bundled_in_plugins` on every record;
`pipeline-stats.json` has both `plugin` and `mcp_server` sections; and runs
`npm run build` + `check-asset-sizes.js` (F-4: 24 MiB per-asset + 95k-file
caps).

---

## Part B — Production branch-CI dispatch (PENDING — human-action gate)

Step 2/3 of Task 14 require a live, production-scale `daily-scrape.yml` run on
the feature branch. This is a **human-action gate**: the run spends real OpenAI
embedding credit, takes up to 330 min, and must be initiated + observed by the
maintainer (it cannot be driven from inside the executor). Deploy/publish/upload
steps are branch-gated OFF (`github.ref == 'refs/heads/main'`), so the dispatch
exercises scrape→filter→link→embed→enrich→build without touching production
search, KV, or the live site.

### Dispatch + watch

```bash
git push -u origin docs/3.2-plan-and-check
gh workflow run daily-scrape.yml --ref docs/3.2-plan-and-check
gh run watch   # or: gh run list --workflow=daily-scrape.yml --limit 1
```

### Assertions to record here after the run completes

Fill these in from `gh run view <id> --json jobs` and the step logs:

- [ ] **B-3 runtime gate.** Actual total runtime ≤ 330 min AND ≤ Task 13
      projected post-mitigation (~293 min) + 15 min variance (≈ 308 min). If
      exceeded → escalate to Option B (split plugin discovery to a weekly
      workflow) per `3.2-CRON-AUDIT.md` before merging.
      `run id: ____  total: ____ min`
- [ ] **All new steps green:** Run plugin discovery, Filter plugins, Filter
      MCPs, Link bundles, Embed plugins, Embed MCPs, Enrich plugins, and the
      Upload-vectors loop (the upload step itself is main-gated OFF on the
      branch — confirm it shows as skipped, not failed).
- [ ] **Deploy / Publish KV / Publish skills-latest / Upload-vectors = SKIPPED**
      (branch is not `main`).
- [ ] **No banned-patterns lint failures** in the `lint` job.
- [ ] **B-1 tier distribution (MCP).** From `pipeline-stats.json` `mcp_server`
      section: at N=38, uniform `Math.floor` percentile split = **3 featured /
      11 solid / 24 listed**. No small-N carve-out.
      `featured: __  solid: __  listed: __`
- [ ] **B-1 tier distribution (plugin).** `plugin` section ≈ 10% featured /
      30% solid / 60% listed (±2pp at N≈4.5k).
      `featured: __  solid: __  listed: __`
- [ ] **B-2 embed cache-hit.** Skill embed step log shows cache hit ≥ 99%
      (the embed-skills.js pre-check refuses < 99% absent `EMBED_FORCE_REEMBED`).
      `cache hit: ____%`
- [ ] **Record counts.** plugins.ndjson: ____ ; mcp-servers.ndjson: ____ ;
      skills.ndjson: ____ ; skills with `bundled_in_plugins` non-empty: ____.
- [ ] **F-4 asset-size guard** passed inside the build step (no asset > 24 MiB;
      file count < 95k).
- [ ] **Anomalies:** ____

### Sign-off (complete after Part B passes)

> Phase 3.2 verified at branch-CI production scale on ____-__-__;
> B-1/B-2/B-3 all production-verified; ready to merge to main.

Until Part B is signed off, the local Part A gate (round-trip + lint + tests +
projection ≤ 320 min) is green; merge is blocked only on the live production
dispatch, which is the maintainer's go/no-go.
