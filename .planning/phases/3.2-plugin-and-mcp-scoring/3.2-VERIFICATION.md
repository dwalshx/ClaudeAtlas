---
phase: 3.2-plugin-and-mcp-scoring
verified: 2026-05-31T13:40:00Z
status: human_needed
score: 13/13 must-have truths verified (automated); 1 production-scale gate pending human dispatch
re_verification: false
human_verification:
  - test: "Production-scale branch-CI smoke (daily-scrape.yml on docs/3.2-plan-and-check)"
    expected: "B-1 (uniform tiers at production N), B-2 (>=99% skill embed cache hit on live OpenAI), B-3 (full run <= 330 min) all assert PASS; deploy/publish/upload steps branch-gated OFF"
    why_human: "Spends real OpenAI credit and runs up to 330 min; cannot run inside verification. Documented human-action gate in 3.2-SMOKE-RESULTS.md Part B."
---

# Phase 3.2: Plugin + MCP Scoring Verification Report

**Phase Goal:** Expand the catalog pipeline to score, filter, embed, tier, bundle-link, and publish two new entity types (`plugin`, `mcp_server`) using the polymorphic envelope + v2 writer pattern, surfaced in mixed-type feeds/llms.txt and wired into the daily-scrape cron.
**Verified:** 2026-05-31T13:40:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1  | Plugin records exist as EntityRecord<PluginExtra> in data/plugins.ndjson via writeNdjsonStreaming + buildHeader('plugin') | ✓ VERIFIED | Ran filter-plugins.js against real data/plugins-raw.ndjson (7339 records) → 3584 plugins written; header line `{"_header":true,"schema_version":2,"entity_type":"plugin",...}`; sample record entity_type=plugin |
| 2  | MCP records exist as EntityRecord<McpExtra> in data/mcp-servers.ndjson via buildHeader('mcp_server') | ✓ VERIFIED | Ran filter-mcps.js → 76 mcp_server records written; header `entity_type":"mcp_server"`; filter-mcps.js:232 writeNdjsonStreaming with buildHeader('mcp_server') |
| 3  | Plugin/MCP scorers + rule packs + recipes registered in F2 registries | ✓ VERIFIED | scorers/index.js:21-22, filter-rules/index.js:29/32, recipes/index.js:16-17, embedding-input/index.js:16-17 all register plugin + mcp_server |
| 4  | Plugin filter applies MIN_DESC=50/COMPONENT_FLOOR/HAS_MANIFEST_OR_LISTING; MCP applies HAS_TRANSPORT + >=1 tool | ✓ VERIFIED | plugin.rules.js isPluginSlop + mcp.rules.js isMcpSlop present; tests pass (plugin.rules.test.js / mcp.rules.test.js) |
| 5  | Percentile tiers (10/30/60) applied UNIFORMLY per entity_type, NO small-N carve-out (D-04) | ✓ VERIFIED | Live run: plugins 358/1075/2151 = Math.floor(N*.1/.3); MCPs 7/22/47 from N=76. No carve-out branch exists (only comments/tests asserting its absence). tier-assignment.test.js passes |
| 6  | Bidirectional bundle graph populated (skill.bundled_in_plugins[] + plugin.extra.bundled_*[]) | ✓ VERIFIED | Ran link-bundles.js against real skills.ndjson (23056) + plugins (3584): "129 plugins carry bundles; 7258 skills bundled"; both files atomically rewritten; 2363 plugins carry forward edges |
| 7  | Plugin/MCP embedding inputs produce vectors with metadata.entity_type | ✓ VERIFIED | Dry-run embed of plugins.ndjson → 3584 vectors, dims=1536, metadata.entity_type=plugin |
| 8  | Skill embedding-input refactor byte-identical to legacy (cache-preservation) | ✓ VERIFIED | embed-skills.js:161 freezes computeContentSha to legacy pipe-join (documented deviation); embed-skills.test.js 100%-cache-hit test passes; legacy-builder parity test in embedding-input/index.test.js passes |
| 9  | Plugin enrich runs cosine dedup; MCP enrich skipped (D-10) | ✓ VERIFIED | enrich.js:288-291 early-returns exit 0 for entity_type=mcp_server with documented rationale |
| 10 | Feeds emit mixed-type entries with _claudeatlas.type_chip | ✓ VERIFIED | generate-feeds.js:115 `type_chip: skill.entity_type || 'skill'`; generate-feeds.test.js passes |
| 11 | llms.txt count line includes plugin + mcp_server counts dynamically | ✓ VERIFIED | generate-llms-txt.js:74-77 reads stats.plugin.total_plugins + stats.mcp_server.total_mcp_servers |
| 12 | daily-scrape.yml runs plugin discovery/filter/embed/enrich steps | ✓ VERIFIED | daily-scrape.yml:208/215/224 invoke filter-plugins.js, filter-mcps.js, link-bundles.js |
| 13 | Cron runtime audit with projection table; mitigation keeps run < 320 min margin (timeout 330) | ✓ VERIFIED | 3.2-CRON-AUDIT.md exists; timeout 300→330 in workflow; ~293 min projection documented (SUMMARY + CLAUDE.md note 12) |

**Score:** 13/13 truths verified by automated means. 1 production-scale assertion (truth 12's "branch-CI dispatch validates end-to-end at production scale") deferred to human gate.

### Required Artifacts

| Artifact | Status | Details |
| -------- | ------ | ------- |
| scripts/lib/scorers/plugin.scorer.js | ✓ VERIFIED | scorePlugin exported (L80); wired via scoreEntity in filter-plugins.js:181 |
| scripts/lib/scorers/mcp.scorer.js | ✓ VERIFIED | scoreMcp exported (L53); registered scorers/index.js:22 |
| scripts/lib/filter-rules/plugin.rules.js | ✓ VERIFIED | isPluginSlop (L31); wired via isSlop in filter-plugins.js:178 |
| scripts/lib/filter-rules/mcp.rules.js | ✓ VERIFIED | isMcpSlop (L24); registered |
| scripts/lib/recipes/{plugin,mcp}.recipe.js | ✓ VERIFIED | pluginRecipe/mcpRecipe shape-conformant stubs; recipes/index.js registers both; tests pass |
| scripts/lib/embedding-input/{index,skill,plugin,mcp}.js | ✓ VERIFIED | buildEmbeddingInput dispatch (L28); per-type builders exported |
| scripts/lib/tier-assignment.js | ✓ VERIFIED | assignPercentileTiers (L30); called by filter.js:288, filter-plugins.js, filter-mcps.js:156 |
| scripts/lib/bundled-links.js | ✓ VERIFIED | linkBundles (L57); proven against real data |
| scripts/filter-plugins.js | ✓ VERIFIED | 283 lines; full read→upcast→slop→score→tier→write; produces 3584 real records |
| scripts/filter-mcps.js | ✓ VERIFIED | 256 lines; derives mcp_server records from component_summary (F-1/F-2 deviation); 76 real records |
| scripts/link-bundles.js | ✓ VERIFIED | 81 lines; atomic rewrite of both files via writeNdjsonStreaming |
| scripts/verify-3.2-roundtrip.js | ✓ VERIFIED | 188 lines; F-4 check-asset-sizes guard at L153-160 |

### Key Link Verification

| From | To | Status | Detail |
| ---- | -- | ------ | ------ |
| filter-plugins.js | scoreEntity → plugin.scorer | ✓ WIRED | L181 `scoreEntity(e)` |
| filter-plugins.js | isSlop → plugin.rules | ✓ WIRED | L178 `isSlop(e)` |
| filter-plugins.js | data/plugins.ndjson via buildHeader('plugin') | ✓ WIRED | L259 |
| filter-mcps.js | data/mcp-servers.ndjson via buildHeader('mcp_server') | ✓ WIRED | L232 |
| filter.js/filter-plugins/filter-mcps | tier-assignment.js | ✓ WIRED | assignPercentileTiers imported + called in all three |
| embed-skills.js | embedding-input/index.js | ✓ WIRED | L52/189 buildEmbeddingInputRegistry |
| upload-vectors.js | Vectorize metadata.entity_type | ✓ WIRED | L143-152 surfaces entity_type distribution; proven plugin vectors carry metadata.entity_type=plugin |
| generate-feeds.js | feeds type_chip | ✓ WIRED | L115 |
| daily-scrape.yml | filter-plugins/mcps/link-bundles | ✓ WIRED | L208/215/224 |
| link-bundles.js | filter.js PRESERVED_FIELDS includes bundled_in_plugins | ✓ WIRED | filter.js:64 bundled_in_plugins in PRESERVED_FIELDS (F-3) |

### Data-Flow Trace (Level 4)

| Artifact | Source | Produces Real Data | Status |
| -------- | ------ | ------------------ | ------ |
| data/plugins.ndjson | filter-plugins.js ← plugins-raw.ndjson (7339) | 3584 scored+tiered records; manifest_completeness=0.4 on sample | ✓ FLOWING |
| data/mcp-servers.ndjson | filter-mcps.js ← component_summary.mcp_servers | 76 records (7/22/47 tiers) | ✓ FLOWING |
| bundle graph | link-bundles.js ← skills + plugins | 129 plugins bundling, 7258 skills back-linked, 2363 forward edges | ✓ FLOWING |
| plugin vectors | embed-skills.js (dry-run) | 3584 × 1536-dim, metadata.entity_type=plugin | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Plugin filter produces real records | node scripts/filter-plugins.js | 3584 plugins, 358/1075/2151 tiers | ✓ PASS |
| MCP filter produces real records | node scripts/filter-mcps.js | 76 mcp_servers, 7/22/47 tiers | ✓ PASS |
| Bundle graph links bidirectionally | node scripts/link-bundles.js | 129 plugins / 7258 skills linked | ✓ PASS |
| Embed dispatch sets metadata.entity_type | EMBED_DRY_RUN=1 embed --entity-type plugin | 3584 vectors, entity_type=plugin | ✓ PASS |
| Banned-patterns lint | node scripts/check-banned-patterns.js --mode=lint | clean (0 new) | ✓ PASS |
| Phase 3.2 unit tests | node --test (7 unit files) | 36/36 + others pass | ✓ PASS |
| Phase 3.2 integration tests | node --test (9 files) | all pass | ✓ PASS |

### Requirements Coverage

Requirement IDs 3.2-DOD-1 .. 3.2-DOD-12 are tracked in plan/SUMMARY frontmatter (not in REQUIREMENTS.md, by design — different ID scheme). All 12 marked requirements-completed in SUMMARY; each maps to a verified truth above. No orphaned requirements.

### Anti-Patterns Found

None blocking. The recipes are intentional SHAPE-CONFORMANT STUBS (discover() empty) — documented per F-7, real discovery in scrape-plugins.js. This is a designed-stub, not an unintended one.

### Test Suite Status

Full suite: 115/117 pass. Both failures originate from `scripts/test/upcaster-render-test.js` (file-level + nested subtest counted) — a Phase 3.1.2 manual post-build smoke that fails on "missing rebuilt HTML" (a stale dist/ precondition), NOT a content regression and NOT a Phase 3.2 deliverable. Excluded from 3.2 gap accounting per verification scope. All Phase 3.2 test files pass.

### Human Verification Required

**1. Production-scale branch-CI smoke**
- **Test:** `gh workflow run daily-scrape.yml --ref docs/3.2-plan-and-check && gh run watch`
- **Expected:** B-1 uniform tiers at production N; B-2 ≥99% skill embed cache hit on live OpenAI; B-3 full run ≤ 330 min. Deploy/publish/upload steps branch-gated OFF (non-destructive).
- **Why human:** Spends real OpenAI credit, runs up to 330 min — cannot execute inside verification. Documented gate in 3.2-SMOKE-RESULTS.md Part B and SUMMARY "Blocker before main merge."

### Gaps Summary

No automated gaps. Every must-have truth and artifact was verified against the actual codebase, including end-to-end behavioral spot-checks run against real production input data (plugins-raw.ndjson, skills.ndjson) that exercised the full filter → score → tier → bundle-link → embed data flow and confirmed real, non-empty, correctly-shaped output at each stage.

The single outstanding item is the documented production-scale branch-CI smoke (B-1/B-2/B-3) — a human-action gate that cannot run inside verification (real OpenAI cost + 330-min runtime). Per the verification scope this is human_verification, not an automated gap. The phase is functionally complete and ready to merge once a maintainer dispatches and signs off the smoke.

**Note on F-1/F-2 deviation (filter-mcps.js):** The plan assumed plugins-raw.ndjson carried records tagged `entity_type:mcp_server`; reality is the scraper writes repo-level records with `component_summary.mcp_servers > 0`. filter-mcps.js correctly adapted to DERIVE mcp_server EntityRecords from component summaries (documented in-file at L9-18). Live N=38 repos → 76 mcp_server records (vs. the plan's assumed ~38), but uniform percentile tiering still applies with no carve-out, so D-04 holds. This is a correct, documented adaptation, not a gap.

---

_Verified: 2026-05-31T13:40:00Z_
_Verifier: Claude (gsd-verifier)_
