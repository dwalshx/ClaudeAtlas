# Phase 3.1.2 — Cutover Plan

**Default cutover window:** D+7 (per Task 3 decision: proceed-as-planned).

## Schedule

| Day | Action |
|---|---|
| **D+0** | F2 merge to main. Lint runs in WARNING mode. Dual-shape envelope live (upcaster preserves legacy flat fields). |
| **D+1 through D+6** | Monitor daily-cron. Watch for `lint:legacy-shape` warnings on PRs. Migrate any newly-discovered consumers. |
| **D+7** | Cutover PR: |
|        | 1. Delete `src/lib/skills.js` shim. |
|        | 2. Delete `scripts/lib/legacy-skill-reader.js` (upcaster no longer needed once on-disk data is v2). |
|        | 3. Delete `scripts/migrate-to-entities.js` (T9 migrator — one-shot, done). |
|        | 4. Remove dual-shape block from any code paths. |
|        | 5. Flip `scripts/lint-no-legacy-skill-shape.js` default mode from `warning` → `error`. |
|        | 6. Update workflow `.github/workflows/lint.yml` to assert `--mode=error`. |

## Rollback

If post-D+0 anomalies surface before D+7:

1. **Site-level:** Revert F2 merge commit. Daily-cron continues consuming
   v1 NDJSON via F1's reader (header-aware path handles either shape).
2. **Data-level:** `gh release download skills-latest-prev --pattern skills.ndjson --output data/skills.ndjson`
   then `gh release upload skills-latest data/skills.ndjson --clobber`. ~5 min to live.

If post-D+7 anomalies surface (cutover commit problematic):

1. Revert the cutover commit (re-introduces shim + legacy reader).
2. Revert F2 merge commit if root cause is in F2 itself.
3. Re-publish v1 release asset via the rollback CLI above.
4. 30-day window before `.v1.bak` files are GC'd.

## Tracked deviations

Per execution prompt:
- **T2.5 (Vectorize tag-array probe):** deferred to Phase 3.4 (no staging
  Vectorize index access at execution time). T8 conservatively populated
  `entity_type` metadata only (NOT `tags`) and worker `?category` resolves
  against legacy `metadata.category` (untouched). Tag-array filtering is a
  3.4 optimization, not a F2 blocker.

- **B1 — `scripts/scrape-discover-repos.js` recipe fold:** deferred. The
  recipe abstraction (T5) created the engine + skill recipe shape, but the
  Track-2 discovery script remains standalone for F2. Folding it requires
  the full engine to grow real HTTP/ETag/checkpoint primitives, which is
  best done in 3.2 when the second recipe (plugins) forces the abstraction
  boundary. Track-2 still writes v1 records; T9 migration upcasts them
  along with everything else. Not a correctness issue — just structural.
