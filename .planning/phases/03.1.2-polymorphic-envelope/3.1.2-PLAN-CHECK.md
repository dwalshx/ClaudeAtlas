---
plan_check_for: 3.1.2-polymorphic-envelope
checker: gsd-plan-checker
date: 2026-05-18
verdict: PASS
round: 2
---

# F2 Plan Check (Round 2)

## Verdict

**PASS with two minor caveats.** Rev 2 systematically addresses every Round-1 BLOCKER, FLAG, and cross-phase finding. The B1 / B2 / B3 / B4 / B5 / B6 fixes are concrete (specific tasks, specific verify commands, specific reorderings) rather than hand-waved. The `_header` contract is now correctly partitioned (F1 Rev 3 owns the mechanism, F2 owns the header schema and a pre-flight verifier). T11 → T9 reordering plus the `skills-latest-staging` asset name makes the smoke gate genuinely gate production data. The Vectorize tag-array uncertainty is now resolved by an explicit T2.5 probe with three documented outcome branches, each wired into T8. The two residual caveats are not blockers — the F1 Rev 3 plan-check has now been updated to PASS (this file's promotion from R2 to canonical resolves NEW-F1), and the Smoke J workflow_dispatch input (NEW-F3) is a small executor-time addition.

## Resolved from Round 1

### BLOCKERs (6/6 resolved)

- **B1 (3 missed consumers)** — RESOLVED. All three scripts in `files_modified`. `scrape-discover-repos.js` → T5 Step 1.5; `backfill-skill-birth-dates.js` → new T7.5; `generate-badges.js` → T8 Step 6. Pre-T11 grep gate at PLAN.md L474–480 + T11 body L1256–1261. Lint script `scripts/lint-no-legacy-skill-shape.js` (T10) is standing guard. (Note: `generate-badges.js`'s current source reads only common fields — Round-1 grep was a false positive on SVG text. Defensive audit is harmless.)
- **B2 (Astro check toolchain)** — RESOLVED. T1 Step 0 installs `@astrojs/check` + `typescript`. All `astro check` verifies fail-closed. R1 fallback documented at L402–407 + L1436–1440.
- **B3 / X1 (`_header` contract)** — RESOLVED. F2 disclaims mechanism ownership (Approach §0), delegates to F1 Rev 3. T1 pre-flight checks F1 Rev 3 PASS verdict + `_header` mentions in `ndjson.js`. T4 positive-asserts no header leak. Cross-verified against F1 PLAN.md lines 143–168, 425–429. Symmetric contract.
- **B4 (Worker tier filter)** — RESOLVED. Approach §2.1 enumerates triad (`?tier` KEEP, `?category` KEEP+fallback, `?type` NEW). T8 Step 5 repeats inline. Regression set requires ≥4 tier-filtered queries. Smoke I asserts non-empty results.
- **B5 (DOD-9/DOD-10)** — RESOLVED. T2 Step 2 captures `data/regression-sitemap-urls.txt`. T11 Smoke F (sitemap diff) + Smoke G (HTTP-200 sweep). Success criterion #18 explicit.
- **B6 (T9 before T11)** — RESOLVED. T11 in Wave 5, T9 in Wave 6. T11 uses `skills-latest-staging`. T9 tags `skills-latest-prev` before publishing. Rollback CLI documented.

### FLAGs (8/8 resolved)

- **F1** RESOLVED — T6 Step 0 enumerates `f4dfb56` chunks (a)-(f) with IN/NOT-IN-F2 calls.
- **F2** RESOLVED — JSDoc semantic + post-trim invariant assert in `filter.js`.
- **F3** RESOLVED — T2.5 probe with 3 outcome branches; T8 Step 4 consumes each.
- **F4** RESOLVED — T7 Step 3 explicit "do not touch `getStaticPaths()` filter"; Smoke C ±1% file count.
- **F5** RESOLVED — "5-7 days end-to-end" with overrun-pattern citation.
- **F6** RESOLVED — R9 added; pre-T1 informal typedef review.
- **F7** RESOLVED — DOD-8 narrowed to NDJSON only; `loadEntities` sniff throws on `[` first byte.
- **F8** RESOLVED — Smoke E split into E.1 (field property) + E.2 (HTML re-render).

### Cross-phase (4/4 resolved)

- **X1** — F1 Rev 3 ships mechanism (lines 425–429); F2 uses it (lines 282–289). Symmetric.
- **X2** — `entities.js::loadEntities` uses F1's `loadAllSkillsSync` (lines 838, 848). No parallel reader.
- **X3** — Covered by F4.
- **X4** — T1 pre-flight gates on F1 Rev 3 PASS verdict. **NOTE:** F1 Rev 3 is now PASS (this file's promotion resolves the operational gate).

## New BLOCKERs

None.

## New FLAGs

- **NEW-F1 (operational, RESOLVED at file-promotion time):** F1 Rev 3's plan-check must be readable as `verdict: PASS` in `3.1.1-PLAN-CHECK.md` (not a sidecar) for F2 T1 pre-flight to pass. Resolved by overwriting the canonical PLAN-CHECK.md with Round 2 content.

- **NEW-F2 (cosmetic):** `scripts/generate-badges.js` reads only common fields today. Round-1 grep was a false positive on SVG label text. T8 Step 6 defensive audit produces zero diff to this file — harmless.

- **NEW-F3 (defensive, EXECUTOR FOLLOWUP):** T11 Smoke J references `gh workflow run daily-scrape.yml --field staging_asset=true`. Current `.github/workflows/daily-scrape.yml` does not expose a `staging_asset` workflow_dispatch input. Two options for the executor:
  1. Add `staging_asset: { type: boolean, default: false }` to the workflow_dispatch inputs (small edit in F1 T5 §5e — could be folded in opportunistically while F1 executes).
  2. T11 Smoke J switches to a local pipeline run on the `f2-branch` instead of triggering the workflow.
  Either works. Safety property holds regardless: production `skills-latest` is untouched until T9 due to T11 → T9 ordering. Smoke J failure surfaces during execution, not silently corrupts.

## Cross-phase findings status (X1–X4)

| ID | Status | Evidence |
|----|--------|----------|
| X1 | RESOLVED | F1 PLAN.md (rev: 3) ships `_header` semantics (lines 425–429); F2 T1 pre-flight verifies; F2 T4 positive-asserts. |
| X2 | RESOLVED | F2 entities.js uses `loadAllSkillsSync`. Recorded in `key_links`. |
| X3 | RESOLVED | T7 Step 3 preserves `getStaticPaths()` tier filter; Smoke C ±1% catches regressions. |
| X4 | RESOLVED | T1 pre-flight grep on F1 PLAN-CHECK now finds `verdict: PASS`. |

## Detailed findings per criterion

1. **Consumer migration completeness.** Re-grepped; all hits in `files_modified` or excluded paths.
2. **Discriminated union compiles.** T1 Step 0 + fail-closed verifies. R1 fallback documented.
3. **Backward-compat reader correctness.** Three layers: upcaster + F1 header filter + sniff-and-throw.
4. **Tag schema completeness.** Unchanged from Rev 1 PASS.
5. **Search ranking viable at F2 day 1.** v1 formula reduces to identity for skill-only catalog.
6. **Recipe engine works for plugins (Smoke D).** Hand-traced against `scripts/scrape-plugins.js`.
7. **Filter rule pack viable.** Body-length invariant + ±1% record count diff.
8. **T2.5 probe handles all 3 branches.** A/B/C wired into T8 Step 4 unambiguously.
9. **T11 staging asset doesn't trample production.** `skills-latest-staging` + T9 ordering + `skills-latest-prev` pre-tag. Three layers.

## Summary recommendation

**PASS — ready for execution.** F1 + F2 can now begin sequential execution per the plan. NEW-F3 is the only operational followup; either path is fine.

Proceed with `/gsd:execute-phase 3.1.1-streaming-foundation`. F2 execution begins after F1 ships and its verification passes.
