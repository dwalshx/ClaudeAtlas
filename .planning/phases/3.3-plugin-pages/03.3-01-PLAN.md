---
phase: 3.3-plugin-pages
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/scrape-plugins.js
  - scripts/filter-plugins.js
  - scripts/__tests__/scrape-plugins.test.js
  - scripts/__tests__/marketplace-resolve.test.js
  - src/lib/types.d.ts
  - src/lib/types.js
autonomous: true
requirements: [D-02, D-08]
must_haves:
  truths:
    - "loadCheckpoint() round-trips an NDJSON .partial and recovers processedSet"
    - "filter-plugins propagates the declared marketplace name alongside the path"
    - "npm run check:patterns is green (no banned readFileSync/JSON.parse on data/)"
  artifacts:
    - path: "scripts/__tests__/scrape-plugins.test.js"
      provides: "loadCheckpoint NDJSON round-trip regression test"
      contains: "loadCheckpoint"
    - path: "scripts/__tests__/marketplace-resolve.test.js"
      provides: "declared-name resolution unit test"
      contains: "marketplace"
    - path: "scripts/scrape-plugins.js"
      provides: "streaming-NDJSON loadCheckpoint"
      contains: "readNdjsonRecords"
    - path: "scripts/filter-plugins.js"
      provides: "marketplace_listings carries declared name"
  key_links:
    - from: "scripts/scrape-plugins.js loadCheckpoint"
      to: "scripts/lib/ndjson.js readNdjsonRecords"
      via: "named import"
      pattern: "readNdjsonRecords"
    - from: "scripts/filter-plugins.js marketplaceListings"
      to: "raw.marketplace_manifest.name"
      via: "declared-name read"
      pattern: "marketplace_manifest"
---

<objective>
Fix the two pipeline-correctness defects that gate the plugin re-enable and the install-command UX, test-first per VALIDATION.md Wave 0:

1. **D-02:** `scripts/scrape-plugins.js loadCheckpoint()` uses the F1-banned
   `JSON.parse(readFileSync(PARTIAL_PATH,'utf-8'))` AND is a latent format bug
   (reads a JSON object `.repos`, but `saveCheckpoint` writes NDJSON array-of-records),
   so `processedSet` resume has never worked. Convert to streaming NDJSON.
2. **D-08/Q1 (Option A):** propagate the DECLARED marketplace name so plugin pages can
   render the correct `name@<declared-name>` install token. `marketplaceListings()`
   currently returns only `[raw.repo_full_name]` (the owner/repo PATH); the declared
   name lives in `raw.marketplace_manifest.name` and is dropped.

Purpose: unblock D-01 (the re-enable PR re-runs the pipeline, so the schema change is free)
and prevent the documented #1 install failure ("plugin not found in any marketplace").
Output: two new test files (RED-first), two corrected scripts, the types.d.ts + types.js
shape update kept in sync, check:patterns green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@.planning/phases/3.3-plugin-pages/3.3-CONTEXT.md
@.planning/phases/3.3-plugin-pages/03.3-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- From scripts/lib/ndjson.js — the F1-safe streaming primitives (already imported in scrape-plugins.js for writeNdjsonStreaming). -->
readNdjsonRecords(path, { keyFn })  // returns Map<key, record>; header-aware; V8-string-limit safe
writeNdjsonStreaming(path, recordsArray)  // chunked openSync/writeSync/renameSync

<!-- scripts/scrape-plugins.js current (lines ~28, 294-309) — VERIFIED: -->
import { writeNdjsonStreaming } from './lib/ndjson.js';   // line 28 — add readNdjsonRecords here
const PARTIAL_PATH = join(DATA_DIR, 'plugins-raw.ndjson.partial');
function loadCheckpoint() {  // ~line 294 — BANNED + WRONG FORMAT
  if (!existsSync(PARTIAL_PATH)) return { repos: [], processedSet: new Set() };
  try {
    const data = JSON.parse(readFileSync(PARTIAL_PATH, 'utf-8'));  // ← banned + reads .repos that saveCheckpoint never writes
    const repos = data.repos || [];
    const processedSet = new Set(repos.map(r => r.repo_full_name));
    return { repos, processedSet };
  } catch { return { repos: [], processedSet: new Set() }; }
}
function saveCheckpoint(repos) { writeNdjsonStreaming(PARTIAL_PATH, repos); }  // ~line 306 — writes ARRAY of repo records

<!-- scripts/filter-plugins.js current (lines ~64-71) — VERIFIED: -->
function marketplaceListings(raw) {
  const mm = raw && raw.marketplace_manifest;
  if (!mm || typeof mm !== 'object') return [];
  return [raw.repo_full_name].filter(Boolean);   // ← returns PATH only; declared name (mm.name) dropped
}
// Consumed at repoToPluginEntity extra.marketplace_listings: marketplaceListings(raw)
// PluginExtra.marketplace_listings is in filter.js PRESERVED_FIELDS? NO — it's recomputed each filter run (safe to change shape).

<!-- src/lib/types.js:94 (JSDoc mirror — MUST stay in sync with types.d.ts per CLAUDE.md) — VERIFIED: -->
// * @property {string[]} marketplace_listings   Phase 3.2 — marketplace listing IDs advertising this plugin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — loadCheckpoint NDJSON round-trip fix (D-02)</name>
  <files>scripts/__tests__/scrape-plugins.test.js, scripts/scrape-plugins.js</files>
  <read_first>
    - scripts/scrape-plugins.js (lines 1-40 imports; 285-320 loadCheckpoint/saveCheckpoint)
    - scripts/lib/ndjson.js (readNdjsonRecords + writeNdjsonStreaming signatures)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q2(b) — the exact replacement code
    - scripts/__tests__/filter-plugins.test.js (test-file conventions: node:test, describe/it, tmpdir usage)
  </read_first>
  <behavior>
    - Round-trip: writeNdjsonStreaming(tmpPartial, [{repo_full_name:'a/b',...},{repo_full_name:'c/d',...}]) then loadCheckpoint reading that path recovers processedSet === Set(['a/b','c/d']) and repos.length === 2.
    - Missing-file: loadCheckpoint on a non-existent path returns { repos: [], processedSet: new Set() } (size 0).
    - Regression assertion (the latent bug): a .partial written by saveCheckpoint (NDJSON array) MUST be readable by loadCheckpoint — proving the old JSON-object `.repos` read was wrong.
  </behavior>
  <action>
    Write scripts/__tests__/scrape-plugins.test.js using node:test (import { test } from 'node:test'; import assert from 'node:assert'). Because loadCheckpoint/saveCheckpoint read a module-level PARTIAL_PATH constant, EITHER (a) export a testable variant `loadCheckpointFrom(path)`/`saveCheckpointTo(path, repos)` from scrape-plugins.js and have the constant-based wrappers delegate to them, OR (b) test the round-trip through the exported pure functions. Prefer (a): refactor loadCheckpoint() → `function loadCheckpoint(){ return loadCheckpointFrom(PARTIAL_PATH); }` and add `export function loadCheckpointFrom(path)`; same for save. This keeps the script's main() untouched while making the logic unit-testable (no real data/ file). Guard main() with the existing invoked-as-script idiom so the import doesn't run the scraper.

    Then implement the fix in scripts/scrape-plugins.js:
    - Line 28: `import { writeNdjsonStreaming, readNdjsonRecords } from './lib/ndjson.js';`
    - Replace loadCheckpointFrom body:
      ```js
      export function loadCheckpointFrom(path) {
        if (!existsSync(path)) return { repos: [], processedSet: new Set() };
        try {
          const repos = [...readNdjsonRecords(path, { keyFn: r => r.repo_full_name }).values()];
          const processedSet = new Set(repos.map(r => r.repo_full_name));
          return { repos, processedSet };
        } catch { return { repos: [], processedSet: new Set() }; }
      }
      ```
    - Remove `readFileSync` from the line-25 fs import IF it becomes unused (grep first; writeFileSync/existsSync/mkdirSync likely remain). Do NOT remove imports still in use.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/scrape-plugins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/__tests__/scrape-plugins.test.js` exits 0 with ≥3 passing subtests
    - `scripts/scrape-plugins.js` contains `readNdjsonRecords` and NO `JSON.parse(readFileSync` substring
    - `node scripts/check-banned-patterns.js` exits 0 (scrape-plugins.js no longer flagged)
  </acceptance_criteria>
  <done>loadCheckpoint reads NDJSON; round-trip + missing-file tests pass; check:patterns clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — propagate declared marketplace name (D-08/Q1 Option A)</name>
  <files>scripts/__tests__/marketplace-resolve.test.js, scripts/filter-plugins.js, src/lib/types.d.ts, src/lib/types.js</files>
  <read_first>
    - scripts/filter-plugins.js (lines 55-150 — marketplaceListings, repoToPluginEntity, PRESERVED_FIELDS at top)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q1 (data shape: marketplace_manifest.name, 2,083 empty-listing plugins)
    - src/lib/types.d.ts (PluginExtra.marketplace_listings shape)
    - src/lib/types.js (lines 86-101 — PluginExtra JSDoc typedef; line 94 marketplace_listings — the canonical mirror of types.d.ts per CLAUDE.md, must stay in sync)
    - CLAUDE.md "Reserved keys" + PluginExtra block (marketplace_listings semantics) + the note that types.js JSDoc mirrors types.d.ts
  </read_first>
  <behavior>
    - resolveMarketplaceListings(raw) with a marketplace_manifest carrying name:'ai-dev-kit' and repo_full_name:'BenWeekes/ai-dev' returns one entry exposing BOTH path 'BenWeekes/ai-dev' (for `/plugin marketplace add`) AND name 'ai-dev-kit' (for `@name` install).
    - No marketplace_manifest → returns [] (the 2,083 empty-listing / GitHub-fallback case).
    - marketplace_manifest present but name missing/blank → entry carries path, name falls back to null (page renders GitHub-fallback for step 2).
    - Shape is backward-tolerant: chosen representation MUST be documented in BOTH src/lib/types.d.ts AND src/lib/types.js (the JSDoc mirror) so the page loader (Plan 02) and detail page (Plan 03) consume it deterministically.
  </behavior>
  <action>
    Choose the marketplace_listings element representation: array of objects `{ path: string, name: string|null }` (clearest; the page reads `.path` for step 1, `.name` for step 2).

    Update src/lib/types.d.ts PluginExtra.marketplace_listings JSDoc/type to `Array<{ path: string; name: string | null }>` and add a one-line note that pre-3.3 records stored bare path strings (loader in Plan 02 must normalize both shapes).

    Update src/lib/types.js:94 — the JSDoc mirror that MUST stay in sync with types.d.ts (CLAUDE.md states types.js JSDoc is canonical mirror of types.d.ts). Change the typedef line from
    `@property {string[]} marketplace_listings   Phase 3.2 — marketplace listing IDs advertising this plugin.`
    to
    `@property {Array<{path: string, name: string|null}>} marketplace_listings   Phase 3.3 / D-08 — {path (owner/repo for `/plugin marketplace add`), name (declared marketplace name for `@name`)}. Pre-3.3 records stored bare path strings; loader normalizes both.`

    In scripts/filter-plugins.js refactor `marketplaceListings(raw)` → `resolveMarketplaceListings(raw)`:
    ```js
    function resolveMarketplaceListings(raw) {
      const mm = raw && raw.marketplace_manifest;
      if (!mm || typeof mm !== 'object') return [];
      const path = raw.repo_full_name;
      if (!path) return [];
      const name = (typeof mm.name === 'string' && mm.name.trim()) ? mm.name.trim() : null;
      return [{ path, name }];
    }
    ```
    Update the repoToPluginEntity call site `marketplace_listings: resolveMarketplaceListings(raw)`.

    Write scripts/__tests__/marketplace-resolve.test.js (node:test) covering the three behaviors above. Import resolveMarketplaceListings — export it from filter-plugins.js if not already exported (add to the module exports next to repoToPluginEntity).

    NOTE: marketplace_listings is recomputed every filter run (NOT in PRESERVED_FIELDS — verified), so the shape change applies cleanly on the next pipeline run with no migration needed.
  </action>
  <verify>
    <automated>node --test scripts/__tests__/marketplace-resolve.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/__tests__/marketplace-resolve.test.js` exits 0 with ≥3 passing subtests
    - `scripts/filter-plugins.js` exports `resolveMarketplaceListings` and the repoToPluginEntity extra uses it
    - `src/lib/types.d.ts` PluginExtra.marketplace_listings documents the `{ path, name }` shape + legacy-string note
    - `src/lib/types.js` PluginExtra JSDoc for marketplace_listings shows the `Array<{path, name}>` shape (grep `types.js` for `marketplace_listings` returns the `{path` form, NOT `{string[]} marketplace_listings`) — the JSDoc mirror stays in sync with types.d.ts
    - `node --test scripts/__tests__/filter-plugins.test.js` still exits 0 (no regression to existing filter tests)
  </acceptance_criteria>
  <done>Declared name propagated as {path,name}; tests green; types documented in BOTH types.d.ts and the types.js JSDoc mirror; existing filter tests unbroken.</done>
</task>

</tasks>

<verification>
- `node --test scripts/__tests__/scrape-plugins.test.js` and `.../marketplace-resolve.test.js` both exit 0
- `npm run check:patterns` exits 0 (loadCheckpoint fix removed the only plugin-side banned pattern)
- `node --test scripts/__tests__/filter-plugins.test.js` still green (no marketplace-shape regression)
- `src/lib/types.d.ts` and `src/lib/types.js` agree on the marketplace_listings shape (JSDoc mirror in sync)
</verification>

<success_criteria>
loadCheckpoint round-trips NDJSON and recovers processedSet; declared marketplace name propagated as {path,name} on plugin records; the shape documented in BOTH types.d.ts and the types.js JSDoc mirror; both new test files green; check:patterns clean; existing filter tests unbroken.
</success_criteria>

<output>
After completion, create `.planning/phases/3.3-plugin-pages/3.3-01-SUMMARY.md`
</output>
</content>
</invoke>
