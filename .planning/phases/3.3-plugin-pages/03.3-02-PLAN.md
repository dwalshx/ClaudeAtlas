---
phase: 3.3-plugin-pages
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - src/lib/plugins.js
  - scripts/__tests__/plugins-loader.test.js
autonomous: true
requirements: [D-04, D-05, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "getPlugins() / getMcpServers() return records read from data/plugins.ndjson + data/mcp-servers.ndjson via streaming NDJSON"
    - "getPluginBySlug / getMcpBySlug resolve a record by slug (duplicates resolvable by direct lookup)"
    - "resolveBundledSkills(ids) turns bundled_skills entity IDs into skill records via an ID index"
    - "resolveBundledPlugins(ids) turns skill.bundled_in_plugins IDs into plugin records (reverse edge)"
    - "installCommand(plugin) derives the two-step tokens incl. @declared-name and GitHub-fallback flag"
  artifacts:
    - path: "src/lib/plugins.js"
      provides: "plugin/MCP loader + bundle resolution + install-command derivation"
      min_lines: 60
      exports: ["getPlugins", "getMcpServers", "getPluginBySlug", "getMcpBySlug", "resolveBundledSkills", "resolveBundledPlugins", "installCommand"]
    - path: "scripts/__tests__/plugins-loader.test.js"
      provides: "loader + bundle-graph + slug + install-command unit tests"
      contains: "resolveBundledSkills"
  key_links:
    - from: "src/lib/plugins.js"
      to: "scripts/lib/ndjson.js readNdjsonRecords"
      via: "streaming read of data/plugins.ndjson + data/mcp-servers.ndjson"
      pattern: "readNdjsonRecords"
    - from: "src/lib/plugins.js resolveBundledSkills"
      to: "src/lib/entities.js allSkills"
      via: "skill-by-id Map index"
      pattern: "skillById|allSkills"
---

<objective>
Build the NEW `src/lib/plugins.js` loader that the page layer consumes — the single
most important architectural piece in 3.3. RESEARCH Pitfall 1: `entities.js`
`getEntitiesByType('plugin')` returns `[]` at build time because it reads ONLY
`data/skills.ndjson`. Plugins/MCPs live in separate files this loader must read directly,
mirroring the verified `generate-feeds.js loadExtraEntities()` precedent.

Also provides the bundle-graph ID→record resolution (RESEARCH Pitfall 3: bundled_* arrays
are entity IDs, not slugs — need a Map index) for BOTH directions, and the two-step
install-command derivation (D-08) consuming the {path,name} marketplace shape from Plan 01.

Purpose: a tested, low-blast-radius loader that the four page sets (Plans 03-05) import.
Output: src/lib/plugins.js + its unit test, both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@CLAUDE.md
@scripts/generate-feeds.js

<interfaces>
<!-- VERIFIED loadExtraEntities precedent — scripts/generate-feeds.js:345 -->
function loadExtraEntities(filename, entityType) {
  const p = join(DATA_DIR, filename);
  if (!existsSync(p)) return [];
  return [...readNdjsonRecords(p, { keyFn: (r) => r.id }).values()];
}

<!-- From src/lib/entities.js — allSkills is the FULL catalog (every tier, pre-getStaticPaths filter). VERIFY exact export name on read; CLAUDE.md/RESEARCH reference `allSkills`. -->
export const allSkills /* : EntityRecord[] (all skills, all tiers) */

<!-- Bundle array shapes — RESEARCH Q4, VERIFIED on-disk: -->
plugin.extra.bundled_skills[]       // entity IDs "skill:<repo>/<path>/SKILL.md"  → resolve to skill records → SkillCard
plugin.extra.bundled_mcp_servers[]  // entity IDs "mcp_server:<repo>/<path>"      → resolve to mcp records → /mcp/<slug>/
plugin.extra.bundled_agents[]       // bare NAME strings → chip only
plugin.extra.bundled_commands[]     // bare NAME strings → chip only
plugin.extra.bundled_hooks[]        // bare NAME strings → chip only
skill.bundled_in_plugins[]          // plugin entity IDs "plugin:<repo>/.claude-plugin/plugin.json" → /plugins/<slug>/

<!-- marketplace_listings shape after Plan 01 (Option A): Array<{path:string, name:string|null}>; legacy records may carry bare path strings — normalize both. -->
<!-- Install flow (RESEARCH Q1, docs-verified): step1 `/plugin marketplace add <path>`; step2 `/plugin install <plugin-name>@<declared-name>`. Empty listings → GitHub-fallback only. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — plugins-loader.test.js against on-disk sample data</name>
  <files>scripts/__tests__/plugins-loader.test.js</files>
  <read_first>
    - scripts/generate-feeds.js (lines 330-365 — loadExtraEntities + loadAllEntities precedent)
    - scripts/__tests__/generate-feeds.test.js (how it tests the loader with fixtures/real data)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q3 (loader code) + § Q4 (bundle resolution code)
    - src/lib/entities.js (confirm the exact export for the all-skills array used by the ID index)
  </read_first>
  <behavior>
    - getPlugins() returns a non-empty array when data/plugins.ndjson exists; [] when absent (graceful).
    - getPluginBySlug(knownSlug) returns the record; getPluginBySlug('nope') returns null.
    - getMcpServers() returns records from data/mcp-servers.ndjson; getMcpBySlug works.
    - resolveBundledSkills(['skill:foo/bar/SKILL.md', 'skill:missing/x/SKILL.md']) returns ONLY the records whose IDs exist (filter(Boolean)), preserving order.
    - resolveBundledPlugins(['plugin:foo/.claude-plugin/plugin.json']) resolves reverse-edge plugin records.
    - installCommand(plugin): for a plugin with marketplace_listings [{path:'a/b',name:'mkt'}] returns { hasMarketplace:true, addCmd:'/plugin marketplace add a/b', installCmd:'/plugin install <name>@mkt', githubFallback:true }. For empty listings returns { hasMarketplace:false, githubFallback:true } (no broken install token). For {path,name:null} returns hasMarketplace:true but installCmd null (GitHub fallback for step 2).
    - Loader uses readNdjsonRecords (assert no banned readFileSync introduced — check:patterns covers this at verify).
  </behavior>
  <action>
    Write scripts/__tests__/plugins-loader.test.js (node:test). Per VALIDATION.md the glob is
    scripts/**/*.test.js, so the test MUST live under scripts/__tests__/ (NOT src/lib/) — it
    imports the loader via relative path `../../src/lib/plugins.js`. Use the on-disk sample
    (data/plugins.ndjson = 3,584 records, data/mcp-servers.ndjson = 76) for the happy path; for
    the resolveBundled* unit cases, construct small synthetic ID arrays and assert against records
    actually present in the sample (read a couple of real IDs to anchor the assertions), OR build
    a tiny in-memory fixture if the loader exposes a pure resolver that takes an index. Keep
    assertions resilient to sample-data drift: assert shape/contract (length≥1, slug lookup
    round-trips, missing-ID filtered) rather than specific record contents.
    This task writes the test FIRST (it will fail to import until Task 2 creates the loader).
  </action>
  <verify>
    <automated>node --test scripts/__tests__/plugins-loader.test.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/__tests__/plugins-loader.test.js exists, imports `../../src/lib/plugins.js`
    - Running it before Task 2 fails with a module-not-found / undefined-export error (RED proven)
    - Test covers all 7 exports: getPlugins, getMcpServers, getPluginBySlug, getMcpBySlug, resolveBundledSkills, resolveBundledPlugins, installCommand
  </acceptance_criteria>
  <done>Test file written, asserts the loader contract, fails RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement src/lib/plugins.js</name>
  <files>src/lib/plugins.js</files>
  <read_first>
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q3 loader code + § Q4 bundle index code (copy verbatim, adapt paths)
    - src/lib/entities.js (the all-skills export to import for skillById index)
    - scripts/lib/ndjson.js (readNdjsonRecords signature)
    - CLAUDE.md "Pipeline footguns" (no readFileSync on data/; allowlist note) + footgun #11 (bundled_in_plugins)
  </read_first>
  <action>
    Create src/lib/plugins.js mirroring RESEARCH Q3:
    - DATA dir resolved via fileURLToPath(import.meta.url) → '../../data'.
    - `function load(file)`: existsSync guard → [] (graceful empty), else `[...readNdjsonRecords(p, { keyFn: r => r.id }).values()]`. Import readNdjsonRecords from '../../scripts/lib/ndjson.js'.
    - `const allPlugins = load('plugins.ndjson'); const allMcps = load('mcp-servers.ndjson');`
    - Exports: getPlugins() → allPlugins; getMcpServers() → allMcps; getPluginBySlug(slug)/getMcpBySlug(slug) via .find (NO is_duplicate filter — direct URL must resolve, matches getSkillBySlug).
    - Build ID indexes: `const pluginById = new Map(allPlugins.map(p => [p.id, p]))`, `const mcpById = new Map(allMcps.map(m => [m.id, m]))`. Import the all-skills array from './entities.js' (use the verified export name) → `const skillById = new Map(allSkills.map(s => [s.id, s]))`.
    - resolveBundledSkills(ids) → (ids||[]).map(id => skillById.get(id)).filter(Boolean).
    - resolveBundledMcps(ids) → same via mcpById (for bundled_mcp_servers → /mcp/ links).
    - resolveBundledPlugins(ids) → same via pluginById (reverse edge for skill pages, D-10).
    - installCommand(plugin): normalize marketplace_listings entries (accept {path,name} OR legacy bare-string path → {path, name:null}). Take first listing. Return:
      ```js
      { hasMarketplace: Boolean(listing),
        addCmd: listing ? `/plugin marketplace add ${listing.path}` : null,
        installCmd: (listing && listing.name) ? `/plugin install ${plugin.name}@${listing.name}` : null,
        githubFallback: true }
      ```
    Add module JSDoc citing RESEARCH Q3/Q4 + the entities.js skills-only gap. Do NOT route any of this through the worker (pure build-time).
  </action>
  <verify>
    <automated>node --test scripts/__tests__/plugins-loader.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/__tests__/plugins-loader.test.js` exits 0 (all subtests green)
    - `grep -c "readNdjsonRecords" src/lib/plugins.js` ≥ 1 ; `grep -c "readFileSync" src/lib/plugins.js` == 0
    - `node scripts/check-banned-patterns.js` exits 0 (new loader adds no banned pattern; if an allowlist entry is needed because it reads data/*.ndjson via the helper, NONE is — readNdjsonRecords is the sanctioned path)
    - src/lib/plugins.js exports getPlugins, getMcpServers, getPluginBySlug, getMcpBySlug, resolveBundledSkills, resolveBundledPlugins (and resolveBundledMcps), installCommand
  </acceptance_criteria>
  <done>Loader implemented; all loader tests green; check:patterns clean; bundle resolution works both directions.</done>
</task>

</tasks>

<verification>
- `node --test scripts/__tests__/plugins-loader.test.js` exits 0
- `npm run check:patterns` exits 0 (loader uses readNdjsonRecords, no banned reads)
- `npm test` still green overall (no collateral breakage)
</verification>

<success_criteria>
src/lib/plugins.js reads plugins.ndjson + mcp-servers.ndjson via streaming NDJSON, resolves slugs and bundle-graph IDs in both directions, and derives the two-step install command with declared-name + GitHub fallback. All tests green; check:patterns clean.
</success_criteria>

<output>
After completion, create `.planning/phases/3.3-plugin-pages/3.3-02-SUMMARY.md`
</output>
