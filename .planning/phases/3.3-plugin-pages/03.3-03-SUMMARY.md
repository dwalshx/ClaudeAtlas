---
phase: 3.3-plugin-pages
plan: 03
subsystem: ui
tags: [astro, plugins, bundle-graph, static-pages, copybutton]

# Dependency graph
requires:
  - phase: 3.3-plugin-pages plan 02
    provides: src/lib/plugins.js loader (getPlugins, installCommand, resolveBundledSkills, resolveBundledMcps)
  - phase: 3.3-plugin-pages plan 01
    provides: marketplace_listings {path, name} declared-name propagation (D-08/Q1 Option A)
  - phase: 3.2-plugin-and-mcp-scoring
    provides: plugin EntityRecords in data/plugins.ndjson with bundled_*[] graph + manifest_completeness
provides:
  - Plugin detail page src/pages/plugins/[...slug].astro — static page per non-duplicate plugin at /plugins/<slug>/
  - Two-step marketplace install UX (D-08) with declared-name step 2, step-2 fallback note, GitHub-only CTA, universal View-on-GitHub fallback
  - Prominent grouped Bundles section (D-09): SkillCards (cap 12 + details disclosure), MCP link chips, agent/command/hook name chips
  - Marketplace listing chips (D-06), Manifest-Completeness ScoreBar signal (D-03), JsonLd Article at /plugins/ URLs
affects: [3.3-plugin-pages plan 05 (landing/browse link into these pages), 3.3 plan 07 (re-enable feeds fresh data), 3.5 homepage redesign]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fully-static getStaticPaths with NO STATIC_TIERS filter for sub-cap entity types (D-07)"
    - "Native <details> disclosure for capped card grids (no JS framework)"
    - "JSX-style {/* */} comments in Astro templates where HTML comments would leak strings into grep-verified output"

key-files:
  created:
    - src/pages/plugins/[...slug].astro
  modified: []

key-decisions:
  - "Plugin author links to github.com/<owner> instead of /creators/<owner>/ — creator pages only exist for skill authors; plugin-only owners would 404"
  - "Marketplace chips (D-06) link to github.com/<path> and display the declared name when known, path otherwise"
  - "Bundles section uses a native <details> show-all disclosure (largest bundler: 3,324 skills -> 5.97 MB page, under the 24 MiB asset guard)"
  - "Did NOT widen check-sitemap-completeness.js despite the postbuild failure — that is Plan 05 Task 2's explicit deliverable in this same wave (parallel-agent file conflict)"

patterns-established:
  - "Sub-cap entity detail pages: clone skills/[...slug].astro, drop tier filter, swap entity-specific signal into ScoreBar"

requirements-completed: [D-05, D-06, D-07, D-08, D-09, D-11]

# Metrics
duration: 36min
completed: 2026-06-12
---

# Phase 3.3 Plan 03: Plugin Detail Pages Summary

**Static /plugins/<slug>/ detail pages for all 3,584 non-duplicate plugins with two-step marketplace install (declared-name aware + GitHub fallback) and the marquee grouped Bundles section rendering bundled skills as linked SkillCards**

## Performance

- **Duration:** 36 min
- **Started:** 2026-06-12T15:29:33Z
- **Completed:** 2026-06-12T16:05:11Z
- **Tasks:** 2
- **Files modified:** 1 (created)

## Accomplishments

- `/plugins/<slug>/` detail page builds for every non-duplicate plugin (3,584 pages — exactly the non-duplicate count, 0% deviation from the ±5% budget) with NO tier filter (D-07)
- Two-step install block (D-08): `/plugin marketplace add <path>` CopyButton + declared-name `/plugin install name@marketplace` CopyButton when known; amber fallback note pointing to the GitHub README when the declared name is unknown; GitHub-only explainer for the 2,083 empty-listing plugins; View-on-GitHub link always rendered
- Grouped Bundles section (D-09) right after Install: bundled skills as linked SkillCards capped at 12 with a native `<details>` "Show all N" disclosure; MCP servers as `/mcp/<slug>/` link chips; agents/commands/hooks as name chips; empty subgroups hidden; entire section omitted when all five arrays are empty
- ScoreBar reuses the skill page's seven signals with Manifest-Completeness substituting for Frontmatter (D-03); QualityBadge + JsonLd Article shape cloned with /plugins/ URLs; marketplace listing chips (D-06)

## Task Commits

Each task was committed atomically:

1. **Task 1: Plugin detail page — scaffold, install block, ScoreBar/JsonLd** - `a2e1ec0` (feat)
2. **Task 2: Prominent grouped Bundles section (D-09)** - `860926b` (feat)

## Files Created/Modified

- `src/pages/plugins/[...slug].astro` (389 lines) — plugin detail page: getStaticPaths over all non-duplicate plugins, two-step install, Bundles section, ScoreBar/QualityBadge/JsonLd, readme preview, metadata, marketplace chips

## Decisions Made

- **Author link goes to GitHub, not /creators/**: creator pages are generated from skill records only; a plugin-only owner would produce a broken internal link.
- **Breadcrumb links to /plugins/** (lands in Plan 05, same wave) — forward-consistent internal linking.
- **HTML comment converted to JSX comment**: Astro emits `<!-- -->` comments into built HTML; a `<!-- Bundles -->` comment made the "no Bundles string on non-bundling pages" criterion fail on every page. JSX-style `{/* */}` comments are stripped.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] HTML comment leaked "Bundles" string into every built page**
- **Found during:** Task 2 (acceptance criterion: non-bundling page greps clean for "Bundles")
- **Issue:** The `<!-- Bundles (D-09...) -->` placeholder comment is preserved by Astro in output HTML, so `naymspace/backpex` (bundles nothing) still matched a literal grep
- **Fix:** Converted to JSX-style `{/* */}` comment (stripped at compile time); also reworded a frontmatter doc comment that contained the literal `claude install-skill` string (Task 1 criterion requires its absence from the file)
- **Files modified:** src/pages/plugins/[...slug].astro
- **Verification:** Rebuild; `grep -c "Bundles" dist/plugins/naymspace/backpex/index.html` returns 0; bundling page still renders the section
- **Committed in:** 860926b (Task 2 commit) / a2e1ec0 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Cosmetic correctness fix required by the plan's own grep-based acceptance criteria. No scope creep.

## Issues Encountered

- **Postbuild sitemap-completeness gate fails after the new pages land (expected — RESEARCH Pitfall 4).** `npm run build`'s Astro build completes cleanly (15,204 pages) but `check-sitemap-completeness.js` reports 29,032 `<loc>` vs the [23,023, 26,505] skill-based window — the +3,584 plugin pages exceed the un-widened tolerance. Widening the gate's expected basis to include `plugins.ndjson` + `mcp-servers.ndjson` is **Plan 05 Task 2's explicit deliverable** in this same wave; fixing it here would conflict with the parallel executor. `check-asset-sizes` was run manually and passes (23,968 assets, all under 24 MiB; largest plugin page 5.97 MB).
- **Local builds run the exact-engine compute-similar fallback** (hnswlib-node optionalDependency not installed on this win32-arm64 box) — prebuild still completed in ~3 min at the current 23k catalog.
- **Verification-build side effects left uncommitted intentionally:** prebuild regenerated cron-owned artifacts (`.claude-plugin/marketplace.json`, `data/api-graph.json`, `data/similar-skills.json`, `public/feed/*.json`, `public/llms.txt`). The daily cron owns and recommits these; committing locally regenerated versions would embed stale-vector state. Also observed a parallel agent's in-progress edit to `src/pages/skills/[...slug].astro` (Plan 04's D-10 reverse edge) — not touched.

## Known Stubs

None — every section renders real EntityRecord data. The step-2 "declared name isn't known" note is intentional D-08 fallback UX for legacy bare-string listings (all 1,501 on-disk listings are legacy-shaped until the Plan 07 pipeline re-run produces {path, name} records; the page already renders the declared-name CopyButton branch when name is present).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plugin detail pages are live in dist/ — Plan 05's /plugins/ landing + /plugins/browse/ can link into them; Plan 04's /mcp/<slug>/ pages will satisfy the Bundles section's MCP chip links (already emitted, internal-link SEO per plan)
- Plan 05 Task 2 must land the sitemap-gate widening before the phase-level `npm run build` goes fully green
- Fresh {path, name} marketplace listings (Plan 07 re-enable) will automatically activate the step-2 declared-name install command on rebuilt pages

---
*Phase: 3.3-plugin-pages*
*Completed: 2026-06-12*

## Self-Check: PASSED

- src/pages/plugins/[...slug].astro — FOUND
- 03.3-03-SUMMARY.md — FOUND
- Commit a2e1ec0 (Task 1) — FOUND
- Commit 860926b (Task 2) — FOUND
