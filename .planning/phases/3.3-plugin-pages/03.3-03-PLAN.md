---
phase: 3.3-plugin-pages
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/pages/plugins/[...slug].astro
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09, D-11]
must_haves:
  truths:
    - "Every non-duplicate plugin has a static detail page at /plugins/<slug>/ in dist/"
    - "Plugin detail shows the two-step install (marketplace add + install name@name) + GitHub fallback via CopyButton"
    - "A prominent grouped Bundles section renders bundled_skills as linked SkillCards (cap ~12 + show-all) and agents/commands/hooks/mcp as chips"
    - "ScoreBar, QualityBadge, JsonLd render using PluginExtra (manifest_completeness substitutes for skill Frontmatter)"
  artifacts:
    - path: "src/pages/plugins/[...slug].astro"
      provides: "plugin detail page with getStaticPaths over all non-duplicate plugins"
      min_lines: 80
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/plugins/[...slug].astro"
      to: "src/lib/plugins.js getPlugins/installCommand/resolveBundledSkills"
      via: "import + getStaticPaths + render"
      pattern: "getPlugins|installCommand|resolveBundledSkills"
    - from: "src/pages/plugins/[...slug].astro"
      to: "src/components/CopyButton.astro"
      via: "two install CopyButtons"
      pattern: "CopyButton"
---

<objective>
Build the plugin detail page `/plugins/<slug>/` — the headline 3.3 deliverable. Mirror
`src/pages/skills/[...slug].astro` (D-11) but: (a) emit ALL non-duplicate plugins in
getStaticPaths with NO STATIC_TIERS filter (D-07 — plugins are fully static, ~3.5k under
Cloudflare caps), (b) render the two-step marketplace install instead of the skill
single-command (D-08), and (c) render the prominent grouped "Bundles" section (D-09) — the
user's called-out "interesting relationship graph."

Purpose: give plugin EntityRecords a browsable home with correct install UX and the
marquee bundle-graph display.
Output: src/pages/plugins/[...slug].astro producing dist/plugins/<slug>/index.html for every
non-duplicate plugin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@.planning/phases/3.3-plugin-pages/3.3-CONTEXT.md
@src/pages/skills/[...slug].astro
@src/lib/plugins.js
@src/components/CopyButton.astro

<interfaces>
<!-- src/lib/plugins.js (Plan 02): -->
getPlugins()              // EntityRecord[] (all plugins, all tiers; filter is_duplicate in getStaticPaths)
getPluginBySlug(slug)
installCommand(plugin)    // { hasMarketplace, addCmd, installCmd, githubFallback }
resolveBundledSkills(ids) // skill records → <SkillCard>
resolveBundledMcps(ids)   // mcp records → /mcp/<slug>/ links
// bundled_agents/commands/hooks are bare-name strings → chips (no resolution)

<!-- PluginExtra (CLAUDE.md / types.d.ts): commands[], hooks[], marketplace_listings[], bundled_*[], manifest_completeness -->
<!-- getStaticPaths pattern (RESEARCH Q3) — NO tier filter, unlike skills/[...slug].astro: -->
export function getStaticPaths() {
  return getPlugins()
    .filter(p => p.is_duplicate !== true)
    .map(plugin => ({ params: { slug: plugin.slug }, props: { plugin } }));
}
<!-- Skill template install line to NOT copy (RESEARCH State-of-the-Art): const installCmd = `claude install-skill ...` — does NOT apply to plugins. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plugin detail page — scaffold, install block, ScoreBar/JsonLd</name>
  <files>src/pages/plugins/[...slug].astro</files>
  <read_first>
    - src/pages/skills/[...slug].astro (FULL — clone structure: frontmatter imports, getStaticPaths, ScoreBar inputs, JsonLd shape, BaseLayout usage, CopyButton usage)
    - src/lib/plugins.js (Plan 02 exports, esp. installCommand + getPlugins)
    - src/components/CopyButton.astro (props: which prop carries the command string; PostHog copy_install_command event)
    - src/components/ScoreBar.astro + QualityBadge.astro + JsonLd.astro (prop contracts)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q1 (install flow) + § Q3 (getStaticPaths, field swap)
  </read_first>
  <action>
    Create src/pages/plugins/[...slug].astro by cloning skills/[...slug].astro and adapting:
    - Imports: import { getPlugins, installCommand, resolveBundledSkills, resolveBundledMcps } from '../../lib/plugins.js'. Keep BaseLayout, QualityBadge, CopyButton, SkillCard, ScoreBar, JsonLd.
    - getStaticPaths: the NO-tier-filter version above (filter is_duplicate !== true, map slug→props.plugin).
    - Frontmatter: `const { plugin } = Astro.props; const inst = installCommand(plugin);`
    - INSTALL BLOCK (D-08): one-line explainer ("Plugins install via a marketplace, in two steps."). Then:
      * If inst.hasMarketplace: show "Step 1" label + <CopyButton command={inst.addCmd} /> and (if inst.installCmd) "Step 2" + <CopyButton command={inst.installCmd} />. If inst.installCmd is null (declared name unknown), show the GitHub-fallback note prominently for step 2.
      * If !inst.hasMarketplace (empty listings, the 2,083-plugin GitHub-only case): show the explainer + GitHub link as the primary CTA, no broken install token.
      * ALWAYS render the existing "View on GitHub" link (plugin.repo_url) as the fallback (D-08).
      Use the exact CopyButton prop name verified from the component on read.
    - ScoreBar: pass the same signal inputs as the skill page, substituting manifest_completeness (plugin.extra.manifest_completeness, D-03) where the skill page uses Frontmatter. Reuse daysToRecency helper.
    - QualityBadge: plugin.quality_tier. JsonLd: clone the Article shape, url `https://claudeatlas.com/plugins/<slug>/`, type chip "plugin".
    - Do NOT copy the skill `claude install-skill` line. Do NOT add a worker route.
    Bundles section is the next task — leave a placeholder comment where it goes (after Install).
  </action>
  <verify>
    <automated>npm run build && (find dist/plugins -name index.html | head)</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without error and `dist/plugins/` contains per-plugin index.html dirs
    - dist plugin page count is within ±5% of `getPlugins().filter(p=>p.is_duplicate!==true).length` (spot-check via `find dist/plugins -name index.html | wc -l`)
    - A built plugin page HTML contains `/plugin marketplace add ` for a marketplace plugin (grep a known one) and contains the GitHub repo_url link
    - No `claude install-skill` string in src/pages/plugins/[...slug].astro
  </acceptance_criteria>
  <done>Plugin detail page builds for all non-duplicate plugins with correct two-step install + GitHub fallback + ScoreBar/JsonLd.</done>
</task>

<task type="auto">
  <name>Task 2: Prominent grouped Bundles section (D-09)</name>
  <files>src/pages/plugins/[...slug].astro</files>
  <read_first>
    - src/pages/plugins/[...slug].astro (Task 1 output — the placeholder location)
    - src/components/SkillCard.astro (props it needs to render a bundled skill card)
    - src/pages/category/[category].astro (VISIBLE_LIMIT / show-all cap precedent for the ~12 cap)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q4 (exact bundled_* shapes: which resolve to pages vs chips; "hide empty subgroups")
  </read_first>
  <action>
    Add a full-width "Bundles" section immediately after the Install block (D-09 ordering). Frontmatter:
    `const bundledSkills = resolveBundledSkills(plugin.extra.bundled_skills); const bundledMcps = resolveBundledMcps(plugin.extra.bundled_mcp_servers);`
    Render 5 conditional subgroups, EACH rendered only when non-empty (RESEARCH: all-empty is the norm — hide blanks):
    1. Skills — bundledSkills as <SkillCard> in a grid, capped at ~12 (VISIBLE_LIMIT pattern). If >12, render the rest behind a "Show all N" details/disclosure (clone the category page's pattern; no new JS framework).
    2. MCP servers — bundledMcps as chips linking to /mcp/<slug>/ (these resolve to pages once Plan 04 lands; link anyway — internal-link SEO).
    3. Agents — plugin.extra.bundled_agents (bare name strings) as non-linking chips.
    4. Commands — plugin.extra.bundled_commands as chips.
    5. Hooks — plugin.extra.bundled_hooks as chips.
    If ALL five are empty, omit the entire Bundles section (no empty header). Use existing chip styling (Claude's discretion on exact chip class). Give the section visual weight (full-width, clear heading) — it is the marquee deliverable.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run build completes without error after the Bundles section is added
    - A built plugin page that bundles skills (find one via the loader) contains a SkillCard link to /skills/ inside a Bundles heading; chips render for agents/commands/hooks
    - A built plugin page that bundles nothing has NO Bundles section header in its HTML (grep absent)
    - npm run check:patterns exits 0
  </acceptance_criteria>
  <done>Grouped Bundles section renders all five subgroups conditionally; bundled skills as SkillCards (capped + show-all); empty subgroups and empty section hidden.</done>
</task>

</tasks>

<verification>
- npm run build emits dist/plugins/<slug>/index.html for every non-duplicate plugin
- A marketplace plugin page contains the two-step install; a GitHub-only plugin page shows the fallback CTA
- A bundling plugin page renders the Bundles section with SkillCards; a non-bundling plugin omits it
- npm run check:patterns green
</verification>

<success_criteria>
Plugin detail pages are fully static (no tier filter), render correct two-step install with declared-name + GitHub fallback, and prominently display the grouped bundle graph with bundled skills as linked SkillCards.
</success_criteria>

<output>
After completion, create .planning/phases/3.3-plugin-pages/3.3-03-SUMMARY.md
</output>
