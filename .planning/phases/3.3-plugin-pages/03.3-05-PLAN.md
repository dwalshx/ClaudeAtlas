---
phase: 3.3-plugin-pages
plan: 05
type: execute
wave: 3
depends_on: ["02", "03", "04"]
files_modified:
  - src/pages/plugins/index.astro
  - src/pages/plugins/browse.astro
  - src/pages/mcp/index.astro
  - scripts/check-sitemap-completeness.js
autonomous: true
requirements: [D-04, D-07, D-12, D-13, D-14]
must_haves:
  truths:
    - "/plugins/ is a curated landing (top-N by quality_score) with the live /api/v1/search?type=plugin bar + category/tag chips"
    - "/plugins/browse/ is a flat A-Z anchor list of every non-duplicate plugin"
    - "/mcp/ is a static sorted list of all MCP servers (no A-Z, no search required)"
    - "search result cards on /plugins/ build /plugins/<slug>/ URLs (not the worker's skill-hardcoded detail_url)"
    - "check-sitemap-completeness no longer false-fails after ~3.6k plugin/MCP pages are added"
  artifacts:
    - path: "src/pages/plugins/index.astro"
      provides: "curated plugin landing + plugin-variant search"
      min_lines: 60
    - path: "src/pages/plugins/browse.astro"
      provides: "A-Z plugin browse"
      min_lines: 40
    - path: "src/pages/mcp/index.astro"
      provides: "static MCP list"
      min_lines: 30
    - path: "scripts/check-sitemap-completeness.js"
      provides: "expected-count basis widened to include plugin + MCP pages"
      contains: "plugins.ndjson"
  key_links:
    - from: "src/pages/plugins/index.astro"
      to: "/api/v1/search?type=plugin"
      via: "search fetch + plugin-variant card renderer building /plugins/<slug>/"
      pattern: "type=plugin"
    - from: "scripts/check-sitemap-completeness.js"
      to: "data/plugins.ndjson + data/mcp-servers.ndjson"
      via: "wc -l record counts added to expected basis"
      pattern: "mcp-servers"
---

<objective>
Build the three discovery pages (D-12 hybrid: curated landing + A-Z browse + static MCP
list) and widen the sitemap-completeness postbuild gate so the ~3.6k new pages don't trip it
(RESEARCH Pitfall 4 — the F1 DOD-10 "sitemap silently drops pages" failure class).

- `/plugins/` — clone `index.astro`'s curated-landing + semantic-search pattern; top-N
  plugins by quality_score + the LIVE `/api/v1/search?type=plugin` bar (D-13, no new search
  infra) + category/tag chips (use TAGS `category:<slug>`, not legacy `category` — RESEARCH Q5).
- `/plugins/browse/` — clone `browse.astro`'s A-Z grouping; flat anchor list of every
  non-duplicate plugin (~300-400 KB HTML, far under the 25 MB cap).
- `/mcp/` — a static sorted list (n≈76; no A-Z, search optional per RESEARCH Q5).

Purpose: the "best plugin in 30 seconds" front door + full long-tail SEO distribution, plus a
green postbuild gate.
Output: three index/browse pages + a one-spot edit to check-sitemap-completeness.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@.planning/phases/3.3-plugin-pages/3.3-CONTEXT.md
@src/pages/index.astro
@src/pages/browse.astro
@src/lib/plugins.js

<interfaces>
<!-- index.astro renderResultCard (RESEARCH Q5, lines ~303-352): hardcodes detail_url || /skills/<slug>/ + skill-category colors. The worker (worker/index.js line 616) ALSO sets detail_url:/skills/<slug>/ for ALL results. For /plugins/ search, IGNORE the worker detail_url and build /plugins/<result.slug>/ page-side. -->
<!-- /api/v1/search?type=plugin (worker ~494-504, 578): filters to plugin vectors via metadata.entity_type. LIVE — do NOT modify the worker. -->
<!-- Category source (RESEARCH Q5): use tags `category:<slug>`, NOT the legacy `category` field (removed in 3.6). Reuse CategoryChip component. -->
<!-- check-sitemap-completeness.js (VERIFIED): counts skills via `wc -l data/skills.ndjson` → skillCount; asserts locCount ∈ [skillCount-25, skillCount + max(1500, 15%*skillCount)]. Adding plugin+MCP <loc> entries raises locCount → may exceed upper bound; widen the expected basis. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /plugins/ curated landing + plugin-variant search + /plugins/browse/ A-Z</name>
  <files>src/pages/plugins/index.astro, src/pages/plugins/browse.astro</files>
  <read_first>
    - src/pages/index.astro (FULL — curated top-N rendering, the inline search JS, renderResultCard at ~303-352, category-chip rendering)
    - src/pages/browse.astro (FULL — A-Z grouping + anchor-list pattern + HTML-size caveat)
    - src/lib/plugins.js (getPlugins; sort by quality_score; filter is_duplicate !== true)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q5 (search JS variant, category source = tags, A-Z size)
  </read_first>
  <action>
    Create src/pages/plugins/index.astro (clone index.astro):
    - Data: `const plugins = getPlugins().filter(p => p.is_duplicate !== true)`. Curated top-N (~60) by quality_score desc.
    - Search bar: reuse the inline search JS but point fetch at `/api/v1/search?type=plugin` (D-13). In the result-card renderer, build the detail URL as `/plugins/${result.slug}/` (IGNORE the worker's skill-hardcoded detail_url, RESEARCH Q5). Add a "plugin" type chip on cards. Keep the debounce + empty/loading states from index.astro.
    - Category/tag chips: derive from plugin TAGS (`category:<slug>`) via a plugin-scoped tag count (NOT the legacy `category` field). Reuse CategoryChip. Each chip can link to a tag-filtered view or just be display (match what skills do — Claude's discretion).
    - Reuse BaseLayout + SkillCard (or a plugin variant if a type chip is needed on the static cards).

    Create src/pages/plugins/browse.astro (clone browse.astro verbatim):
    - Swap loader to getPlugins().filter(is_duplicate !== true), group A-Z by name/slug initial, anchor list links to `/plugins/${slug}/`. Keep the size-conscious flat-anchor structure (no SkillCards — anchors only, per the browse.astro pattern).
  </action>
  <verify>
    <automated>npm run build && (test -f dist/plugins/index.html && find dist/plugins/browse -name index.html | head)</automated>
  </verify>
  <acceptance_criteria>
    - dist/plugins/index.html exists and contains the string `type=plugin` (search wired) and `/plugins/` detail links
    - dist/plugins/browse/index.html exists, is < 25 MB, and contains anchor links to `/plugins/<slug>/`
    - The /plugins/ search card renderer builds `/plugins/` URLs (grep src for `/plugins/${` in the renderer) — NOT `/skills/`
    - Category chips derive from tags: src/pages/plugins/index.astro references `category:` tag filtering, NOT `.category` legacy field for chip building
  </acceptance_criteria>
  <done>/plugins/ curated landing with plugin-scoped live search + tag chips, and /plugins/browse/ A-Z, both build.</done>
</task>

<task type="auto">
  <name>Task 2: /mcp/ static index + widen sitemap-completeness gate</name>
  <files>src/pages/mcp/index.astro, scripts/check-sitemap-completeness.js</files>
  <read_first>
    - src/pages/browse.astro OR src/pages/index.astro (whichever is the simpler list pattern for the static n≈76 MCP list)
    - src/lib/plugins.js (getMcpServers)
    - scripts/check-sitemap-completeness.js (FULL — the skillCount basis at lines ~85-110; how wc -l is invoked without readFileSync on data/)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q5 (/mcp/ index) + Pitfall 4 (sitemap gate widening)
  </read_first>
  <action>
    Create src/pages/mcp/index.astro: a static sorted list of getMcpServers() (by quality_score desc), each linking to `/mcp/${slug}/`. n≈76 → plain list, no A-Z, no search bar required (Claude's discretion to add the search bar pointing at `?type=mcp_server`, but not required). Reuse BaseLayout + a simple card/row.

    Widen scripts/check-sitemap-completeness.js so the expected `<loc>` basis includes plugin + MCP pages (RESEARCH Pitfall 4). Following the existing pattern (wc -l on the data file, NEVER readFileSync on data/):
    - Add `const PLUGINS_NDJSON = join(ROOT,'data','plugins.ndjson')` and `MCP_NDJSON = join(ROOT,'data','mcp-servers.ndjson')`.
    - Count records via the SAME `wc -l` mechanism the script already uses for skills (guard with existsSync → 0 if absent, so skills-only branches/CI don't break).
    - Expected basis becomes `skillCount + pluginCount + mcpCount` (note: duplicates are filtered out of getStaticPaths so the page count is slightly lower — keep the existing lower-bound slack of -25 and the max(1500, 15%) tolerance, which absorbs the dedup gap).
    - Update the log line + the FATAL message to reflect the combined basis.
    Do NOT introduce a banned read; reuse the existing wc -l execSync invocation.
  </action>
  <verify>
    <automated>npm run build && npm run postbuild</automated>
  </verify>
  <acceptance_criteria>
    - dist/mcp/index.html exists and links to `/mcp/<slug>/` entries
    - scripts/check-sitemap-completeness.js references both `plugins.ndjson` and `mcp-servers.ndjson` and uses existsSync guards (no readFileSync on data/)
    - `npm run postbuild` exits 0 (sitemap-completeness + asset-sizes both pass with the new pages present)
    - `npm run check:patterns` exits 0 (no banned read added to the gate)
  </acceptance_criteria>
  <done>/mcp/ static list builds; sitemap-completeness gate counts plugin+MCP pages and passes postbuild.</done>
</task>

</tasks>

<verification>
- npm run build emits /plugins/, /plugins/browse/, /mcp/ index pages
- npm run postbuild (sitemap-completeness widened) exits 0 with all four page sets present
- /plugins/ search hits ?type=plugin and links to /plugins/ detail URLs
- npm run check:patterns green
</verification>

<success_criteria>
The hybrid discovery surface is live: curated /plugins/ landing with plugin-scoped live search + tag chips, /plugins/browse/ A-Z, /mcp/ static list — all fully static — and the sitemap-completeness postbuild gate passes with the expanded page count.
</success_criteria>

<output>
After completion, create .planning/phases/3.3-plugin-pages/3.3-05-SUMMARY.md
</output>
