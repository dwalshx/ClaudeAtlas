---
phase: 3.3-plugin-pages
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - src/pages/mcp/[...slug].astro
  - src/pages/skills/[...slug].astro
autonomous: true
requirements: [D-04, D-05, D-07, D-10, D-11]
must_haves:
  truths:
    - "Every MCP server has a static detail page at /mcp/<slug>/ in dist/"
    - "MCP detail swaps PluginExtra commands/hooks for McpExtra transport + tools[] (D-05)"
    - "MCP empty sections (no bundle graph, thin similar corpus) are conditionally hidden"
    - "Skill detail pages render a compact 'Bundled in these plugins' chip row when skill.bundled_in_plugins is non-empty"
  artifacts:
    - path: "src/pages/mcp/[...slug].astro"
      provides: "MCP detail page, getStaticPaths over all MCP records"
      min_lines: 60
      contains: "getStaticPaths"
    - path: "src/pages/skills/[...slug].astro"
      provides: "reverse-edge 'Bundled in' chip row"
      contains: "bundled_in_plugins"
  key_links:
    - from: "src/pages/mcp/[...slug].astro"
      to: "src/lib/plugins.js getMcpServers"
      via: "getStaticPaths"
      pattern: "getMcpServers"
    - from: "src/pages/skills/[...slug].astro"
      to: "src/lib/plugins.js resolveBundledPlugins"
      via: "reverse-edge resolution of skill.bundled_in_plugins"
      pattern: "resolveBundledPlugins"
---

<objective>
Two related deliverables, both consuming Plan 02's loader:

1. **MCP detail page `/mcp/<slug>/` (D-04/D-05/D-07/D-11):** clone the in-tree skill detail
   template (`src/pages/skills/[...slug].astro`) with the McpExtra field swap — `transport` +
   `tools[]` substitute for `commands[]` + `hooks[]`. n≈76, fully static, no bundle graph on
   MCP pages (MCPs are bundled BY plugins but carry no inverse field). Conditionally hide
   empty sections (thin similar corpus, no bundles) per Claude's Discretion.

2. **Skill-page reverse edge (D-10):** add a COMPACT "Bundled in these plugins" chip row to
   `src/pages/skills/[...slug].astro` from `skill.bundled_in_plugins[]` (plugin entity IDs →
   resolveBundledPlugins → /plugins/<slug>/ links). Render ONLY when non-empty (the common
   case is empty). Secondary context, not the headline.

Purpose: complete the MCP page set and close the reverse direction of the bundle graph.
Output: src/pages/mcp/[...slug].astro + an edit to skills/[...slug].astro.

NOTE: This plan clones the SKILL template (already in-tree), NOT the Plan 03 plugin page.
This keeps Plan 04 parallel with Plan 03 in Wave 2 (depends_on ["02"] only) — no need to
wait for Plan 03's plugin detail page to exist. The skill template carries every helper the
MCP page needs (Install/CTA block, ScoreBar, similar-entity infra, JsonLd); the MCP page is
~90% the skill template per D-05/D-11 with the transport/tools field swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@.planning/phases/3.3-plugin-pages/3.3-CONTEXT.md
@src/pages/skills/[...slug].astro
@src/lib/plugins.js

<interfaces>
<!-- McpExtra (CLAUDE.md / types.d.ts): server_path, manifest, readme_markdown, tools[], transport ('stdio'|'sse'|'streamable-http'|null), manifest_completeness -->
<!-- src/lib/plugins.js (Plan 02): getMcpServers(), getMcpBySlug(slug), resolveBundledPlugins(ids) -->
<!-- MCP records are NOT enriched (D-10): is_duplicate always false; the getStaticPaths is_duplicate filter is a harmless no-op. -->
<!-- skill.bundled_in_plugins[] = plugin entity IDs "plugin:<repo>/.claude-plugin/plugin.json" (RESEARCH Q4). -->
<!-- getStaticPaths (MCP, no tier filter): getMcpServers().map(mcp => ({ params:{slug:mcp.slug}, props:{mcp} })) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP detail page with field swap + empty-section hiding</name>
  <files>src/pages/mcp/[...slug].astro</files>
  <read_first>
    - src/pages/skills/[...slug].astro (the in-tree template to clone; MCP reuses ~90% of it per D-05/D-11 — Install/CTA block, ScoreBar, similar-entity infra, JsonLd, getStaticPaths shape). Cloning the skill template (not the Plan 03 plugin page) keeps this plan parallel with Plan 03 in Wave 2.
    - src/lib/types.d.ts (McpExtra exact fields: transport, tools[], server_path, manifest_completeness)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q3 (MCP field swap) + § Q5 (/mcp/ index note; n≈76)
  </read_first>
  <action>
    Create src/pages/mcp/[...slug].astro by cloning src/pages/skills/[...slug].astro and adapting:
    - Imports: getMcpServers from '../../lib/plugins.js'. Keep BaseLayout, QualityBadge, ScoreBar, JsonLd.
    - getStaticPaths: `getMcpServers().filter(m => m.is_duplicate !== true).map(mcp => ({ params:{slug:mcp.slug}, props:{mcp} }))` (filter is the harmless no-op above).
    - Frontmatter: `const { mcp } = Astro.props;`
    - FIELD SWAP (D-05): where the skill page shows skill-specific body sections, show mcp.extra.tools[] (as a list/chips) and mcp.extra.transport (a single labeled value, e.g. "Transport: stdio"; if null, hide the label).
    - NO install marketplace block (MCPs don't install via /plugin install). Show the "View on GitHub" link as the primary CTA. (If MCP install guidance exists in McpExtra.manifest, render it; otherwise GitHub-only.)
    - NO Bundles section (MCPs carry no inverse bundle field — D-05).
    - Conditionally HIDE: tools[] section if empty; similar-MCP section if thin/absent (n≈76 → likely empty); transport label if null. Do NOT render blank headers (Claude's Discretion).
    - ScoreBar: use mcp.extra.manifest_completeness for the manifest signal. QualityBadge: mcp.quality_tier. JsonLd: url `https://claudeatlas.com/mcp/<slug>/`, type "mcp_server".
  </action>
  <verify>
    <automated>npm run build && (find dist/mcp -name index.html | wc -l)</automated>
  </verify>
  <acceptance_criteria>
    - npm run build completes without error and dist/mcp/ contains ~76 per-MCP index.html dirs (count within ±5% of getMcpServers().length)
    - A built MCP page contains a "Transport" label for an MCP whose transport is non-null, and lists tools for one with tools[]
    - No "/plugin install" string and no "Bundles" header in any dist/mcp/*.html
    - No empty-section blank headers (grep a no-tools MCP page: tools heading absent)
  </acceptance_criteria>
  <done>MCP detail pages build for all records with transport/tools field swap and conditional empty-section hiding, cloned from the in-tree skill template.</done>
</task>

<task type="auto">
  <name>Task 2: Reverse-edge 'Bundled in' chip row on skill pages (D-10)</name>
  <files>src/pages/skills/[...slug].astro</files>
  <read_first>
    - src/pages/skills/[...slug].astro (the file being edited — find a clean insertion point, e.g. after the install/score block, before similar-skills)
    - src/lib/plugins.js (resolveBundledPlugins export from Plan 02)
    - .planning/phases/3.3-plugin-pages/3.3-RESEARCH.md § Q4 (skill.bundled_in_plugins shape, reverse resolution) + CLAUDE.md footgun #11
  </read_first>
  <action>
    Edit src/pages/skills/[...slug].astro:
    - Add import: `import { resolveBundledPlugins } from '../../lib/plugins.js';`
    - In frontmatter: `const bundledInPlugins = resolveBundledPlugins(skill.bundled_in_plugins);`
    - Render a COMPACT chip row titled e.g. "Bundled in these plugins" ONLY when `bundledInPlugins.length > 0`. Each chip links to `/plugins/${p.slug}/` showing p.name. Use existing chip styling. Keep it secondary (small, below the main content) — NOT a headline section.
    - Guard: this is the ONLY change to this file in this task; do not disturb getStaticPaths or existing skill rendering. Importing plugins.js into the skill page is build-time only (the loader's plugin/mcp reads are graceful-empty if files absent on a CI branch).
    NOTE: skill.bundled_in_plugins is preserved across filter re-runs via PRESERVED_FIELDS (footgun #11) — do not touch filter.js here; just consume the field.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run build completes without error after the reverse-edge edit
    - src/pages/skills/[...slug].astro contains resolveBundledPlugins and bundled_in_plugins
    - A skill page whose bundled_in_plugins is non-empty (locate one via the loader) renders a chip linking to /plugins/<slug>/; a skill with empty bundled_in_plugins renders NO Bundled-in header
    - npm run check:patterns exits 0 ; existing skill getStaticPaths unchanged (tier filter still present)
  </acceptance_criteria>
  <done>Skill detail pages show a compact reverse-edge chip row when bundled_in_plugins is non-empty, hidden otherwise; no regression to skill rendering.</done>
</task>

</tasks>

<verification>
- npm run build emits dist/mcp/<slug>/index.html for every MCP record and rebuilds skill pages with the reverse edge
- An MCP page shows transport/tools; no install/Bundles section
- A bundled skill page shows the compact Bundled-in chip row linking to /plugins/
- npm run check:patterns green
</verification>

<success_criteria>
MCP detail pages are fully static with the transport/tools field swap and conditional empty-section hiding (cloned from the in-tree skill template); skill pages render the compact reverse bundle edge when present. Both directions of the bundle graph are now rendered (forward on plugins from Plan 03, reverse on skills here).
</success_criteria>

<output>
After completion, create .planning/phases/3.3-plugin-pages/3.3-04-SUMMARY.md
</output>
</content>
</invoke>
