---
phase: 3.3-plugin-pages
plan: 07
type: execute
wave: 4
depends_on: ["05", "06"]
files_modified:
  - .github/workflows/daily-scrape.yml
autonomous: false
requirements: [D-01, D-02]
must_haves:
  truths:
    - "plugins-raw-ndjson-bootstrap release exists with data/plugins-raw.ndjson as a permanent asset"
    - "The cold/warm plugin-discovery sweep wall-clock is MEASURED on a branch dispatch BEFORE the daily flag is flipped"
    - "PLUGINS_ENABLED is flipped to 'true' on main ONLY after the measured run + existing ~4.5-5.5h fits under the 360-min cap"
    - "The first warm scheduled run post-flip stays green AND commits data/history/<today>.json"
  artifacts:
    - path: ".github/workflows/daily-scrape.yml"
      provides: "PLUGINS_ENABLED flipped true after validation"
      contains: "PLUGINS_ENABLED"
  key_links:
    - from: "branch dispatch of daily-scrape.yml (PLUGINS_ENABLED=true)"
      to: "Run plugin discovery step duration"
      via: "Actions log read — the timing gate"
      pattern: "plugin discovery"
---

<objective>
The human-gated re-enable (D-01) — mirrors the 3.2.1 phase-gate validation discipline. The
user's fine-grained PAT cannot dispatch workflows via CLI and wrangler can't run on the
win32-arm64 box, so release creation, workflow dispatch, branch measurement, the flag flip,
and the first-warm-cron observation are all CHECKPOINTS the user performs via the GitHub UI.

Sequence (D-01, RESEARCH Q2d):
1. Create the `plugins-raw-ndjson-bootstrap` release from local `data/plugins-raw.ndjson` (25.8 MB).
2. Dispatch `bootstrap-plugins-raw.yml` (Plan 06) via the GitHub UI to warm the GHA cache.
3. Dispatch a BRANCH run of `daily-scrape.yml` with `PLUGINS_ENABLED='true'` (branch dispatches
   are publish/deploy-gated to main, so this exercises scrape→filter→embed safely) and READ the
   "Run plugin discovery" step duration.
4. Flip `PLUGINS_ENABLED='false' → 'true'` on main (line 77) ONLY if measured + existing run fits
   under 360 min with margin.
5. Observe the first warm scheduled run: green + fresh `data/history/<today>.json` commit.

Purpose: turn the plugin pipeline back on WITHOUT risking the irreplaceable daily history
snapshot (Pitfall 5 — a run that dies past 360 min loses that day's snapshot, the exact reason
the pipeline was gated off).
Output: the flag flip commit on daily-scrape.yml + a recorded measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/3.3-plugin-pages/3.3-RESEARCH.md
@.planning/phases/3.3-plugin-pages/3.3-CONTEXT.md
@.github/workflows/daily-scrape.yml

<interfaces>
<!-- daily-scrape.yml line 77: env: PLUGINS_ENABLED: 'false'  → flip to 'true' (the single operative change). -->
<!-- The 9 gated plugin/MCP steps already exist and AND-combine PLUGINS_ENABLED=='true' with github.event_name != 'push'. No structural YAML change beyond the flag (RESEARCH Q2c). -->
<!-- RESEARCH Q2d: sweep is ≤240 search calls (~27 min throttle floor) + per-repo walk for ~7,300 repos; D-03 confirms sweep+walk is the long-pole. Warm full runs were 4.5-5.5h pre-3.2.1; HNSW reclaimed build headroom (~50 min vs 360 cap). -->
<!-- SESSION-HANDOFF gotcha: PAT can't `gh workflow run`; use GitHub UI. wrangler can't run on win32-arm64; deploys CI-only. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create plugins-raw bootstrap release + warm the GHA cache</name>
  <what-built>The bootstrap workflow (Plan 06) and all plugin/MCP pages (Plans 02-05) are merged and building. The plugins-raw GHA-cache seed needs a release asset to download from — only the user can create the release (the local 25.8 MB file isn't committed).</what-built>
  <how-to-verify>
    1. Create the `plugins-raw-ndjson-bootstrap` GitHub release with the local `data/plugins-raw.ndjson` (25.8 MB, dated 2026-05-30) attached as an asset. Via the GitHub UI: Releases → Draft new release → tag `plugins-raw-ndjson-bootstrap` → attach `data/plugins-raw.ndjson` → Publish. KEEP this release permanently (disaster-recovery, like skills-raw-ndjson-bootstrap).
    2. Dispatch `bootstrap-plugins-raw.yml` from the Actions tab (workflow_dispatch). Confirm it succeeds and saved a cache key starting `plugins-raw-ndjson-bootstrap-`.
    3. Verify: `gh cache list --limit 5` shows a `plugins-raw-ndjson-bootstrap-*` entry.
  </how-to-verify>
  <resume-signal>Reply "release + cache ready" (or paste any error from the bootstrap run).</resume-signal>
  <action>PAUSE. Present the release-creation + bootstrap-dispatch steps to the user (GitHub UI — PAT cannot dispatch via CLI). Do not proceed until the user confirms the release exists and the bootstrap run saved a plugins-raw-ndjson-bootstrap-* cache.</action>
  <verify>User reply confirms the plugins-raw-ndjson-bootstrap release exists and `gh cache list` shows a plugins-raw-ndjson-bootstrap-* entry.</verify>
  <done>plugins-raw-ndjson-bootstrap release created (permanent) and GHA cache warmed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Measure the plugin-discovery sweep on a safe branch run</name>
  <what-built>The GHA cache is warm. Before flipping the daily flag on main, the plugin-discovery sweep wall-clock MUST be measured on a safe branch run (Pitfall 5: a run that overruns 360 min on main loses that day's history snapshot).</what-built>
  <how-to-verify>
    1. On a feature branch, temporarily set `PLUGINS_ENABLED: 'true'` (daily-scrape.yml line 77). Push the branch.
    2. Dispatch `daily-scrape.yml` on that BRANCH via the Actions tab (branch dispatches are publish/deploy/upload-gated to main — safe; they exercise scrape→filter→embed without touching prod or KV).
    3. In the run log, read the "Run plugin discovery (scrape-plugins)" step duration AND the total job duration. Record both.
    4. Confirm the total job duration (plugin steps + existing skills+build) is comfortably under 360 min (the HNSW optimization reclaimed ~5h of build headroom; the sweep should now fit — but VALIDATE, don't assume).
    5. Confirm the "Filter MCP servers" step produced MCP records (RESEARCH Q2c — verify scrape-plugins emits the mcp-servers/ entries filter-mcps expects). Confirm no banned-pattern failure now that the gated plugin path is live (the Plan 01 loadCheckpoint fix should keep check:patterns green).
  </how-to-verify>
  <resume-signal>Reply with the measured plugin-discovery duration + total job duration, and whether it fits under 360 min. If it does NOT fit, STOP — do not flip the flag; we replan the sweep (defer to 3.7 incremental rework).</resume-signal>
  <action>PAUSE. Walk the user through the branch dispatch (set PLUGINS_ENABLED=true on a feature branch, dispatch daily-scrape.yml on that branch via the GitHub UI). Do not flip the main flag until the user reports the measured durations and confirms the run fits under 360 min.</action>
  <verify>User reply provides the plugin-discovery step duration + total job duration and a fits-under-360-min determination; the Filter-MCP-servers step produced records; check:patterns stayed green on the live gated path.</verify>
  <done>Sweep wall-clock measured on a safe branch; go/no-go decision recorded before any main change.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Flip PLUGINS_ENABLED on main + observe first warm scheduled run</name>
  <what-built>The branch measurement confirmed the plugin pipeline fits the daily budget. Now flip the flag on main and observe the first warm scheduled run.</what-built>
  <how-to-verify>
    1. On main (via a normal PR/commit), change daily-scrape.yml line 77 `PLUGINS_ENABLED: 'false'` → `'true'`. This is the single operative change (the 9 gated steps activate automatically).
    2. After the next scheduled 06:30 UTC run (or a manual main dispatch), verify: `gh run list --workflow=daily-scrape.yml --limit=1` shows green, the run completed under 360 min, and a fresh `data/history/<today>.json` was committed (the snapshot contract held).
    3. Confirm the deployed site serves fresh plugin data: the /plugins/ pages reflect the new plugins.ndjson (spot-check a known plugin slug resolves).
  </how-to-verify>
  <resume-signal>Reply "flag flipped, first warm run green + history committed" (or paste the failing run). Phase 3.3 is complete once this lands.</resume-signal>
  <action>PAUSE. Have the user flip daily-scrape.yml line 77 PLUGINS_ENABLED false→true on main and observe the next scheduled run. Phase 3.3 completes only when that run is green with the history snapshot committed.</action>
  <verify>`gh run list --workflow=daily-scrape.yml --limit=1` shows green under 360 min AND data/history/<today>.json was committed; /plugins/ pages reflect fresh plugins.ndjson.</verify>
  <done>PLUGINS_ENABLED=true on main; first warm scheduled run green; daily history snapshot intact; fresh plugin/MCP data live.</done>
</task>

</tasks>

<verification>
- plugins-raw-ndjson-bootstrap release exists with the asset; bootstrap workflow saved a warm cache
- Branch-measured plugin-discovery + total job duration recorded; fits under 360 min before any main flag change
- PLUGINS_ENABLED='true' on main; first warm scheduled run green + data/history/<today>.json committed
</verification>

<success_criteria>
The plugin pipeline is re-enabled safely: cache seeded from a permanent release, sweep measured on a branch before the main flip, flag flipped only after the budget check, and the first warm run confirmed green with the daily history snapshot intact. Fresh plugin/MCP data flows to the live /plugins/ and /mcp/ pages.
</success_criteria>

<output>
After completion, create .planning/phases/3.3-plugin-pages/3.3-07-SUMMARY.md
</output>
