---
phase: 3.3-plugin-pages
plan: 04
subsystem: ui
tags: [astro, mcp, static-pages, bundle-graph, getStaticPaths]

# Dependency graph
requires:
  - phase: 3.3-plugin-pages plan 02
    provides: src/lib/plugins.js loader (getMcpServers, resolveBundledPlugins)
  - phase: 3.2-plugin-and-mcp-scoring
    provides: data/mcp-servers.ndjson (McpExtra records) + skill.bundled_in_plugins reverse edge (link-bundles.js)
provides:
  - /mcp/<slug>/ static detail page for every MCP server record (76 pages, fully static, no tier filter)
  - McpExtra field swap on detail pages — transport label + tools[] chips replace commands/hooks (D-05)
  - Reverse bundle edge on skill pages — compact "Bundled in these plugins" chip row from skill.bundled_in_plugins (D-10)
affects: [3.3-plugin-pages plan 05 (mcp index + sitemap gate), 3.5 homepage redesign, verifier]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Fully-static getStaticPaths for sub-cap entity types (no STATIC_TIERS filter; is_duplicate filter kept as no-op parity)"
    - "Conditional section rendering — never emit a section header when its data is empty"
    - "Keep UI strings out of Astro template HTML comments (comments survive into built output and pollute grep verification)"

key-files:
  created:
    - src/pages/mcp/[...slug].astro
  modified:
    - src/pages/skills/[...slug].astro

key-decisions:
  - "MCP page omits Similar/Related/Integrates sections entirely — no MCP similarity corpus exists (compute-similar is skills-only) and the skill-keyed helpers (getSimilarSkills/getSkillIntegrations) would mismatch on slug collisions"
  - "MCP author links to github.com/<owner>, not /creators/<owner>/ — creator pages are generated from skill owners only; MCP-only owners would 404"
  - "JsonLd keeps schema.org Article type (valid vocab); mcp_server entity type carried via keywords instead of an invalid @type"
  - "MCP breadcrumb links to /mcp/ — the index ships in Plan 05 within this same phase"

patterns-established:
  - "Entity detail clone pattern: skill template minus install-command, minus tier filter, plus extra.* field swap"

requirements-completed: [D-04, D-05, D-07, D-10, D-11]

# Metrics
duration: 57min
completed: 2026-06-12
---

# Phase 3.3 Plan 04: MCP Detail Pages + Skill Reverse Bundle Edge Summary

**76 fully-static /mcp/<slug>/ detail pages with transport + tools[] field swap (no install block, no Bundles section), plus the D-10 reverse bundle edge — a compact "Bundled in these plugins" chip row on skill pages linking /plugins/<slug>/**

## Performance

- **Duration:** 57 min
- **Started:** 2026-06-12T15:29:58Z
- **Completed:** 2026-06-12T16:27:16Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 edited)

## Accomplishments

- `/mcp/<slug>/` detail page cloned from the in-tree skill template (D-11): GitHub-only CTA (no `/plugin install` anywhere), Server Interface section rendering `extra.transport` (prettified labels: stdio / SSE / Streamable HTTP) + `extra.tools[]` chips, ScoreBar with the Manifest signal from `extra.manifest_completeness`, JsonLd at `https://claudeatlas.com/mcp/<slug>/`.
- All empty sections conditionally hidden — Server Interface, README, transport label, tools list each render only when data exists; no blank headers possible by construction.
- Skill pages now render the reverse bundle edge: `resolveBundledPlugins(skill.bundled_in_plugins)` → compact chip row (Integrates-with styling) before Similar Skills, only when non-empty. 7,258 of 23,047 skills carry a non-empty reverse edge.
- Both directions of the bundle graph are now rendered (forward on plugin pages from Plan 03, reverse on skills here).

## Task Commits

Each task was committed atomically (--no-verify per parallel-executor protocol):

1. **Task 1: MCP detail page with field swap + empty-section hiding** - `e004135` (feat)
2. **Task 2: Reverse-edge 'Bundled in' chip row on skill pages** - `d1aa4c8` (feat)

## Files Created/Modified

- `src/pages/mcp/[...slug].astro` - MCP detail page; getStaticPaths over all `getMcpServers()` records (fully static, D-07), McpExtra field swap (D-05)
- `src/pages/skills/[...slug].astro` - +26 lines: `resolveBundledPlugins` import, frontmatter resolution, conditional chip-row section; getStaticPaths/STATIC_TIERS untouched

## Verification Evidence

- `find dist/mcp -name index.html | wc -l` → **76** (= getMcpServers().length exactly, 0% deviation)
- Sample MCP page contains `Transport` label + `Tools (N)` heading with chips
- `grep -rl "/plugin install" dist/mcp/` → 0 files; `grep -rl ">Bundles<" dist/mcp/` → 0 files
- Bundled skill (`microsoft/skills/azure-aigateway`, featured) renders "Bundled in these plugins" with `href="/plugins/microsoft/skills/"`; unbundled skill (`K-Dense-AI/scientific-agent-skills/research-lookup`) emits **zero** occurrences of the section string
- `npm run check:patterns` → clean (0 baselined, 0 new)
- Astro build: 15,204 pages in ~183s, `Complete!`

## Decisions Made

- MCP page drops Similar/Related/Integrates sections (skill-keyed helpers, no MCP similarity data at n=76) — per plan's "conditionally hide thin/absent" discretion.
- MCP author → GitHub profile link (avoids broken `/creators/` internal links for MCP-only owners).
- JsonLd `@type` stays `Article` (plan's literal `type "mcp_server"` is not valid schema.org vocabulary); `mcp_server` carried in `keywords`.
- All current 76 MCP records have non-null transport and non-empty tools, so the no-tools acceptance grep has no real-data subject; conditional hiding verified by construction (`{(transport || tools.length > 0) && ...}` guard).

## Deviations from Plan

None functional - plan executed as written. One presentation fix during Task 2: the section's explanatory HTML comment initially contained the literal UI string "Bundled in these plugins"; Astro preserves template HTML comments into built output, so every skill page (including unbundled ones) matched a naive grep. Reworded the comment so the string appears in built HTML only when the section truly renders.

## Issues Encountered

1. **Postbuild sitemap-completeness gate FATAL (pre-assigned to Plan 05, not fixed here).** `<loc>` count 29,032 exceeds the [23,023, 26,505] ceiling — Plan 03's 3,584 plugin pages (built from this shared working tree) account for the overage; this plan's 76 MCP pages alone would land at 25,448 (inside range). `scripts/check-sitemap-completeness.js` is in Plan 05's `files_modified` (RESEARCH Pitfall 4), so it was deliberately not touched. Measured numbers logged in `deferred-items.md` for the Plan 05 handoff. The Astro build itself completes cleanly.
2. **Concurrent-build collision with the parallel Plan 03 executor.** Both executors share this working directory; simultaneous `npm run build` runs crashed one build with `ENOTEMPTY: rmdir 'public/badge'`. Resolved by waiting for the competing node process to exit and retrying (clean on retry). Flagged in `deferred-items.md` for the orchestrator (worktree isolation or build serialization for build-heavy parallel plans).

## Known Stubs

- `/mcp/` breadcrumb link targets the MCP index page that ships in **Plan 05** (wave 3, same phase). Intentional forward link — the phase ships as a whole; Plan 05's `src/pages/mcp/index.astro` resolves it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- MCP detail pages + skill reverse edge ready; Plan 05 (landing/browse/index pages) can link into `/mcp/<slug>/` and must widen the sitemap gate (measured basis in `deferred-items.md`).
- Generated build artifacts (`public/feed/*`, `public/llms.txt`, `data/api-graph.json`, `data/similar-skills.json`, `.claude-plugin/marketplace.json`) were modified by local builds and left unstaged — the daily cron owns them; the orchestrator/verifier should not attribute them to this plan.

---
*Phase: 3.3-plugin-pages*
*Completed: 2026-06-12*

## Self-Check: PASSED

- src/pages/mcp/[...slug].astro exists
- src/pages/skills/[...slug].astro contains resolveBundledPlugins + bundled_in_plugins
- Commits e004135 (Task 1) and d1aa4c8 (Task 2) present in git log
