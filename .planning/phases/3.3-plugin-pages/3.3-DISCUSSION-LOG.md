# Phase 3.3: Plugin Pages - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 3.3-plugin-pages
**Mode:** Advisor (4 parallel research agents, calibration tier: standard / vendor philosophy "pragmatic")
**Areas discussed:** Pipeline re-enable, Entity scope, Detail page content, Index/discovery shape

---

## Pipeline re-enable & data freshness

| Option | Description | Selected |
|--------|-------------|----------|
| Bootstrap-first, then pages | One-time workflow_dispatch plugins-raw bootstrap, validate sweep fits daily budget, flip PLUGINS_ENABLED=true, fix loadCheckpoint footgun; deep rework to 3.7 | ✓ |
| Pages on stale data, re-enable later | Build against 12-day-old plugins-raw.ndjson, flip flag as follow-up | |
| Fold full 3.7 rework into 3.3 | Complete incremental-discovery + bootstrap + flag now | |

**User's choice:** Bootstrap-first, then pages
**Notes:** Research flagged the real timeout culprit is the discovery SWEEP (not the per-repo walk, which already has processedSet skip), so a content-sha skip buys little for 3.3 — confirmed deferring the deep rework to 3.7. `loadCheckpoint()` banned-pattern fix folded in.

---

## Entity scope

| Option | Description | Selected |
|--------|-------------|----------|
| Plugins + MCP pages | /plugins/ + /mcp/ ; marketplace listings as chips | ✓ |
| Plugins only | Just /plugins/; MCP homeless | |
| Plugins + MCP + marketplaces | Adds /marketplaces/ landing pages (net-new inversion artifact) | |

**User's choice:** Plugins + MCP pages
**Notes:** MCP is 38 records reusing the same templates (field swap), closing a gap no later phase reopens. Both fully static (under Cloudflare caps). Marketplace landing pages declined — marketplaces aren't entities (D-01); listings shown as chips instead.

---

## Detail page content (install UX + bundle graph)

| Option | Description | Selected |
|--------|-------------|----------|
| Two-step install + bundle section | Explainer + marketplace-add + install@marketplace + GitHub fallback; prominent grouped Bundles section + compact reverse chips on skill pages | ✓ |
| Minimal (mirror skill page) | Single copy command + compact bundle chips | |

**User's choice:** Two-step install + bundle section
**Notes:** Install flow VERIFIED two-step against current Claude Code docs (code.claude.com/docs/en/discover-plugins). The `@marketplace` is the declared marketplace name, not owner/repo — marketplace-name resolution is Claude's discretion with GitHub fallback for ambiguity. Bundle graph is the headline deliverable → prominent on plugin pages, compact reverse-edge on skill pages.

---

## Index/discovery shape

| Option | Description | Selected |
|--------|-------------|----------|
| Hybrid landing + A-Z browse | Curated /plugins/ + /plugins/browse/ A-Z | ✓ |
| Curated landing only | Top-N only; thin SEO | |
| Full A-Z browse only | Flat list; weak front door | |

**User's choice:** Hybrid landing + A-Z browse
**Notes:** Reproduces the proven homepage→/browse split. At ~4,500 plugins the A-Z HTML is ~300-400KB (a tenth of the 35k-skill caveat). Leans on the live /api/v1/search?type=plugin. Survives the 3.5 homepage redesign untouched.

## Claude's Discretion

- MCP empty-section handling at n=38 (conditional hide)
- Marketplace-name resolution for install command (+ GitHub fallback)
- Exact card caps, chip styling, section ordering
- Plugin category source (tags vs legacy category field)
- getStaticPaths / sitemap wiring for new static page sets

## Deferred Ideas

- Marketplace landing pages (/marketplaces/) — needs new inversion artifact + metadata model
- Deep incremental plugin discovery (content-sha skip) → Phase 3.7
- Bundle graph as scoring signal → Phase 3.4+
- Homepage Top Skills/Top Plugins integration → Phase 3.5
