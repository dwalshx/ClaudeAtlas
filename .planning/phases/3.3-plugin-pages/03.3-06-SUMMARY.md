---
phase: 3.3-plugin-pages
plan: 06
subsystem: infra
tags: [github-actions, gha-cache, bootstrap, plugins, ndjson]

# Dependency graph
requires:
  - phase: 3.2-plugin-and-mcp-scoring
    provides: "daily-scrape.yml Restore plugins-raw cache step (restore-keys: plugins-raw-ndjson-, gated behind PLUGINS_ENABLED)"
  - phase: 3.0.1-pipeline-state-persistence
    provides: "bootstrap-skills-raw.yml — the proven release-asset → GHA-cache seed pattern this mirrors"
provides:
  - ".github/workflows/bootstrap-plugins-raw.yml — one-shot workflow_dispatch that seeds the plugins-raw GHA cache from the plugins-raw-ndjson-bootstrap release asset"
affects: [3.3-plugin-pages plan 07 (human-gated dispatch + flag flip), daily-scrape plugin pipeline re-enable]

# Tech tracking
tech-stack:
  added: []
  patterns: ["release-asset → actions/cache/save bootstrap with prefix-matched restore-keys (third instance: etag, skills-raw, plugins-raw)"]

key-files:
  created: [.github/workflows/bootstrap-plugins-raw.yml]
  modified: []

key-decisions:
  - "Cache key prefix locked to plugins-raw-ndjson- to match daily-scrape.yml's existing restore-keys (no consumer changes needed)"
  - "Workflow authored autonomously; dispatch + release creation + PLUGINS_ENABLED flip stay human-gated in Plan 07 (measure-before-flip discipline)"

patterns-established:
  - "Bootstrap workflows carry an adapted header comment block documenting the restore-key prefix contract and the keep-the-release-permanently rule"

requirements-completed: [D-01]

# Metrics
duration: 4min
completed: 2026-06-12
---

# Phase 3.3 Plan 06: Bootstrap plugins-raw cache workflow Summary

**One-shot bootstrap-plugins-raw.yml workflow seeding the GHA plugins-raw cache from the plugins-raw-ndjson-bootstrap release, key-prefix-matched to daily-scrape.yml's existing restore step**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-12T15:14:27Z
- **Completed:** 2026-06-12T15:18:30Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Authored `.github/workflows/bootstrap-plugins-raw.yml` as a faithful mirror of the proven `bootstrap-skills-raw.yml` pattern: `workflow_dispatch` → checkout → `gh release download plugins-raw-ndjson-bootstrap --pattern 'plugins-raw.ndjson'` → `ls -lh` + `sha256sum` + `wc -l` sanity output → `actions/cache/save@v4` with key `plugins-raw-ndjson-bootstrap-${{ github.run_number }}`.
- Cache key prefix `plugins-raw-ndjson-` exactly matches daily-scrape.yml's existing `Restore plugins-raw cache` step (`restore-keys: plugins-raw-ndjson-`, lines 202-210, gated behind `PLUGINS_ENABLED`), so the contract is wired with zero consumer changes.
- Header comment block adapted for plugins: documents the restore-key prefix contract, the keep-the-release-permanently disaster-recovery rule, and re-dispatch-on-eviction semantics; trailing confirm step points the operator at the Plan 07 measure-before-flip protocol.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author bootstrap-plugins-raw.yml mirroring the skills bootstrap** - `3870b4d` (feat)

## Files Created/Modified

- `.github/workflows/bootstrap-plugins-raw.yml` - One-shot workflow_dispatch seeding the plugins-raw GHA cache from the `plugins-raw-ndjson-bootstrap` release asset

## Decisions Made

- Kept the prefix-discipline comment style from the skills bootstrap but corrected the consumer list — only daily-scrape.yml consumes `plugins-raw-ndjson-` (there is no weekly-discover consumer for plugins, unlike skills-raw).
- Confirm-step "Next steps" reference Plan 3.3-07's measure-before-flip protocol instead of suggesting an immediate `gh workflow run daily-scrape.yml`, since `PLUGINS_ENABLED` is still false and the flag flip is human-gated.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- Plan's automated check passes: file contains `workflow_dispatch`, `plugins-raw-ndjson-bootstrap-`, `plugins-raw.ndjson`, `actions/cache/save` (node verify exits 0).
- `gh release download plugins-raw-ndjson-bootstrap` and `actions/cache/save@v4` present; cache key is `plugins-raw-ndjson-bootstrap-${{ github.run_number }}` under the required `plugins-raw-ndjson-` prefix.
- `npm run check:patterns` clean (0 baselined, 0 new) — no banned patterns introduced in the workflow.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required. (Plan 07 covers the human-gated release creation + workflow dispatch.)

## Next Phase Readiness

- The bootstrap workflow is authored and ready to dispatch. Plan 07 (Wave 4, human-gated) can now: create the `plugins-raw-ndjson-bootstrap` release from the local `data/plugins-raw.ndjson` (25.8 MB, dated 2026-05-30), dispatch this workflow from the Actions tab (PAT can't dispatch via CLI), measure the cold sweep, then flip `PLUGINS_ENABLED=true`.
- No blockers.

---
*Phase: 3.3-plugin-pages*
*Completed: 2026-06-12*

## Self-Check: PASSED

- .github/workflows/bootstrap-plugins-raw.yml exists on disk
- Task commit 3870b4d present in git log
- Plan automated verify exits 0; check:patterns clean
