# Phase 3.0.0 - Plan Check

**Verifier:** gsd-plan-checker (goal-backward)
**Date:** 2026-04-25
**Plan under review:** .planning/phases/03.0.0-split-track-scrape/3.0.0-PLAN.md
**Goal:** Daily GHA scrape always completes <30 min, produces healthy data/history/<today>.json + updated skills.json, on free-tier GHA, without breaking existing schemas.

---

## Verdict: NEEDS_REVISION

One BLOCK (R6 / skills-raw.json size - actual file is 308 MB on disk, not 8 MB as plan and ARCHITECTURE.md claim - committing weekly is unworkable as written) plus several FLAGs the planner should resolve before execution. The architecture is sound; the failures are around two specific snippets that do not compose with the real code, plus a sizing error in R6.

---

## Per-Concern Verdict Table

| # | Concern | Verdict | Evidence |
|---|---------|---------|----------|
| 1 | R3 merge logic matches actual filter.js shape | FLAG | filter.js is NOT a per-skill loop with currentSkillsBySlug - it is a series of filter / map / sort passes over an array. Plan Step 0/0b inserts a NEW pre-pass that mutates raw[] before the existing filter chain, which works but contradicts CONTEXT.md line 138. The Step 0/0b implementation in the action block is correct; the legacy CONTEXT.md snippet is misleading. Also: filter.js keys preserved enrichments by id, but the new merge keys by slug - both valid, but the inconsistency should be noted in a code comment. |
| 2 | Track 2 incremental composes with size-range partitioning | FLAG | Plan correctly applies pushedFilter to BOTH searchCodeBySize (line 263) and searchByTopics (line 314). However: searchByTopics issues q=topic:TOPIC - appending pushed:>YYYY-MM-DD is valid GitHub syntax but semantically filters topic-tagged repos by recent push activity. Acceptable for the incremental intent but worth a one-line comment clarifying the semantic. |
| 3 | ETag cache extraction is behavior-preserving | PASS (with note) | Task 1 instructions match scrape.js line numbers exactly (rate limit 37-116, etag 118-199, saveETagCache 196-199). Two callers at 627 and 665 - plan correctly notes both. ONE GAP: Task 1 verify only runs node --check + import-keys check - proves syntax, not behavior. Recommend adding a brief end-to-end smoke run against a tiny seed list. |
| 4 | First post-deploy run fits in <30 min | PASS | Math: 826 unique repos x 1 GET each. Bootstrap cache was seeded by the OLD scrape that DID touch every repo currently in skills.json. 304 responses are cheap. Track 1 warm: <5 min realistic. Track 2 incremental with pushed greater-than-3d after 7+ days dark: bounded by 10 search-req/min x ~30 queries = 5-15 min. Total 25-35 min, fits 90-min step timeout. |
| 5 | Un-gitignoring skills-raw.json is sustainable | BLOCK | Actual data/skills-raw.json on disk is 308 MB, not 8 MB as plan and ARCHITECTURE.md state. Verified by ls -la: 308,373,942 bytes - likely because scrape.js truncates body_markdown to 5000 chars at line 661, not the 1500 chars filter.js applies later. Committing 308 MB weekly is unworkable: ~16 GB/year repo growth, AND any single push of a 308 MB file exceeds GitHub 100 MB single-file hard limit and is REJECTED. Task 6 as written will fail at git push. |
| 6 | Re-scoring is pure / tier classification re-runs | PASS | scoreSkill in score.js:19-46 is pure (no side effects; reads only the skill arg). filter.js:170-174 already recalibrates quality_tier from quality_score after all processing. Plan Step 0b re-scores BEFORE Steps 1-4, then existing Step 3 re-tiers - composes correctly. Note: score.js comment block says Featured 80+, Solid 50-79 but filter.js uses 90/70 thresholds - pre-existing drift, not introduced here. |
| 7 | Wave parallelism - Task 2 and Task 3 do not conflict | PASS | Task 2: new file scripts/scrape-pulse.js + package.json. Task 3: only scripts/scrape.js. Both depend on Task 1 being merged first. No file overlap. Parallel execution safe. |
| 8 | Wave 5 verification commands runnable on Windows | FLAG | Task 5 verify uses python -c import yaml - Windows dev box may not have PyYAML. Task 6 uses grep -q which works in Git Bash but not in PowerShell/CMD. Acceptable since CI is Ubuntu. Local pre-CI in Task 7 needs bash, which the env reminder confirms is the shell on this box, fine. |


## Additional Concerns

| # | Concern | Verdict | Evidence |
|---|---------|---------|----------|
| 9 | scoreSkill import path | PASS | filter.js currently does not import scoreSkill. score.js exports it (line 19). Clean addition. |
| 10 | Filter atomicity | FLAG | If filter.js fails mid-write, skills.json is left half-merged. Existing risk, not introduced here. writeFileSync to temp + rename would harden it. Out-of-scope improvement. |
| 11 | Track 1 only refreshes 6 fields | FLAG | repo_topics, repo_license, repo_language, repo_description, repo_default_branch can also change - same GET /repos response, zero extra API cost. Plan should expand TRACK1_FRESHNESS_FIELDS to capture them. |
| 12 | repo_archived populated on every record | PASS | scrape.js:431 always sets repo_archived. writeHistorySnapshot correctly skips archived/forked. |
| 13 | Track 2 incremental returns zero on quiet days | PASS | Task 3 wraps the regression guard with if MODE === full. Zero incremental results = filter sees existing skills-raw.json unchanged and re-emits skills.json with Track 1 freshness. R8 in plan documents this. |
| 14 | Daily commit picks up Track 1 snapshot | PASS | daily-scrape.yml:130 includes data/history/. Today snapshot lands and commits. |
| 15 | data/history/ has only 1 file as of 2026-04-14 | FLAG | Underlying cause is the 6h cap killing runs before commit-back. This phase fixes that root cause. Verify in Task 7 step 5 that today AND a prior snapshot both end up in the repo. |

---

## Goal-Backward Summary

Will executing this plan as written achieve daily scrape under 30 min + healthy data + zero downtime?

Conditional yes - IF the BLOCK on skills-raw.json size is resolved before execution. Without that fix, Task 6 produces a workflow that fails at git push due to GitHub 100 MB single-file limit, defeating the weekly-discover safety-net architecture.

The other dimensions hold up:
- Track 1 design is correct and bounded (Star Pulse, ~826 GETs, fits 5000/hr easily).
- Track 2 mode flag composes correctly with size-range partitioning.
- R3 merge + re-score is correctly placed inside filter.js main flow.
- Workflow restructure preserves all existing steps (filter, embed, upload, build, deploy, health, commit).
- First-run timing math is defensible (~25-35 min on bootstrap-warm cache).
- No schema changes, no new secrets, backward compat for npm run scrape preserved.

---

## Required Revisions (BLOCKERS)

1. **skills-raw.json sizing strategy.** Actual file is 308 MB. Pick one:
   - (a) Gzip-compress before commit (data/skills-raw.json.gz, ~30 MB). Update filter.js to read gzipped input. Update .gitignore.
   - (b) Commit a slim manifest only (id, content_sha, scraped_at per skill, ~5 MB) and rebuild full raw on demand.
   - (c) Use Git LFS for skills-raw.json (verify free-tier ceiling, ~1 GB bandwidth/month).
   - (d) **Recommended.** Do NOT commit skills-raw.json at all. Weekly workflow runs Track 2 full -> filter -> embed -> upload -> build -> deploy -> commit, just like daily but in full mode. The weekly safety net then validates skills.json against a full sweep rather than persisting raw output. Collapses Task 6 substantially; .gitignore stays unchanged; no Task 5 follow-up cleanup needed.

   Update Task 6 action and done. Update R6 risk register.

2. **Fix sizing claim in ARCHITECTURE.md line 183 and CLAUDE.md.** Both claim ~8 MB. Actual is 308 MB. The drift caused the planner sizing error and would mislead future work. Fix as part of this phase or as an immediate follow-up.

---

## Suggested Improvements (FLAGs)

1. **C1:** Add a filter.js comment explaining the slug-vs-id keying choice for the Track 1 freshness merge (slug = stable repo identity for engagement fields; id = stable record identity for content enrichments).
2. **C3:** Add a 30-second behavioral smoke-test to Task 1 verify (run scrape against a tiny seed list, ctrl-C after first repo) to prove no behavior change beyond syntax.
3. **C2:** One-line comment in searchByTopics noting pushed:> filter semantics for incremental mode.
4. **C8:** Replace python -c import yaml verify with gh workflow view daily-scrape.yml or actionlint.
5. **C11:** Expand Track 1 fields to include repo_topics, repo_license, repo_language, repo_default_branch, repo_description - same API call, zero cost, closes a freshness gap.
6. **C15:** Task 7 should assert data/history/ contains today AND a prior snapshot (proves the commit-back chain works post-fix).
7. **Hidden Task 5 follow-up inside Task 6 (PLAN line 987).** Tasks should be self-contained - move the Reset skills-raw.json cleanup step into Task 5 (or eliminate entirely if revision 1 option (d) is accepted).
8. **Task 4 testing.** Plan says pick one approach - make it explicit: prefer the exported-helper unit test; fall back to subprocess only if a blocker is found.

---

## File Outputs

- This report: .planning/phases/03.0.0-split-track-scrape/3.0.0-PLAN-CHECK.md

## Summary for Orchestrator

**Verdict: NEEDS_REVISION.** One blocker (skills-raw.json is 308 MB not 8 MB; weekly commit will fail at git push) plus 8 non-blocking improvements. Architecture is sound; routing back to planner with a focused fix on Task 6 + R6.
